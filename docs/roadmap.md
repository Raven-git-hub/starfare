# Starfare — Roadmap

*Last updated 04-08-26. This is the authoritative, checklist-level roadmap. `docs/design.md` §17 carries only a summary; when the two disagree on **status**, this file wins, and on **design**, that one wins. Update the relevant checklist in the same commit as the work it records — a checked box with no commit behind it is exactly the failure mode the 16-07 reconciliation caught.*

**How phases work here:** each phase is a legitimate finish line, not a checkpoint. A phase isn't done until its **exit criteria** hold, and no phase starts until its **entry dependencies** do. "Phase" refers only to this roadmap — manufacturing levels are *tiers* (design.md §15.2).

---

## Status Dashboard

| Phase | Name | Status |
|---|---|---|
| 0 | Prove it's fun, learn to code | ✅ Done (residual experiments optional) |
| 1 | Playable economy sandbox (widened) | 🔶 In progress — Stage 1 ✅, checklist ✅ (19-07-26); Stage 2: harness + driver + zero-state ✅ (01-08-26), resource representation ✅ (02-08-26), state inspector ✅ (03-08-26), live world→seed + home-seating ✅ (03-08-26), establish-venture action + claims-in-snapshot ✅ (03-08-26), testbed server + container ✅ (03-08-26), testbed web UI ✅ (03-08-26), refining (titanium→alloy) ✅ (03-08-26), home-system picker (/starters) ✅ (04-08-26), system-layout endpoint (/system/:id) ✅ (04-08-26), guild-management overlay (read-only) ✅ (04-08-26), planet manifest + establish-in-UI (/recipes) ✅ (04-08-26), multi-input recipe engine ✅ (04-08-26), refined-goods catalog (11 recipes) ✅ (04-08-26), supply panel by tree-tier (/goods) ✅ (04-08-26), read-only Production view ✅ (05-08-26), production profile as inert owned state (slice 2a-i) ✅ (06-08-26), production gates live — stepProduction reads the profile (slice 2a-ii) ✅ (06-08-26), production resolver extracted + preview/snapshot emission (slice 2b-i) ✅ (07-08-26), interactive Production view — controls + live resolved display (slice 2b-ii) ✅ (07-08-26), review flag computed engine-side (slice 2c-i) ✅ (07-08-26), review flag displayed — badge + issue list (slice 2c-ii) ✅ (07-08-26), engine rewrite to rate-based resolution — continuous rate + stockpile drawdown + per-good batchCarry (recipe-ratio correction), schema 5→6 ✅ (10-08-26), Syndicate fork delivery — commitment goes live, mine→(Syndicate/downstream/stockpile) end to end ✅ (10-08-26), one-pot distribution — priority-ordered claimants + single reserveLevel (Slice A) ✅ (10-08-26), Syndicate windowed accrual (Slice B, engine+client) ✅ (10-08-26), player Production console (/console) ✅ (10-08-26), commitment-injection dev scaffold (licence step 0) ✅ (11-08-26), auto-tick heartbeat + watch UI (server+client, schema-unchanged) ✅ (11-08-26), scripted licence-first bot + lockstep run-recorder (tools/ only, JSONL trace, schema-unchanged) ✅ (11-08-26), testbed durability prototype — file state-snapshot + action journal, opt-in (dev rig, not the Phase-2 server) ✅ (24-08-26), **the resource price engine — `stepPriceRecompute` filled, per-good Syndicate value on state ✅ (26-08-26)**, **licence Slice 3a — commitment-as-sale, committed goods earn credits ✅ (26-08-26)**, **licence Slice 3b-i — the licence entity + fee math, locked at issuance, nothing charged ✅ (27-08-26)**, **licence Slice 3b-ii — the mid-window join pro-rate ✅ (27-08-26)**, **licence-layer hardening — the sale's mis-payment fixed + the tripwire gaps closed ✅ (27-08-26)**, **licence distribution — per-venture met/breach via the pursue-order fill ✅ (27-08-26)**, **console rewire — the Syndicate roster goes per-venture + the pursue ranking ✅ (27-08-26)**, **cycle length ruled — `N` 24 → 1,440, a 24-hour commitment cycle ✅ (27-08-26)**, **the midnight anchor + the calendar layer — `dayAnchorTick` and `sim/calendar.js` ✅ (27-08-26)**, **the operator CLI — `tools/admin.js`, `verify-cycle` as the post-redeploy check (tools/ only, no `sim/` change) ✅ (27-08-26)**, **the player licence panel wired — `establishVenture` + `applyForLicence` from the game client (client-only) ✅ (27-08-26)**, **console & HUD legibility pass — durations not "hours", friendly site names, the synced tick ring; + three tested engine touches (`site.name`, mid-window `met`, the tick-0 label) ✅ (28-08-26)**, **persistent per-good production/consumption history — `guild.productionHistory` in the engine, the console's trend sparklines plotted from it (+ the trend-column squash/placeholder fix) ✅ (28-08-26)**, **per-galaxy UTC offset — "server midnight" becomes an explicit choice at galaxy creation ✅ (28-08-26)**, **licence Slice 3b-iii — the licence fee is CHARGED at the window boundary (discounted if met, full basic if breached; one guild-wide lump; guild credits carved out of invariant 3) ✅ (28-08-26)**, **the console's system inventory panel — the right hero's resting state becomes a per-system, per-tier inventory, plus the Tier-3 catalog seam (`TIER3_GOODS`) ✅ (28-08-26)**, **the embedded console's inventory reaches the player — it rides the venture bridge into the game's right zone (client-only) ✅ (28-08-26)**, **in-game inventory panel cosmetics — name inside the bar, ~2x type/bars, no raw system id (client-only) ✅ (28-08-26)**, **the served client shell goes `Cache-Control: no-cache` — a redeploy is visible on an ordinary refresh ✅ (28-08-26)**, **factory output commits — the Syndicate accrual, sale and fee go producer-general; cascade breach RULED intended ✅ (28-08-26)**, **the TRADE tab + Syndicate SELL — `sellToSyndicate` (player-allocated per system) + the Exchange's first screen ✅ (29-08-26)**, **the price-history store — three coarsening rings per good, so the TRADE graph has data ✅ (29-08-26)**, **the TRADE chart finished — 3D/2W/1M/3M mapped onto the rings, the `priceBase` line, the footer fix, full width (client-only) ✅ (29-08-26)**, **the asset economy slice 1 — assets as individual entities in a per-guild inventory, the 15/10 starter gift at founding, and `establishVenture`'s three-gate occupy (engine-only) ✅ (30-08-26)**, walking skeleton continuing |
| 2 | Persist and tick on a server | ⬜ Not started |
| G | Galaxy generation recovery (parallel track) | 🔶 In progress — generator complete (rings, ×4/×1/×0.25 gradient, repair, validation, settlement slots) + viewer renders `ring` (02-08-26); remaining items are decisions only |
| 3 | Multiplayer foundations | ⬜ Not started |
| 4 | Territory, routes, tolls, travel time (real map) | ⬜ Not started |
| 5 | The political layer | ⬜ Not started |
| 6 | Bots and the Storyteller | ⬜ Not started |
| 7 | Self-hosting, config, onboarding, polish | ⬜ Not started |

---

## Phase 0 — Prove it's fun, and learn to code ✅

**Goal:** know the economy is tense and fun before writing real code, and get far enough in JavaScript to read everything that gets written.

- [x] Walk a single shipment through its full lifecycle on paper
- [x] Build and hand-play the fuel market simulator (`analysis/fuel_market_simulator.xlsx`)
- [x] Run the deliberate squeeze — confirmed drama, confirmed the purchasing-power model, surfaced open question #31
- [x] Extract the simulator's exact formulas and semantics into the design doc as the port contract (design.md §8, done 16-07)
- [ ] *(Optional, any time)* Fight-the-squeeze experiment: replay from turn 4 using only existing levers, to test whether counter-play exists (#31)
- [ ] *(Ongoing)* JavaScript learning — note: `galaxy-map-hex.html`, the intended hands-on textbook, is among the lost artifacts; recover it or adopt `client/seed_viewer.html` as the replacement textbook

**Exit criteria:** met — the squeeze produced real tension and a real open balance question, which is exactly what Phase 0 existed to find out.

---

## Phase 1 — The economy as a playable sandbox 🔶

**Goal:** sit down, play a session as one guild against several bot guilds, and feel a real fuel squeeze happen — with territory, tolls, and ventures as levers you can actually use to cause or resist it.

**Scope was deliberately widened (16-07-26)** from the original one-guild spine. What that buys: an early, honest answer to whether the game's *shape* is fun. What it knowingly leaves unproven: persistence (Phase 2) and real player-vs-player trust (Phase 3+). The guard-rail: territory is an **abstract graph** behind a world-interface boundary — the hex galaxy, coordinates, gate-placement rules, and per-leg risk stay in Phase 4.

Working spec: `docs/prompts/phase-1-sandbox-prompt.md` plus the Stage 1 report (delivered in-session 16-07; key findings absorbed into design.md §8, §15.6, §16, §19).

### Stage 1 — Read and report ✅

- [x] Reconciliation of documents vs repo (found: stale doc copy in circulation; 15-07 generator work documented but lost; missing artifacts list)
- [x] Spreadsheet extracted cell-by-cell; transcription verified; port semantics identified (posted price, force-sell, floor-and-record, over-demand, fractional credits)
- [x] Plan: graph shape, file layout, tick-loop shape, world-interface boundary, bot tiers 1–2, contest design
- [x] Invention table delivered (~24 rows) with proposals

### Decision checklist — ✅ answered 19-07-26 (unblocks Stage 2; rulings in `docs/phase-1-tuning.md`)

Rulings needed from the project owner. "Accept proposal" is a valid answer to any of them; each decision gets recorded in design.md §19 in the same commit that implements it.

- [x] **#45 — Option A or B.** A *(recommended)*: fuel stays instant/Syndicate-delivered; a second raw good moves by shipment and pays tolls. B: fuel-only, produced fuel physically ships to the reserve. Most of the remaining numbers hang on this.
- [x] **#48 — force-sell: keep or drop.** Dropping it enables the pooled fuel-allocation model and the licence-premium anti-hoarding mechanism (design.md §8, `docs/fuel-allocation-model.md`); it re-derives the port's semantic #2 and the squeeze signature. Enforcement of illegal withholding is Phase 5; in Phase 1, dropping it just makes hoarding possible-and-expensive.
- [x] **#42 — posted-price semantics** (proposal: step 3 publishes next window's price)
- [x] **#43 — credit rounding** (proposal: `round(qty × price)`, identical both sides)
- [x] **#44 — scarcity allocation** (proposal: validate-as-they-arrive, whole order fails, fixed bot order)
- [x] **#46 — land rent out of Phase 1** (proposal: yes, tolls + tariffs only)
- [x] Eight-step tick order confirmed as amended in design.md §15.6 (adopted in the 17-07 consolidation; veto here if wrong)
- [x] Invariant 3 per-field rulings (proposals: credits **fail**; consumption **floors + records**; hoard-sales **fail**; reserve over-buys **fail whole order**; venture inputs floor via `min(rate, available)`; influence **fail**; Syndicate ledger exempt/may go negative)
- [x] **#47 — tuning numbers**, decide or accept proposals: player guild start · transit ticks per edge (2 open / 1 secured) · toll unit + default + range · tariff unit + default + range · influence starting stock + earn rate · contest open-cost/min-stake/window · disposition scale/deltas/grudge-multiplier/decay/cap · bot trading band + rolling window + budget floor · bot contest-defense policy · shortfall throttle in sandbox (record-only in the squeeze check) · graph topology (proposed: Citadel + 6 homes + 3 contested middles) · second good's market levers (if Option A)

### Stage 2 — Harness, then walking skeleton

- [x] `package.json` (zero runtime dependencies; tests on `node:test`)
- [x] Invariant checks: conservation of fuel (1), conservation of credits (2, with an explicit Syndicate ledger), non-negativity (3, per-field), determinism (9) — asserting **every tick**, halting loudly with tick number and offending values
- [x] Canonical serialization + state hashing for byte-identical comparison
- [ ] `tick(state, actions)` pure function; all eight §15.6 steps as named functions in fixed order; no DOM access anywhere in `sim/`
- [x] Action intake validated as-it-arrives (state-as-it-stands), first-valid-wins — `actions.js`; wired into the driver and tested end-to-end via `advance` (01-08-26)
- [ ] Walking skeleton: 2 guilds, 3 systems + Citadel, one venture each, fuel market live, one claim, one toll paid end-to-end
  - [x] **First slice landed (01-08-26):** the driver `advance(state, actions)` (`sim/run.js`, intake→tick→assert — invariants now assert every tick in the running loop), the guild-less **zero-state** boot (`sim/scenarios/zero-state.js`), and the conservation-clean **`foundGuild`** action (starting credits debited from the Syndicate ledger). Watchable via `node sim/demo.js`; determinism green through the driver. Remaining on this line: fuel market (step 3), a second guild/bot, a real claim, a toll end-to-end.
  - [x] **Resource representation landed (02-08-26):** guild-level typed `stockpiles` (replacing the typeless per-venture `outputStockpile`); the canonical resource vocabulary `sim/resources.js` (17 raw goods, **drift-guarded** against the generator's archetype pools by a test); the derived `galacticSupply` totals (`sim/supply.js`) written each tick with a **consistency invariant** asserted every tick; and step-1 production now depositing a mining venture's `resourceType` into its owner's stockpile. Watchable via `node sim/demo.js` — Titanium climbs 5→10→15 and the per-good totals + fuel pool print. Remaining on this line: fuel market/pricing (step 3), a second guild/bot, a real claim, a toll end-to-end.
  - [x] **Venture ↔ seed link landed (02-08-26):** the first time the engine references the seed. Ventures carry a `siteId` (a resource node or settlement slot); `sim/seed.js` resolves it against `data/seed.json` (read-only); occupancy is the derived lookup `sim/occupancy.js`; and a new invariant asserts every `siteId` resolves to a real site, one venture per site, and a mining venture's `resourceType` matches its node. Settlement slots were added to the generator in the same slice (see Track G). Watchable via `node sim/demo.js` — the mine now shows seated at `pl_00001_n02 (titanium on pl_00001)`. The tick hot path stays seed-free (only seating-validation and the invariant read the seed).
  - [x] **State inspector landed (03-08-26):** a developer debug lens on live engine state (design.md §7 hides real supply from *players*; this is god's-eye and labelled so on the page). `sim/snapshot.js` is a pure `buildSnapshot(state)` that derives per-good galactic totals, the fuel reserve/hoard split, guild breakdowns, and seated-venture occupancy straight from the engine's own selectors (`computeGalacticSupply`/`computeOccupancy`/`getSite`) — it invents no number; its CLI boots the demo scenario and writes `dev/state_snapshot.json` (gitignored — a throwaway dev artifact). `client/state_inspector.html` loads that file by file-picker (same pattern as `seed_viewer.html`) and renders it. Tripwire `sim/tests/snapshot.test.js` pins every snapshot total to those selectors so the lens can never silently drift; the page was render-verified in headless Chromium. Remaining on this line: fuel market/pricing (step 3), a second guild/bot, a real claim, a toll end-to-end.
  - [x] **Live world wired to the seed + guild home-seating landed (03-08-26):** the live TERRITORY layer stops being a placeholder graph and references the real seed (design.md §15.3). `sim/seed.js` gains a landmark index (`getCitadel`/`getOutpost`/`getOutposts`/`getSystem`/`getLandmark`/`isStarterSystem`/`getTerranHomeworld`/`getSeedNumber`); the zero-state's world is now `{ seed }` and its Syndicate claims are the real **Citadel + 9 outposts** (retiring the invented `home_i`/`middle_i` nodes and the 3 placeholder waystations); `foundGuild` seats a guild on a chosen starter system's Terran homeworld and adds its home claim. Two new invariants assert every tick: **claim-integrity** (each claim's `landmarkId`+`landmarkKind` resolves to a real seed landmark) and **guild-home** (a guild's denormalised home matches the seed's starter-eligibility + homeworld, and a matching ownership claim exists). Watchable via `node sim/demo.js` — the player founds at sys_0002, its mine sits on the homeworld's titanium node, and the 10 Syndicate claims print by real id. 113 tests green; determinism re-verified. Remaining on this line: fuel market/pricing (step 3), a second guild/bot, a toll end-to-end.
  - [x] **establishVenture action + claims in the snapshot landed (03-08-26)** — groundwork for an interactive **testbed** for the tick/advancement system (design's own "the update discipline is where economic games die"). `establishVenture` (sim/actions.js) seats a NEW mining venture, belonging to an existing guild, onto a real unoccupied seed resource node, validated the validate-as-they-arrive way (guild exists; venture id unique galaxy-wide; node resolves, is a resource node, matches the venture's `resourceType`, and is unoccupied; `productionRate` a positive integer) and backstopped by the site-occupancy invariant. It moves no credits/fuel (no site cost yet) and starts producing on the next tick. `productionRate` is operator-supplied, not baked — only Titanium's 5/tick is ruled, so the testbed takes the rate as an explicit dial rather than inventing others. `buildSnapshot` gains the SHARED **claims** layer (each row's seed landmark resolved, with coords) so the debug lens can finally see Syndicate waystations + guild homes; snapshot schema 1→2. Watchable via `node sim/snapshot.js` — the emitted snapshot founds a guild, adds a second mine via the action, and carries 11 resolved claims. 126 tests green; determinism re-verified.
  - **Testbed direction (decided 03-08-26):** this feeds a self-hostable dev rig — a thin Node HTTP server wrapping the SAME `sim/` (one engine, still guarded by the tests/invariants — no browser-side copy), in a Docker container on the project's Docker host (listen port **7331**, the galaxy seed), repo **volume-mounted** so edits show up on restart. State stays **in-memory + ephemeral with a Reset** (a stress-test wants to blow it away and retry); **Postgres is deferred to Phase 2** (its stated headline), the entities already being row-shaped so that swap is a swap, not a rewrite. Sliced: (1) this pure-engine step; (2) thin server + container; (3) authoring UI (found-guild / add-venture / play-pause auto-tick). Slice 2 will ship its own design/roadmap note when the server lands, so "a server arrived early as a harness" is recorded, not silent.
  - [x] **Testbed server + container landed (03-08-26)** — testbed slice 2. `sim/server.js`: a thin Node built-in-`http` server (no framework, no dependencies) that holds ONE in-memory live state and exposes the engine — `GET /snapshot`, `POST /tick` (advance one turn), `POST /action` (intake one action, **NO tick** — manual tick), `POST /reset`. Zero game logic: every mutation goes through the same `intake`/`advance` the tests guard, and the live state lives behind a single get/set holder (the seam Phase 2 swaps for Postgres — no speculative abstraction built now). An invariant trip is caught and returned as a 500 with the live state left on its last good value, so the tripwire is visible and the server stays up. A `Dockerfile` (node:22-alpine, `CMD node sim/server.js`) + `.dockerignore` containerise it on the project's Docker host (listen port **7331**, the galaxy seed); the repo can be volume-mounted so edits show up on restart, and the image is self-contained so it also runs without a mount. Tripwire `sim/tests/server.test.js` boots the real server on an ephemeral port and drives it over HTTP (11 tests): the **manual-tick contract** (an action applies but does NOT tick; a `/tick` does and production mints), rejections (occupied node / unknown type → 200 + reason), 400s on malformed bodies, 404 routing, and reset. 137 tests green. Verified under plain Node here (`node sim/server.js` served correctly); `docker build`/`docker run` are the human's to run on the Docker host. Next: the authoring UI (slice 3) — found-guild / add-venture pickers + a snapshot poll, served so the browser drives all this by clicking.
  - [x] **Testbed web UI landed (03-08-26)** — testbed slice 3, closing the interactive rig. The server now serves a page at `GET /` (`client/testbed.html`); the JSON liveness probe moved to `GET /health` (same shape, so scripts keep working). The page reuses the state-inspector lens (supply table, fuel panel, guild cards with home, ventures/occupancy, and a new territory/claims panel) and adds **Found-guild / Establish-venture / Tick / Reset** controls plus a status line that surfaces each action's accepted / REJECTED-with-reason (and any engine 500). It computes nothing — same-origin `fetch('/snapshot')`, re-rendered after each action and each tick (**refresh-on-action**; manual tick, no background polling), so the galaxy moves only when you press TICK. Verified end-to-end in headless Chromium (found → establish → tick×2 → galactic titanium 10; a repeat-node establish rejected in amber without mutating; reset to zero-state; zero console errors); the server route is tripwired in `sim/tests/server.test.js` (GET / is HTML with the controls present; GET /health is JSON). 138 tests green. Open at `http://<docker-host>:7331/`. This completes the **manual** testbed; auto-tick (the galaxy on its own clock, with tick-interval polling) remains a separate later slice.
  - [x] **Refining engine landed — titanium → alloy ingots (03-08-26):** the first raw→processed conversion, and the first venture kind beyond mining. A **recipe catalog** (`sim/recipes.js`, one recipe: `titanium_to_alloy` = 3 titanium → 1 alloy_ingot `[FIRST-CUT]`) is the fixed set of conversions — a refinery names a `recipeId`, it can't invent one. `sim/resources.js` gains a **processed-goods** category (`alloy_ingots`) distinct from raw resources and from the special fuel; processed goods live in stockpiles and appear in galactic supply (supply + the non-negativity/known-good invariant now range over raw + processed). Step-1 production handles refining: a venture on a **settlement slot** consumes its recipe's input from and produces its output into the owner's stockpile, **up to `productionRate` batches/tick, throttled by available input** (so nothing goes negative — invariant 3 holds without a special case). `establishVenture` now branches mining (resource node + `resourceType`) vs refining (settlement slot + `recipeId`), each validated up front and backstopped by the occupancy invariant (a refinery must sit on a settlement slot and its recipe must resolve). Refinery throughput is operator-supplied, not baked. Verified: a mine feeding a refinery converts titanium→alloy every tick, throttles when starved, and stays green; 152 tests (+14); determinism re-verified. The testbed UI shows it already (alloy_ingots in the supply table, refining ventures in the ventures table); a click-to-establish-refinery UI comes with the per-guild homeworld view next.
  - [x] **Home-system picker landed — /starters + a dropdown (04-08-26):** the per-guild homeworld view, slice 1 (UI quick wins). `sim/seed.js` gains the enumerator `getStarterSystems()` — the list-all counterpart the single-system `isStarterSystem`/`getTerranHomeworld` pair was missing — returning every starter-eligible system `{ id, name, ring, terranHomeworldId }`, id-sorted (deterministic, like `getOutposts`). `sim/server.js` exposes it at **`GET /starters`** (a property of the SEED, not live state, so it's fetched once on page load and never on tick); the server stays zero-game-logic — it just calls the selector. In `client/testbed.html` the Found-guild form's free-text `homeSystemId` becomes a **dropdown** populated from `/starters` (368 real startable homes on seed 7331 — no id to fat-finger, nothing invented), and the **influence field is removed**: starting influence is a recorded onboarding grant (phase-1-tuning.md → Influence, `[FIRST-CUT]` 100) that belongs in the seeding scenario, single-sourced; the debug rig founds at the action's honest **0** default rather than duplicate that number in a form where it could drift (no tick step reads influence yet). Tripwires: `sim/tests/seed.test.js` pins the enumerator (count 368, id-sorted, each starter's `terranHomeworldId` agrees with `getTerranHomeworld`); `sim/tests/server.test.js` pins `GET /starters` (count/shape/sort) and asserts the served page now carries `<select id="f-home">` and no longer the influence input. 154 tests (+2). Verified end-to-end in headless Chromium: the dropdown fills with 368 systems (first `sys_0002 · FEN-6425 (outer)`), found-by-dropdown → establish → tick×2 mints titanium and the guild renders influence 0, a repeat establish rejects in amber without mutating, and the only 404 is the browser's automatic `/favicon.ico` (pre-existing, benign). This completes the **home-system picker**; the per-guild homeworld view continues as the **Guild Management tab** (below).
  - [x] **System-layout endpoint landed — GET /system/:id (04-08-26):** slice 1 of the **Guild Management tab**, the interactive home view. `sim/seed.js` gains `getSystemLayout(id)` — a system's full STATIC territory: its identity plus every planet with its `resourceNodes` (`{ id, resourceType }`) and `settlementSlots` (`{ id }`, id-only, no resourceType per design §15), **empty slots included** so the UI can see where a venture COULD be seated, not only where one sits. It reads a raw-system index (`bySystemRaw`, references into the already-cached seed) added to `buildIndex`. `sim/server.js` serves it at **`GET /system/:id`** (seed data, not live state — cacheable per system in the browser; unknown id → 404, empty id → 400), staying zero-game-logic. This is the clean seam the "keep the tick central" rule demands: the browser fetches the immutable **layout** once, fetches **live** state (occupancy, stockpiles) fresh from the snapshot every render, and composes the two — every displayed figure still traces to a field the engine handed it. Tripwires: `sim/tests/seed.test.js` pins `getSystemLayout` (sys_0002 → 1 Terran planet `pl_00004`, 15 nodes + 15 slots, node0 titanium, slot id-only; sys_0009 → 4 planets; every returned site id resolves via `getSite`; unknown id → null); `sim/tests/server.test.js` pins the route (sys_0002 shape, sys_0009 4-planet, 404). 158 tests (+4). Verified live over HTTP (sys_0002/sys_0009 shapes, 404 on unknown, 400 on empty id, URL-decoding of the id). **Plan for the tab** (agreed 04-08-26, so a later session doesn't relitigate): (a) **per-system stockpiles — ruling B1 (§15.2/§15.4).** *This **supersedes** the original 04-08-26 plan for this point, which read: "no per-system stockpile — the model has no shared/localised pile."* The lease system (§5, 05-08-26) makes resources **system-scoped**: a guild's holdings are a separate pool per system (intra-system free, cross-system shipped). `galacticSupply` remains the Σ-of-holdings **pricing aggregate only** (supply.js), hidden from players by §7 but watchable in this god's-eye lens. Re-keyed in code 05-08-26 (`sim/stock.js`) — a no-op slice, byte-identical galactic supply; see the landed entry below. The tab shows a selected guild's OWN holdings at guild level; any per-system **flow** figure is derived in the **snapshot** (engine-side, tripwired), never summed in the browser. (b) **auto-tick stays parked** as the north-star flag; build against manual TICK. (c) layout is an **endpoint**, keeping the snapshot pure live telemetry. Remaining slices: **(2)** the Guild Management tab shell — guild selector, its holdings, a collapsible per-system list (home first; built to scale to N though a guild controls only its home today), each system's ventures; read-only, keeps the §7 "debug lens — not player UI" label. **(3)** the System Manifest drill-down (system→planet→node/slot, ported from `seed_viewer.html` for look only), with settlement-slot rows added, the Establish buttons wired to the existing `establishVenture` action (mine on node; refinery-with-recipe-picker on slot), and the standalone establish-venture form then **removed** from the testbed.
  - [x] **Guild Management overlay landed — read-only (04-08-26):** slice 2 of the Guild Management tab. A full-screen, `seed_viewer.html`-style detail overlay (`#guild-overlay`, `position:fixed; inset:0`), opened from each guild card's new **MANAGE** button and dismissed with **BACK** or **Escape**. It is a **read-only** god's-eye lens (keeps the §7 "NOT player UI" warnbar) and **computes nothing**: live figures — the selected guild's identity, credits/influence/fuel, holdings (`stockpiles`), and each system's ventures — come from the latest `/snapshot`; each controlled system's static layout (planets → nodes + slots) comes from `GET /system/:id`, fetched once and cached (the seed never moves). The two are composed in the browser into a per-system summary — `N planets · nodes occ/total · slots occ/total` — where **occupancy is live** (from the snapshot) and **the site list is static** (from the layout), so every number still traces to a field the engine produced. A **guild selector** switches which guild is managed; systems are **collapsible**, home first (sorted from the guild's system-kind claims — one today, built to scale to N). Crucially the overlay carries its **own TICK/REFRESH**, so you open a guild, press TICK, and watch its holdings and ventures move **in place** (the single `render(snap)` path re-renders the overlay from the fresh snapshot; the browser holds only view state — which guild, which sections are open). Client-only (`client/testbed.html`); no engine/endpoint change, determinism untouched. Tripwire: `sim/tests/server.test.js`'s `GET /` now asserts the served page carries the `MANAGE` control, `#guild-overlay`, and `#go-guild`. 158 tests. Verified end-to-end in headless Chromium: found → establish a mine **and** a refinery → MANAGE → the overlay shows holdings, the home system's summary `nodes 1/15 · slots 1/15`, and on expand both ventures (`Mine @ …n01 mines titanium`, `Refinery @ …s01 refines titanium_to_alloy`); an **in-overlay TICK** then moves holdings to `alloy_ingots 1 · titanium 2` (mining mints, refining consumes — up **and** down) with the overlay re-rendering in place; Escape closes; the only 404 is `/favicon.ico`. This completes the read-only tab; slice 3 makes it interactive.
  - [x] **Planet manifest + establish-in-UI landed (04-08-26):** slice 3 — the Guild Management tab becomes interactive, and establishing ventures moves out of the standalone form into the game-like flow **galaxy → system → planet → venture** (one venture per site, backed by the occupancy invariant). Inside the overlay, an expanded system now shows its **planets as clickable rows**; drilling into one opens the **planet manifest** — every resource node and settlement slot as a row, composed live (an occupied site shows its venture from `/snapshot`; an empty one shows an establish control). **Deploy a mine** on an empty resource node (extracts that node's resource; `productionRate` an operator dial defaulting to titanium's ruled 5); **deploy a Refinery** on an empty settlement slot via a **recipe picker** fed by the new **`GET /recipes`** endpoint (read-only catalog from `sim/recipes.js` — RULES, not state; change a recipe by editing that file + restarting, never a live mutation). **Factory** is shown **disabled** on every slot — the shape of the slot's dual nature (refinery *or* factory) is on screen, but the manufacturing tier (processed→constructed goods, factory venture type) doesn't exist in the engine yet, so it's honestly greyed out rather than faked. Ventures get **auto-generated ids** (`guild_site`, unique since one venture per site); deploy sends the existing `establishVenture` action and the overlay re-renders in place, flipping the site to occupied. The **standalone establish-venture form is retired** from the main testbed (a pointer note replaces it). On assets: we deliberately did **not** model an asset inventory yet — establishing assumes an asset is on hand — because a held pool only means something once its economy exists (manufacturing to build assets, the market to buy them, droids to staff them, per design §4's infrastructure-asset model); a fake "2 miners" pool would be an invented gate with nothing behind it. The picker is built so its **data source is the swap point**: today it offers the venture kinds the engine supports; when assets become real it lists the guild's available assets, with no change to the galaxy→system→planet→venture flow. Client + one endpoint (`GET /recipes`); no engine change, determinism untouched. Tripwires: `sim/tests/server.test.js` pins `GET /recipes` (titanium_to_alloy input/output) and asserts `GET /` no longer serves the standalone form (`#btn-venture` gone). 159 tests (+1). Verified end-to-end in headless Chromium: found → MANAGE → system → planet manifest (15 nodes / 15 slots, factory disabled) → DEPLOY MINE (node flips occupied, `Mine … rate 5`) → DEPLOY refinery (recipe `titanium_to_alloy`, slot flips occupied) → in-overlay TICK×2 moves holdings to `alloy_ingots 2 · titanium 4` (up **and** down); back-to-system works; only 404 is `/favicon.ico`. Next: the live recipe editor (session-scoped dev overrides + a POST alongside `GET /recipes`), and/or a second/bot guild for real shared contention.
  - [x] **Multi-input recipe engine landed (04-08-26):** the prerequisite for the refined-goods expansion — a recipe may now require **several** input goods at once, not just one (real manufacturing rarely takes a single feedstock). `sim/recipes.js`: a recipe's `input: {good, qty}` becomes `inputs: [{good, qty}, …]` (a single-input recipe is just length 1). `sim/tick.js` refining: batches = `min(productionRate, floor(have/qty) for EVERY input)` — the **scarcest** input caps throughput; then it consumes each input and produces the output, so no stockpile goes negative (invariant 3 holds without a special case) and a refinery missing *any* input is a clean no-op. First real two-input recipe + test case: **`titanium_alloy` = 3 titanium + 1 carbon_products → 1 titanium_alloy** (ratios `[PLACEHOLDER]` — they exist so the recipe runs and tests, to be tuned in the live editor; recorded in phase-1-tuning.md). The processed good **`alloy_ingots` was renamed `titanium_alloy`** to fit the manufacturing-tree naming (`sim/resources.js`; the drift guard covers raw goods only, so processed renames are free). `client/testbed.html`'s recipe picker now renders multi-input labels (`3 titanium + 1 carbon_products → 1 titanium_alloy`). Only the engine's recipe *shape* changed — no new vocabulary beyond the rename; the refined-goods catalog (carbon_fiber_weave, silicon_wafer, …) + the live editor come next as one slice. Tripwires updated: `recipes.test.js` (integrity guard now iterates `inputs`), `refining.test.js` (rewritten: two-input consumption, **scarcest-input throttle drains carbon to exactly 0**, missing-input no-op), `resources.test.js` / `supply.test.js` / `server.test.js` (the rename + `GET /recipes` shape). 159 tests; **determinism re-verified** (same two-input scenario twice → identical tick-50 state). Verified in headless Chromium: the picker shows the two-input label and a refinery deploys and converts. Next: the refined-goods catalog + live recipe editor (ratios set there), then auto-tick.
  - [x] **Refined-goods catalog landed (04-08-26):** the Phase-2 (raw→processed) layer of the manufacturing tree — **10 new processed goods + 10 new recipes** (11 total with `titanium_alloy`), from the approved design list. New goods in `sim/resources.js`: `battery_cells`, `carbon_fiber_weave`, `composite_resin`, `conductive_material`, `heat_resistant_alloy`, `magnetic_assemblies`, `nanotube_cable`, `radiation_shielding`, `refrigerant_fluid`, `silicon_wafer`. Recipes in `sim/recipes.js` (output ← inputs): carbon_fiber_weave ← carbon_products+polymers; nanotube_cable ← carbon_products+nitrogen+silica; battery_cells ← lithium+polymers+nitrogen; conductive_material ← copper+silica; silicon_wafer ← silica+silver+palladium; composite_resin ← polymers+nitrogen; heat_resistant_alloy ← tungsten+helium+carbon_products; radiation_shielding ← lead+titanium+xenon; magnetic_assemblies ← neodymium+xenon+gold+lithium; refrigerant_fluid ← ammonia+nitrogen. The input sets deliberately keep **process gases** (helium/xenon/nitrogen) as manufacturing consumables. **Every ratio is `[PLACEHOLDER]`** — new recipes seed at all-1s (titanium_alloy keeps 3:1 titanium); real numbers come from the **live recipe editor (next slice)**. This slice is **pure data** — no engine or client logic changed (the multi-input engine already runs any recipe; the refinery picker already lists every recipe from `GET /recipes`, so all 11 appear automatically with multi-input labels). `recipes.js` now uses a small local `recipe(...)` constructor so the catalog reads as a scannable table; everything stays frozen (determinism intact). Tripwires: two **pinned** tests (`recipes.test.js`, `resources.test.js`) fix the exact recipe-id set and processed-goods vocabulary, so any catalog change is deliberate and loud; the existing integrity guard already asserts every input/output is a real good. 161 tests (+2). Verified: a new recipe (conductive_material ← copper+silica) produces end-to-end through the real engine with invariants clean, and the refinery picker lists all 11 with no console errors. Next: the **live recipe editor** — session-scoped dev overrides changed via a normal action (RESET reverts to `recipes.js`; determinism preserved), so ratios get tuned by playing and then baked back into the file.
  - [x] **GALACTIC SUPPLY by manufacturing-tree tier + GET /goods (04-08-26):** cosmetic-but-structural — the supply panel's flat good list is now split into the four design-§3 tiers: **1 Raw Materials, 2 Processed Materials, 3 Manufactured Parts, 4 Constructed Assets**. Tiers 3–4 have no goods yet, so they render as labelled "— none yet —" placeholders — the visible skeleton of the tree still to be filled. To avoid the browser hardcoding (and drifting from) the vocabulary, a small read-only **`GET /goods`** endpoint serves the categorized vocabulary from `sim/resources.js` (`{ raw, processed }` — RULES, not state); the client fetches it once (like `/recipes`, `/starters`), caches it, and **buckets the live snapshot totals** under it — quantities stay live, the raw-vs-processed split stays engine-owned. Falls back to a flat list if `/goods` hasn't returned yet, re-rendering into sections on arrival. Client + one endpoint; no engine/state change, determinism untouched. Tripwire: `server.test.js` pins `GET /goods` (titanium in raw, titanium_alloy in processed, no crossover). 162 tests (+1). Verified in headless Chromium: the four sections render, tiers 3–4 show placeholders, and live values bucket correctly (titanium/carbon under Raw, titanium_alloy under Processed) with no console errors. Also logged this session: a PARKED gameplay idea — **input-priority flow** for refineries competing over a shared input (see the deferred-items list above).
  - [x] **Stockpiles re-keyed to system-scoped — ruling B1 landed in code (05-08-26):** the code now matches ruling B1 (§15.2): `guild.stockpiles` went from a flat `{good:int}` to a nested `{systemId:{good:int}}` — a separate pool per system a guild operates in. A new **`sim/stock.js`** owns the nested shape behind three accessors — `getStock(guild, systemId, good)`, `addStock(guild, systemId, good, delta)`, and `guildTotals(guild)` (flatten across systems) — so the structure lives in exactly one file. `stepProduction` deposits/draws in the venture's OWN system pool, keyed by a new **denormalised `Venture.systemId`** (stamped at establish from `getSite(siteId).systemId`, mirroring `resourceType`); `invariants.js` guards it against the site (`venture-system-matches-site`). `supply.js` and `snapshot.js` total via `guildTotals`, so galactic supply is unchanged and the **snapshot still emits a flat guild-total** — the clients need no change. **Behaviourally a no-op** (one guild controls one system today): a found → titanium-mine + carbon-mine + refinery → tick-50 run yields byte-identical galactic supply pre/post (`carbon_products 159, titanium 2, titanium_alloy 81`), and determinism still holds (run-twice hash equal — not a golden pin, so nothing golden moved). Doc moved with code: the §15.2 B1 note and §15.4 Guild.stockpiles/Venture flipped from "pending" to landed. Tripwires: two new `occupancy.test.js` cases (a matching systemId passes; a drifted one is caught) plus every existing stockpile assertion re-pointed through `guildTotals`. 164 tests (+2). Next: the three-gate production flow, built on these system pools.
  - [x] **Resource-processing pipeline slice 1 — read-only Production view (05-08-26):** the first of three slices (slice 2 adds sliders + a per-system production profile and rewrites `stepProduction`'s allocation; slice 3 adds real licences). This slice ONLY makes the CURRENT production picture visible — no sliders, no allocation change, no selling. In `client/testbed.html`'s Guild-Management overlay, each expanded system gains a **MANIFEST / PRODUCTION** sub-toggle; PRODUCTION shows, for that system, **one collapsible section per good PRODUCED** (raw + processed), each with **LEFT producers** (every venture outputting the good — mines for raw, refineries for processed — row = venture id · site · output/tick · commitment, plus a system total) and **RIGHT consumers** (every venture taking it as a recipe input — consumption/tick = recipe input qty × productionRate, the NAIVE demand — plus a downstream-demand total), and a **contention summary** (produced vs downstream demand, SHORT/surplus/balanced + the system's on-hand) that truthfully reflects the engine's **first-come-first-served in establishment order** draw. A **Syndicate-commitment** figure sits beside every venture and in the totals — a **placeholder**, 0/unlicensed for all today. Units are per-**TICK**; per-**hour** is left a clean seam (needs the tick-duration constant, an open tuning item — `docs/phase-1-tuning.md`). Engine/data support (the only engine touches): a **reserved `Venture.syndicateCommitment`** placeholder (engine-owned, 0 = unlicensed; `createVenture` + establish path; design §15.4/§5), and the snapshot now emits a guild's **per-system stockpile breakdown** (`stockpilesBySystem` via `sim/stock.js`) plus each venture's **`systemId`** and **`syndicateCommitment`** (schema 2→3). The browser composes the view from these engine fields + `GET /recipes`, computing no game state. A latent **ruling-B1 bug** surfaced and was fixed in the same slice: a mine founded via `foundGuild`'s inline `ventures` (the demo/CLI path) never had `systemId` stamped, so its output pooled under a stray `null` system and a same-system refinery **starved**; `foundGuild` now stamps `systemId` like `establishVenture`. **No-op proof:** the commitment field + snapshot breakdown are inert for the tick (galactic supply byte-identical with a nonzero commitment stuffed on every venture; determinism re-verified); the systemId fix is a deliberate correctness change (the refinery now converts). Tripwires: snapshot per-system-stockpile + venture-commitment/systemId pins, the `foundGuild`-pools-in-the-right-system + refinery-not-starved pair, the `createVenture` default, and the served-page Production scaffolding (`renderProductionView`, `data-sysmode="production"`, `pc-grid`). **169 tests (+5)**; the real client renderer verified end-to-end against a live server snapshot (sections render, titanium SHORT by 1/tick under a rate-5 mine feeding a rate-2 refinery). **Open item deferred, not invented:** the per-hour **tick-duration** constant (`docs/phase-1-tuning.md`). Next: slice 2 (sliders + production profile + allocation rewrite).
  - [x] **Resource-processing pipeline slice 2a-i — the Production Profile as inert owned state (06-08-26):** the storage foundation for §5's three gates, built to be **provably unread by the tick** so production behaviour is unchanged. A new **`sim/profile.js`** accessor owns the nested `productionProfile` shape (`systemId → { goods: good → {order, downstreamPct, stockpile}, throttles: ventureId → pct }`, §15.4), exactly as `sim/stock.js` owns the stockpile shape — sparse read accessors (`getGoodPolicy`, `getThrottlePct`) fill the structural defaults (`order` = `["syndicate","downstream","stockpile"]`, `downstreamPct` = 100, `stockpile` = `{percent,0}`, throttle = 100) so callers never branch on presence, plus a `setEntry` merge and a `cloneProfile` deep-copy. `createGuild` defaults the field to `{}` and clones any supplied value through `cloneProfile` (no aliasing into engine state), mirroring `stockpiles`. A new **`setProductionProfile`** action stores a per-`(guild, system)` policy — validated for **integer 0–100** ratios, an `order` that is a **permutation of the three forks**, a `stockpile` of `{mode: percent|quantity, value: ≥0 int}`, and **known stockpile goods** (never `deuterium_fuel`) — but **advisory** by design: a `throttles` key naming a not-yet-existing venture, or a `goods` key for a good with no producer, is **deliberately accepted** (reconciled at read time, §5/§15.4), noted in-comment so a future reader doesn't "fix" it. **No number invented** — every value is player-supplied or a structural default. **NO tick step reads the profile** (that is slice 2a-ii): the load-bearing **no-op proof** runs a mine→refinery scenario 50 ticks and asserts galactic supply is **byte-identical** with vs. without an aggressive profile applied, and that determinism holds (run-twice `hashState` equal — confirming `serialize.js` canonicalizes the new field, invariant 9). Decisions taken (unpinned by the prompt): the action payload is one `(guildId, systemId)` window carrying optional `goods` and/or `throttles`, **merged** field-by-field into the profile (not replace); and it does **not** stamp a tick, matching sibling owned-state actions (`establishVenture`) and the profile shape's lack of an `updatedAtTick` field (§15.2 "match how siblings handle it; if they don't stamp, don't invent it"). Doc: §15.4's Production Profile block updated from "reserved" to "landed — `stepProduction`'s consumption the one remaining piece." **190 tests (+21)**, zero failures. Next: slice 2a-ii (`stepProduction` reads the profile — the three gates go live).
  - [x] **Resource-processing pipeline slice 2a-ii — `stepProduction` reads the profile, the three gates go live (06-08-26):** rewrote `stepProduction` (`sim/tick.js`) to consume the System Production Profile. Per guild, per system (sorted for determinism): **sub-step 1** mines fresh raw output into the pool and records it as this tick's `fresh` flow; **sub-step 2** routes each fresh raw good through **Gate 1** (the waterfall — read `getGoodPolicy`, pour `fresh` through the forks in `order`: **Syndicate** takes `min(Σ producers' syndicateCommitment, remaining)` — `0`/inert today, structured for slice 3; **Downstream** takes `min(floor(Gate-2 demand × downstreamPct/100), remaining)` = `supplied`; **Stockpile** floor-by-%/quantity when ranked high, catch-all when last, always staying in the pool) then **Gate 3** (distribute `supplied` across the good's consumers — **FCFS** in establishment order when no consumer has a throttle *explicitly set* (new `hasThrottle` accessor), else **proportional** to each line's throttled ask with the rounding remainder handed to the earliest-established line); **sub-step 3** runs each refinery in lockstep, `batches = min(floor(rate × throttle/100), min over inputs floor(alloc/qty))`, consuming allocations and producing output. **Gate 2** is the naive full-tilt demand (`Σ rate × input-qty`, uses rated `productionRate` not the throttle). **The settled behaviour change — "never touch the pile" (§5):** the forks route only *this tick's fresh* production (`supplied ≤ fresh`), so a refinery whose demand exceeds fresh supply now **stalls at fresh** instead of drawing down the accumulated stockpile as the old whole-pool draw did. Non-negativity holds by construction (draws bounded by `supplied ≤ fresh`, allocations by `supplied`, consumption by allocation). Read-time reconciliation is **structural, not a filter pass**: the tick iterates the real `guild.ventures`, so a stale throttle (named venture removed) is never consulted and a profile-silent venture reads the default — confirmed by tests, no explicit stale-filtering code needed. **Existing assertions that legitimately changed** (never-touch-the-pile, with arithmetic): `refining.test.js` "consumes BOTH inputs" — a rate-2 refinery fed by a rate-5 titanium mine now runs **1** batch/tick (fresh titanium 5 < demand 6 ⇒ floor(5/3)=1), not 2 off the pile, so alloy 2→1 and the pile grows untouched; "throttles to the SCARCEST input" — fresh carbon 1/tick caps it at **1** batch (was 2 drawing the pile), and the accumulated carbon is no longer drained to 0. The slice-2a-i **no-op proof was inverted** (`profile.test.js`): the profile now *moves* output, so the test asserts a hard-throttled/hoarding profile produces **strictly less** `titanium_alloy` than the default. New tripwires (`gates.test.js`, +9): default-profile-≈-old regression, never-touch-the-pile (pile untouched, differs from whole-pool), Gate-1 stockpile floor starves downstream by exactly the floor + downstream-cap remainder to stockpile, Gate-3 FCFS earliest-first + proportional split (two refineries with *different outputs* competing for one short input, so the 7/3 split is visible in the pool), reconciliation (stale throttle ignored / silent venture defaults to 100), and determinism run-twice at **tick 50**. **199 tests (+9 net)**, zero failures. Doc: §15.4 Production Profile block updated to "landed and LIVE." **Reserved:** Syndicate-fork delivery (slice 3), Gate-2 effective demand `[FUTURE]`, the review-flag display (slice 2b). Next: slice 2b (sliders + gate/throttle display in the Production view).
  - [ ] **Production view — interactive (slice 2b): plan ratified 07-08-26.** Recorded so the build reads the decisions, not a chat paraphrase (the mechanism lands in design.md §5 'Display mechanism'): **(1)** the resolved per-line numbers come from the **engine, never the browser** — extract the gate resolution into one pure `resolveProduction` that both `stepProduction` (moves goods) and a read-only `previewProduction(state)` selector (reports) call; the **snapshot** carries the stored `productionProfile` + the preview, and the browser computes no game number. **Preview, not last-tick actuals** (fresh is unrecoverable post-tick; a preview is a pure function of state — can't drift, updates as a control moves). **(2)** sliced small for a line-by-line review: **2b-i** = the `resolveProduction` extraction + `previewProduction` + snapshot emission, a provable **no-op** (galactic supply byte-identical, determinism holds), snapshot schema **3→4**; **2b-ii** = the client controls (fork rank + amounts, per-venture throttles) and the live resolved display; **2c** = the review flag (**moved out of 2b**). **(3)** controls are **numeric inputs + rank up/down buttons**, not literal range-sliders (integer-honest, readable). **(4)** the **Syndicate fork** renders greyed/disabled with a 'no licences yet' label (like Factory today), inert at 0 until slice 3.
  - [x] **Production view — interactive slice 2b-i landed (07-08-26): `resolveProduction` extracted, `previewProduction` + snapshot emission added, no-op proven.** The gate arithmetic that lived inside `stepProduction` moved wholesale into a new **`sim/production.js`**: `resolveProduction(guild, systemId)` is the ONE pure resolver — it reads the profile (via `sim/profile.js`) and the ventures and **nothing from the pool** (under "never touch the pile" the resolution depends only on rates, recipes, and the profile), returning a plain integer report (`{ systemId, mines, goods:{[good]:{fresh,demand,fork:{syndicate,downstream,stockpile},supplied}}, lines:[{ventureId,good,demand,alloc}], refineries:[{ventureId,batches}] }`). **`stepProduction` (`sim/tick.js`) is now just the apply half** — deposit each mine's `fresh` (stamping `updatedAtTick`), run each refinery's `batches` (consume inputs, produce output) — the ONLY place `addStock` is called for production; net pool deltas are byte-for-byte what the old in-line routing produced (deposit-then-run order is additive, so the final pool is identical and non-negativity holds exactly as before). A read-only **`previewProduction(state)`** selector calls the SAME resolver per guild → per system (same deterministic order, null/undefined seatless system included, not dropped). The **snapshot** (schema **3→4**, additive) now carries each guild's stored `productionProfile` (sparse — deep-cloned via `cloneProfile`, exactly as stored so 2b-ii can tell set from defaulted) and a top-level `production` block = `previewProduction`; both are derived/owned-state reads, neither is in serialized state or the determinism hash (`sim/snapshot.js` stays a pure assembler — the preview lives in `sim/production.js`, not the browser). **No-op proof:** galactic supply **byte-identical** over a real mine→two-refinery (+stalled line) 50-tick scenario (`conductive_material` 200, `silicon_wafer` 100, `silica` 50, `carbon_products` 200, `copper` 800), determinism holds run-twice at tick 50, and the **anti-drift tripwire** asserts the report's `mines`/`refineries` predict the exact pool deltas a real `tick` then produces (they can't diverge — one resolver). **No behaviour test moved** — every `gates.test.js`/`refining.test.js` assertion passes unchanged; the only existing-test edit is the snapshot **schema pin 3→4** (a version bump, not behaviour). Purity pinned (state hash identical across a `previewProduction` call) and the sparse-profile + `production`-block snapshot shape pinned. **Decisions taken (unpinned by the prompt):** both `resolveProduction` and `previewProduction` live in ONE new `sim/production.js` (keeping the gate maths in exactly one file and `snapshot.js` a pure assembler, rather than a separate `sim/preview.js`); the report key names are `{mines, goods, lines, refineries}` with per-good `{fresh, demand, fork:{syndicate,downstream,stockpile}, supplied}`; the `production` block is an array `[{guildId, systems:[<report>]}]` (arrays keep deterministic order through `serialize.canonicalize`); no percentage fields in the schema (`alloc/demand`, `alloc/supplied` are render-time presentation, computed in 2b-ii). **209 tests (+10)**, zero failures. Next: slice 2b-ii (client controls + live resolved display).
  - [x] **Production view — interactive slice 2b-ii landed (07-08-26): the view reads the resolved `production` block and the controls send `setProductionProfile`.** **Client-only** — the sole non-doc file touched is `client/testbed.html` (plus its served-page tripwire); no `sim/` logic, snapshot shape, or action changed, so the engine is untouched and determinism holds trivially. **Display switched to engine truth:** `renderProductionView` now sources every per-good and per-line figure from the snapshot's `production` block for the managed guild+system (via a new `findSystemReport`) — per good: `fresh`, the Gate-1 fork split (`syndicate`/`downstream`/`stockpile`), `supplied`; per consuming line: own-max (`demand`), `alloc`, and `alloc` as a percent of own-max (`alloc/demand`) and of upstream (`alloc/supplied`); per refinery: `batches`. The old browser-side naive recompute (`systemProductionModel` + `pcColumn`) and its "naive / does not account for throttling / FCFS" caption are **deleted** — the resolved allocation is now on screen directly. **The only arithmetic the browser does** is those two display ratios, in one helper `ratioPct(numer, denom)` that returns `—` when the denominator is 0 (divide-by-zero guard). **Controls added:** per-good Gate-1 **fork rank** (up/down buttons reordering the three forks; the **Syndicate** fork rendered greyed with a "no licences yet" label — inert at 0, editable amount omitted, like Factory-today), a **downstream cap** numeric input (`downstreamPct` 0–100), a **stockpile** mode toggle (`percent`/`quantity`) + integer value; per-consuming-line Gate-3 **throttle** numeric input (0–100, one per refinery). Controls populate from the guild's **sparse** stored `productionProfile` where an entry exists and from the **§15.4 documented defaults** where it doesn't (held as a named `PROFILE_DEFAULTS` constant citing §15.4 — structural, not invented; defaulted controls show a "(default)" marker). A control change builds a **partial `setProductionProfile` patch** (only the changed field), POSTs via the existing `/action` helper, and re-renders from the returned snapshot through the single `render()` path — resolved numbers move in place; a rejection surfaces the engine's reason in amber (out-of-range values are sent unclamped so the engine validates). **Manual tick only** — no polling, no auto-tick; the §7 "NOT player UI" warnbar stays. **Decisions taken (unpinned by the prompt):** the throttle lives in a dedicated per-system "GATE 3" panel (one row per refinery) rather than repeated inside each good section (a throttle governs the whole line, not per-input-good); controls fire on `change` (commit on blur/Enter) so a re-render never eats a keystroke; markers pinned by the tripwire are `class="fork-move"`, `class="dscap"`, `class="throttle"`, and the `no licences yet` label. **Served-page tripwire** (`server.test.js`) updated: asserts the new control markers present and the removed `pc-grid` / "naive" caption absent — the same client-slice test-tracking pattern as the retired `#btn-venture`. **209 tests, zero failures** — no `sim/` behaviour test moved (engine untouched); the only test edit is the served-page assertions. **Headless-Chromium walkthrough (verified):** found `g1` on `sys_0002` → establish titanium+carbon mines and a rate-2 `titanium_alloy` refinery → open PRODUCTION → titanium line resolves own-max 6 / alloc 6 / 100% / 100% / batches 2 → set downstream cap **50%** → line moves in place to alloc **3** / 50% own / batches **1** (accepted) → set line throttle **75%** (accepted, batches 1) → TICK ×2 moves holdings to titanium 14 · carbon_products 18 · titanium_alloy 2 → cap **150** is **REJECTED** in amber ("downstreamPct … must be an integer 0..100") without mutating → RESET returns to zero-state (guilds 0, tick 0). Zero console errors bar the benign `/favicon.ico` 404. Next: slice 2c (the review flag).
  - [x] **Review flag — slice 2c-i landed (07-08-26): the engine computes the §5 review flag, a proven no-op.** The engine half of slice 2c (the client badge is 2c-ii). Two new **`sim/profile.js`** enumerators — `storedThrottleIds(guild, systemId)` and `storedPolicyGoods(guild, systemId)` (sorted, `[]` when absent; the plurals of `hasThrottle`, keeping profile-shape knowledge in that one file). A new **`reviewSystem(guild, systemId)`** in `sim/production.js` returns a **deterministically sorted** list of issue objects for one `(guild, system)`, computed from the current ventures + recipes: **(a)** `{ kind: 'stale_throttle', ventureId }` for a stored throttle naming a venture no longer in the system; **(b)** `{ kind: 'stale_good_policy', good }` for a stored `goods` policy whose good the system no longer produces (`mineGoods ∪ refineryOutputs`); **(c)** `{ kind: 'unmanaged_surplus', good }` for a **raw** mined good with no in-system recipe-input consumer **and** no stored policy. **(d) computes nothing** — chosen scarcity (a downstream cap / stockpile floor / throttle that starves a consumer while the good piles) is intent, not an oversight, so it is deliberately never flagged; setting any policy for a good silences its (c) surplus flag. Issues sort by `(kind, ventureId|good)`. **Attached on the READ path only:** `previewProduction` now spreads each system's `resolveProduction` report and adds `review: reviewSystem(...)` — `resolveProduction`/`stepProduction` (the move path) are **untouched** (`sim/tick.js` byte-identical; `resolveProduction`'s body byte-identical), so production is a **provable no-op**. Snapshot **schema 4→5** (additive): the `review` array rides inside the existing per-system `production` entry. **No-op proof:** galactic supply **and** the full state hash are **byte-identical to `origin/main`** over a 50-tick scenario carrying a stale throttle + an unmanaged surplus (`ef97b87…` both sides), and determinism holds run-twice at tick 50; `previewProduction` does not mutate state even when it raises issues (hash identical across the call). **Tests (`review.test.js`, +12):** each of (a)/(b)/(c) fires; (c) is silenced by any policy, does not fire when a refinery consumes the good, and never flags a processed output (raw-only); **(d) does not fire** while a capped good genuinely piles over ticks; the sort is pinned; the no-op/determinism/purity are pinned; the snapshot carries `review` at schema 5. **221 tests (+12), zero failures** — no `sim/` behaviour test moved; the only existing-test edits are the two schema pins (4→5) and 2b-i's anti-drift test acknowledging the additive `review` field (its mines/refineries→pool-delta assertions unchanged). **Doc (same commit):** the §5 ruling "Review flag — the exact trigger" is written out (the (a)/(b)/(c) triggers + the (d) exclusion + raw-only scoping) — it had been referenced but not yet spelled out in §5. **Decisions taken (unpinned by the prompt):** issue key names `{ kind, ventureId }` / `{ kind, good }` with `kind` ∈ `stale_throttle | stale_good_policy | unmanaged_surplus`; sort key `(kind ascending, then ventureId|good ascending)`; `reviewSystem` lives in `sim/production.js` beside the resolver (one production-telemetry home) and is attached in `previewProduction`, never in `resolveProduction`. Next: slice 2c-ii (the client review badge + per-issue display).
  - [x] **Review flag — slice 2c-ii landed (07-08-26): the testbed displays the engine's `review`, client-only. This CLOSES the 2b/2c Production-view family.** The sole non-doc file touched is `client/testbed.html` (plus its served-page tripwire); **no `sim/` change** — the display renders the `review` array 2c-i already emits, so the engine is untouched and determinism holds trivially. **Two surfaces, both reading `findSystemReport(snap, guild.id, sysId).review` verbatim:** (1) a **per-issue list** at the top of the Production view (`renderReviewList` / `renderProductionView`) — a labelled "⚠ REVIEW · N items" block listing each issue in the engine's given (sorted) order as a plain-language line, with an "advisory only — the engine never rewrites your stored policy (§5)" footer; it renders even when no ventures remain, so a stale throttle that outlived its venture still surfaces. (2) a **system-header badge** ("⚠ N review") shown in each `sys-head`'s `.ssum` **only when N > 0** (no `⚠ 0` clutter), so an operator spots a flagged system in the collapsed list without drilling in. **Pure presentation:** `reviewIssueText` maps the three engine kinds (`stale_throttle` / `stale_good_policy` / `unmanaged_surplus`) to sentences; the browser re-derives nothing, computes no game state, and never auto-fixes (an unknown future `kind` is surfaced raw rather than hidden). **Served-page tripwire** (`server.test.js`) extended: asserts `class="review-issue"` and `class="review-badge"` are served — the same source-literal pattern as `fork-move` / `data-sysmode`. **221 tests, zero failures** — no `sim/` test changed (engine untouched); the only test edit is the two added served-page assertions. **Headless-Chromium walkthrough (verified):** found `g1` on `sys_0002` → establish a lone titanium mine (no refinery) → the collapsed system header shows **⚠ 1 review** → open PRODUCTION → the list reads "titanium is unallocated — mined here but nothing consumes it and no policy is set." → establish a carbon mine + a `titanium_alloy` refinery that consumes the titanium → REFRESH → the badge and the issue **clear** (review block removed); a system with no issues shows **no** badge. Zero console errors bar the benign `/favicon.ico` 404. **Decisions taken (unpinned by the prompt):** the review block sits at the **top** of the Production view (above the resolved-figures note) so it is seen first; the badge lives **inside** `.ssum` (prepended to the occupancy summary, right-aligned) rather than as a separate header element; the flag is styled **amber** (`--fuel`), not error-red (`--warn`), to read as advisory/caution — matching the rejected-action and "no licences yet" amber; class names `review-block` / `review-head` / `review-issue` / `review-foot` / `review-badge`. This lands the last piece of the read-only-view → interactive-controls → review-flag arc (slices 2b-i, 2b-ii, 2c-i, 2c-ii).
- [x] Determinism test green: same start, run twice, identical hash at tick 50 *(satisfied 06-08-26 by `gates.test.js`'s tick-50 run over a real mines→two-refineries scenario with a Gate-1 stockpile floor + Gate-3 throttles — the first determinism test with substantive production content running the full gate resolution, beyond `run.test.js`'s earlier tick-10 plumbing check)*
- [ ] **Gate:** shown running before scaling up
  - [x] **Resource-processing pipeline — engine rewrite to rate-based resolution (designed 10-08-26, §5 "Engine rewrite" + "Correction — the recipe ratio"; landed 10-08-26):** replaced the shipped `floor(batches)` resolver, which shed the fractional part every tick (a rate-1 line at 67% stalled at 0). New model, live in `sim/production.js`: a line runs at a **continuous rate = min(throttle cap, scarcest input's alloc/qty)** (no floor stall); the **stockpile is a drawable input source with a per-good `reserveFloor`** (reserve line — lands the deferred drawdown, folded into Gate 3 per Ruling 1: the pool = downstream fresh take + `stockpile − reserveFloor`, split proportionally, order-independent 5/5); and the **bottleneck cause** (`{rate, bottleneckGood}`) is first-class derived telemetry (the starved-line warning surface — engine emits, no threshold). Gate 2's **effective (cross-input) demand** is realised through the min-rate coupling (a line held by a scarce partner input under-draws the rest), not by changing the naive Downstream-cap figure. **Recipe-ratio correction (same slice, build-review):** the first cut rounded each input to a whole unit while accruing the output fractionally, which broke the recipe ratio (a 1:1 line ran ~1.5:1). Corrected to **per-good sub-unit carries** — every input AND the output carries its own remainder in `Venture.batchCarry` (`{good: fraction in [0,1)}`, fenced with its own tripwire), all paced by the one rate: `carry_g += rate × q_g`, `floor` drawn/minted, remainder carried. The explicit spill-back is retired — a non-binding input is simply **under-drawn**, its surplus left in the pool. The recipe ratio now holds on average (bounded transient); a new **recipe-ratio tripwire** guards it (red on the pre-correction build, green after). Non-negativity holds by construction (each line draws ≤ its alloc; the pool ends ≥ the reserve floor). A deliberate **behaviour change**: `gates.test.js`/`refining.test.js`/`production.test.js` were rewritten to the rate-based-with-carries truth. Snapshot schema **5→6** (additive: `drawable`/`pool` per good, `drawn` per line, `{rate,bottleneckGood,minted,batchCarry}` per refinery, `batchCarry` per venture); the testbed keeps rendering (shows the effective rate/tick). **232 tests, zero failures.** Parked (design, not this slice): the **adviser layer** (Plant Manager et al.) and the **player-facing Production console** (mocked 10-08-26). Deferred constants → `phase-1-tuning.md` (starved-line warn threshold, per-hour tick-duration).
  - [x] **Resource-processing pipeline — Syndicate fork delivery (the engine cut) (designed + landed 10-08-26, §5 "Syndicate fork delivery — commitment goes live"):** closed the last inert seam in the production engine — the Gate-1 **Syndicate fork now DELIVERS** committed output. Two touches: (1) `resolveProduction` (`sim/production.js`) computes the aggregate commitment (Σ `syndicateCommitment` over mines producing the good) **before** the no-consumer skip, and skips a good only when it has neither consumers nor commitment — so a committed **mine-to-sell** good (no in-system consumer) now routes its Gate-1 waterfall and emits a `goods` entry (Downstream 0, Syndicate `min(commitment, fresh)`, Stockpile catch-all); goods **with** consumers are untouched (the fork was already routed first). (2) `applyProduction` (`sim/tick.js`) adds a delivery step after deposit/draw/mint: per good (sorted, deterministic) it removes `fork.syndicate` from the pool — the committed units **sink into the Syndicate's infinite backend** (no receiving entity; `invariants.js`'s galactic-supply check already models selling as a sink). Delivery is **instant, per-tick, full commitment** — no licence subsystem, fee, breach, buffer, dial-down, equity, or dividends (all re-homed to the future licence layer; the dialled-down actual-send has no data home yet, §5). Commitment is a **fed-in placeholder** (set via `createVenture`'s existing `syndicateCommitment` param; no action/Licence entity). **Provable no-op for the unlicensed** (`0` for every venture today): the existing 232-test suite is unchanged. Non-negativity holds by construction (with consumers the pool ends ≥ `stockpile fork + reserveFloor`; with none, ≥ `start + fresh − delivered`, and `delivered = min(commitment, fresh) ≤ fresh`). New tripwire `sim/tests/syndicate-delivery.test.js` (+5): committed-mine-feeding-refinery (`fresh − delivered − consumed`, galactic drops by exactly the commitment), mine-to-sell delivers with no consumer, over-commitment caps at fresh (never negative), the unlicensed no-op, determinism at tick 50. No snapshot schema bump (`fork.syndicate` already in the preview). **237 tests (+5), zero failures.** Reserved (later slice): refinery-**output** commitment (this sums over mines only); the whole licence economy.
  - [x] **Resource-processing pipeline — one-pot distribution with priority-ordered claimants (Slice A) (designed 10-08-26, §5 "The distribution model"; landed 10-08-26):** reworked the per-good distribution from "route fresh through the Syndicate/Downstream/Stockpile forks + a separate Gate-3 stockpile drawdown" into **one pot with three priority-ordered claimants**. Per good, per system: `pot = start-of-step reserve + fresh`; the claimants draw in the profile's `order` — **Reserve** holds `min(reserveLevel, available)` (stays in the pot, lowers `available`); **Production** offers consumers `min(demand×downstreamPct, available)` and `available` drops by what they *actually* draw (the existing rate-based Gate-3 resolution — throttles, min-rate cross-input coupling, per-good `batchCarry` — unchanged); **Syndicate** delivers `min(commitment, available)` to the sink, now drawn from the pot in priority order (so it may come from reserve, not just fresh; commitment still `0`/inert). A finalize pass places Reserve/Syndicate with the actual consumer draw known, so a claimant ranked below Production sees the undrawn remainder. **Profile shape change** (`sim/profile.js` + `setProductionProfile`): the per-good `stockpile {mode,value}` fork amount **and** `reserveFloor` are **replaced by the single `reserveLevel`** (int ≥ 0, default 0); a stored profile with the old keys is a harmless migration (ignored, absent → 0). **Report shape**: per-good drops `drawable`/`pool` for `reserve0`/`pot`/`reserveDelta` (the console's trend arrow); `fork.{stockpile,downstream,syndicate}` report reserve HELD / consumer DRAW / syndicate DRAW; `supplied` = the consumer draw cap. `applyProduction` unchanged in shape (deposit fresh, draw `drawn`, mint, deliver `fork.syndicate`). Non-negativity by construction (every claim ≤ `available`; the pot ends ≥ 0). Deliberate **behaviour change**: how `downstreamPct` interacts with the reserve is now "cap on total consumer draw from the pot" (was "cap on fresh, reserve drawn separately to the floor") — the gates/refining/production behaviour tests moved to the one-pot truth and a `reserve-priority.test.js` was added (reserve held by priority 1st-vs-last, syndicate drawing from reserve, priority deciding a tight pot, reserveDelta, non-negativity, determinism). The recipe-ratio and determinism guards stay green. Testbed kept rendering (its stockpile mode/value control became a single reserve-level input). Schema stays **6** (unreleased; its per-good shape redefined in place). **242 tests (+5 net), zero failures.** Out of scope → **Slice B**: the windowed accrual, the paced (required-rate) Syndicate draw, met/breach, the commitment telemetry (% / required-rate), the Licence entity/fee, and the player Production console. Deferred numbers (window `N`, tick-duration) stay `[FIRST-CUT]` in `docs/phase-1-tuning.md` — not invented here.
  - [x] **Resource-processing pipeline — Syndicate windowed accrual (Slice B-i; engine + snapshot) (designed 10-08-26, §5 "The Syndicate commitment is a per-good windowed accrual" + the four boundary rulings; landed 10-08-26):** replaced Slice A's per-tick full Syndicate delivery with the **per-good windowed accrual**. The commitment is now an **aggregate target `Q`** = Σ `Venture.syndicateCommitment` over the mines producing the good (× the window **fraction**, pinned to **1** — the mid-window-join pro-rate stays deferred, guarded by a `fraction == 1` tripwire), delivered over an **`N`-tick window** at a **paced** required-rate default `(Q − delivered) / ticks-remaining` (ticks-remaining counted inclusive so the pace is the minimum steady send; the §5 divide-by-zero is honoured as a defensive guard, not in-window arithmetic — flagged as a build-time mechanical pin) or the player's **absolute** / **percent-of-fresh** control (`syndicate {mode,value}` on the good's profile — `sim/profile.js` accessor + `setProductionProfile` validation). The fractional send lands on integers via a per-good **send carry** (floor-and-carry, `batchCarry` discipline, fenced `[0,1)` with its own invariant tripwire). The fork draws **FRESH ONLY** (capped at the fresh not yet consumed — never reduces the reserve), **never exceeds `Q`** (`delivered ≤ Q`), **self-terminates** at `Q` (surplus overflows to stockpile), and at the boundary (`tick % N == 0`) resolves **met/breach** — a status flag, **no fee** — then the next tick rolls (`delivered` → 0, `windowStart` re-stamped). **Window state's home (the pinned data-model call, §15.4):** a new guild-level `Guild.syndicateWindows` = `systemId → good → { windowStart, delivered, sendCarry }`, reached only through a new **`sim/windows.js`** (parallel to `sim/stock.js`/`sim/profile.js`), created **lazily** by `applyProduction` only for a committed good. **`N`** is the single engine-wide `state.windowN` (absent unless a scenario sets it; the resolver falls back to the flagged `FIRST_CUT_WINDOW_N` = **24** `[FIRST-CUT]` → `phase-1-tuning.md` — not invented in game logic, overridable per scenario/test). `windowStart`/`delivered`/`sendCarry` are serialized (invariant 9); the telemetry (`%`-achieved, delivered, required-rate, ticks-remaining, resolved send, met/breach status) is derived, carried per good in the snapshot's `production` block, **schema 6→7**, and enters no hash — the client renders it and computes nothing (§5 display rule). **Provable no-op for the unlicensed:** a commitment-0 run creates NO window state and no `windowN` field, and is **byte-identical to pre-Slice-B `HEAD`** (`d5e267b1…` both sides) — the key no-op proof. Behaviour change for licensed ventures (per-tick full → windowed paced): `syndicate-delivery.test.js` and `reserve-priority.test.js`'s commitment pins were rewritten to the windowed truth (fresh-only, paced), and a new **`window-accrual.test.js`** (+11) ships the mandated tripwires (fraction==1 deferral guard; send carry ∈ [0,1); delivered ≤ Q; fresh-only zero-fresh-delivers-0; boundary met/breach + roll; self-terminate overflow; impossible-pace breach guarded; galacticSupply drops by exactly delivered; determinism over a multi-window run; the send carry preserves determinism). **255 tests (+13 net), zero failures.** Out of scope → Slice B-ii (the testbed UI control + resolved-send/pace readout); the licence-management layer (per-licence split, window-end reserve-backfill); the breach fee; the mid-window-join pro-rate.
  - [x] **Resource-processing pipeline — Syndicate windowed accrual, testbed client (Slice B-ii; display only) (designed 10-08-26, §5 "Because percentage mode does not self-correct…" + the Display mechanism rule; landed 10-08-26):** surfaced the windowed accrual in the testbed Production view — **client-only, no engine change**. `client/testbed.html` replaces the Syndicate fork's `no licences yet` placeholder with the **send control** (a three-way **Paced | Absolute | Percent** mode selector + a value input for Absolute (units/tick) and Percent (% of fresh)), following the `dscap`/`reserve-level` pattern and POSTing only `{ goods: { [good]: { syndicate: { mode, value } } } }` through the existing `setProductionProfile` (no new action), and renders the **windowed readout** from the snapshot's per-good `window` sub-object **verbatim**: %-achieved, delivered/Q, the **required-rate (pace) AND resolved send side by side** (the §5 rule the slice exists to satisfy — a percent-mode player can see the drift toward breach), ticks-remaining, met/breach status. An uncommitted good (Q 0 — every venture in the live testbed today) shows a muted "no commitment yet" while the control stays settable (advisory). The `syndicate` field + `syndicateSet` flag join `goodPolicy`; the served-page tripwire (`server.test.js`) swaps the `no licences yet` assertion for `syn-mode`/`syn-value`/`syn-readout`. **Proof it is display-only:** NO `sim/` engine file changed (the only `sim/` edit is `server.test.js`); the full suite is unchanged at **255 tests, zero failures**; the client→engine contract was checked end-to-end against the real server (the Percent/Absolute deltas accepted and read back, a negative value rejected by the engine in amber), and the pure render helpers were unit-evaluated (pace and resolved send emit side by side; met/breach status classes; "no commitment yet" for Q 0). **One deferral surfaced, not invented:** re-selecting **Paced** after a set mode needs an engine **clear-path** (`setProductionProfile` only merges and its validation rejects a null/empty `syndicate`, so there is no delete) — out of this display-only slice's scope; it is surfaced as a flagged status message and recommended as a tiny follow-up engine slice, rather than reaching into the engine here. This CLOSES the Slice B family (B-i engine + B-ii client). Deferred → the licence-management layer (per-licence split, window-end reserve-backfill), the breach fee, the mid-window-join pro-rate.
  - [x] **Production Profile — the `setProductionProfile` clear-path (the Slice B-ii follow-up) (10-08-26):** gave the action a way to **return a per-good field to its default**, completing the sparse-profile model (§15.4: absent = default is the single canonical encoding). Each per-good field is now **tri-state**: a valid value **sets**, an omitted/`undefined` field is **left untouched**, and **`null` CLEARS** — `sim/profile.js` `setEntry` deletes the key (via a small `applyField` helper generalised uniformly across `order`/`downstreamPct`/`reserveLevel`/`syndicate`), removing a good entry once its every field is cleared so the profile stays truly sparse; `sim/actions.js` `setProductionProfile` validation now **accepts `null`** for a field as the clear instruction (it was rejected before). The testbed's Syndicate **"Paced"** selection POSTs `{ goods: { [good]: { syndicate: null } } }` — a real clear, replacing Slice B-ii's deferred amber message — so the send control round-trips back to the paced required-rate. **Action-layer + client only:** `sim/production.js`/`tick.js`/`windows.js` are **untouched** (a cleared field is absent, and the resolver already reads absent as paced), and the unlicensed run is **byte-identical to pre-slice HEAD** (`d5e267b1…`, hash unchanged). New tripwires (profile.test.js +4, actions.test.js +1): null accepted (not a validation error), the set→clear round-trip reverts to default, clearing a never-set field is a harmless no-op, a clear leaves the good's other fields intact, and the clear generalises to the other per-good fields. **260 tests (+5), zero failures.**
  - [x] **Player-facing System Production Console — `client/console.html` + `GET /console` (a UX probe) (10-08-26):** a second, **player-facing** window over the SAME live engine as the testbed, in the Archive look of `docs/mockups/console_mock6_1.html` (Cinzel + IBM Plex, the void/panel/amber tokens, the flow grid: producers → pipe → the three Gate-1 claimant arms → engine-calc → consumers, with the reserve panel + commitment bar). **Display only — no game logic, no game number in the browser:** every figure is read **verbatim** from the schema-7 snapshot's `production` block (design.md §5 display rule); the mockup's stale hand-data model and its client-side Syndicate pace math were deliberately NOT copied (see `docs/mockups/README.md`). Controls reuse the existing `setProductionProfile`/`throttles` actions — Gate-1 priority (`order`), the single `reserveLevel` (a slider **and** input; the mockup's floor/available %-of-stockpile arithmetic is retired), `downstreamPct`, per-line `throttles`, and the Slice B-ii Syndicate send (Paced|Absolute|Percent incl. the `syndicate:null` clear-path) — plus TICK/REFRESH/RESET, refreshing on action and on manual tick like the testbed. Mockup→engine remaps: the calc chips are `supplied`/`demand`/`fork.syndicate`; the commitment bar is the `window` sub-object (pct-achieved, delivered/Q, **pace AND resolved send side by side**, ticks-left, met/breach) or "no commitment yet" for an uncommitted good (Q 0 — every good in the live testbed today, until licences); tier-3/4 render dim/empty (no goods in the engine yet). `sim/server.js` gains ONLY the static `GET /console` route (same read-from-disk pattern as `GET /`); `client/testbed.html` is untouched. **Proof it is display-only:** no game-logic file changed (`git diff` over `sim/` is just `server.js`'s route + the `server.test.js` pin), so the engine is byte-identical and determinism/no-op are intact; verified end-to-end against the live server that `GET /console` serves and that every rendered figure traces to a snapshot field and every control POST is accepted (incl. the Paced clear). **261 tests (+1 server pin), zero failures.** Parked → a system picker (one system today), and the console lighting up its commitment bar once the licence layer makes a commitment non-zero.
  - [x] **Licence layer step 0 — the commitment-injection dev scaffold (designed 11-08-26, §15.4 "Scaffold 11-08-26"; landed 11-08-26):** the FIRST build toward the licence layer — two **deliberately throwaway** dev-scaffold actions that inject the windowed-accrual placeholders at runtime, so a test scenario can light the console's commitment bar and give a scripted bot real targets **before** the `Licence` entity exists. `setSyndicateCommitment {guildId, ventureId, commitment}` (`sim/actions.js`) writes a venture's `syndicateCommitment` (integer ≥ 0; `0` clears to unlicensed) — the venture must exist AND belong to that guild; **REJECTED (fail-loud, no state change) on a non-`resourceType` venture with `commitment > 0`** (the resolver sums commitment over MINES only, so a refinery's would be silently inert), while `0` is always allowed. It sets `venture.syndicateCommitment` only — it does **NOT** set `committedFromTick` (so `windowFraction` stays 1 and the deferred mid-window pro-rate tripwire stays green) and does **NOT** stamp `updatedAtTick` (matching the sibling owned-state actions, §15.2). `setWindowN {windowN}` — the codebase's **first state-scoped action** (no `guildId`; window length is engine-wide state) — sets `state.windowN` (integer ≥ 1), **REJECTED once `state.tick > 0`** (a setup-only knob fixed before the run). Both route through the existing generic `POST /action` (no `sim/server.js` change), move no credits/fuel, and touch only state the windowed engine already reads — **NO engine/resolver change, NO snapshot-schema bump (stays schema 7), NO fee, NO credits** (breach stays status-only). Two **testbed controls** (`client/testbed.html` Production view): a per-mine commitment input (reads its value back from the snapshot's per-venture `syndicateCommitment`), and an engine-wide window-length input **disabled once `snapshot.tick > 0`** (no readback — `windowN` deliberately stays out of the snapshot); both fire on change, POST unclamped so the engine validates and a rejection surfaces in amber. The **player console (`client/console.html`) is UNCHANGED** — its commitment bar lights automatically from the schema-7 snapshot once a commitment is non-zero. **Provable no-op (the key guard):** a run calling NEITHER action is **byte-identical to pre-change `HEAD`** — two pinned determinism hashes (an unlicensed chain and a committed-via-`createVenture` chain) reproduce exactly, since the engine files are untouched. New tests: `actions.test.js` (+15: the two constructors, set/clear, the non-mine fail-loud with state unchanged, negative/non-integer, wrong-guild/unknown-venture, `windowN < 1`/non-integer, the `tick > 0` refusal with state unchanged, and the committedFromTick-absent guard), a new `commitment-scaffold.test.js` (+5: injected commitment → non-zero `Q` accruing to met/breach at the boundary, breach touches no credits/ledger, the injected `N` moves the `tick % N` cadence, and the two no-op golden-hash proofs), and `server.test.js` (+2 markers: `syn-commit`/`win-n` present in the served page). **278 tests (+17), zero failures.** The real `Licence` entity + breach fee + apply-workflow stay DEFERRED and become `syndicateCommitment`'s source later, retiring these setters; `N=60` is the SCENARIO's runtime value (set via `setWindowN`), never baked in — the flagged `FIRST_CUT_WINDOW_N=24` fallback stays.

  - [x] **Auto-tick (the parked heartbeat) + the watch UI (designed + landed 11-08-26, §15.7 "The auto-tick heartbeat"):** the licence-layer sequence's step (2) — an **OPTIONAL** server-side heartbeat so a running galaxy is watchable without clicking TICK, **server + client only, ZERO engine or snapshot-schema change** (schema stays 7; the only `sim/` file touched is `server.js` + its tests). The manual tick body is extracted into a shared **`tickOnce()`** = `advance(getState(), []) → setState`; **both** `POST /tick` and the heartbeat call it, so there is structurally **one** tick path and "no new game logic" holds by construction. Three module-level fields (timer handle, `intervalMs`, `lastError`) and two endpoints: **`POST /autotick/start {intervalMs}`** validates a positive integer ≥ a **10 ms busy-loop floor** (a dev-rig plumbing guard, *not* tuning — does not go to `phase-1-tuning.md`), then starts (or **replaces**, so you can change speed live) an **`unref()`'d** timer firing `tickOnce()` each interval; **`POST /autotick/stop`** clears it (idempotent 200 no-op when already stopped). Four rulings: **(1)** the interval is **playback speed**, decoupled from the deferred per-hour tick-**duration**; **(2) halt-on-trip** — the heartbeat has no HTTP caller to hand a 500, so a throwing tick **stops the timer** and records `lastError {message, tick}` rather than firing into a broken state forever (the last good state stays live, exactly the `POST /tick` contract); **(3) reset stops the heartbeat** before zeroing (so the clock isn't running mid-redeploy); **(4)** the status is **server state, not game state** — `autotick {running, intervalMs, lastError}` rides on `GET /health`, deliberately **out of the snapshot**. **Watch UI:** the testbed (`client/testbed.html`) gains a play/pause + interval control that POSTs start/stop and, while playing, a client poll loop re-fetching `/snapshot` on a **fixed ~1 s cadence — independent of the server interval** (playback speed ≠ refresh speed) through the existing `render()`, polling `/health` so the button stays honest and a halt-on-trip surfaces (button → paused + amber error); the player console (`client/console.html`) gains only a client-side **LIVE / PAUSE-VIEW** toggle over that same ~1 s poll and **NO `/autotick` control** — a player cannot pause a persistent real-time world, so its "pause" freezes the client's own refresh, not the clock (the galaxy keeps turning; you've stopped re-drawing). Both **suspend the poll re-render while a control is focused** so a mid-run tweak isn't clobbered. The client computes **no game number** (§5) — polling only re-fetches and re-renders. **Proof:** determinism/no-op two ways (a timing-free `tickOnce`×N == advance×N hash equivalence, and a real-timer run — start tiny, let T fires happen, stop, assert T manual advances byte-identical + timer ends stopped & unref'd); halt-on-trip (fault-injected throw stops the timer + records `lastError`, last good state intact); reset-stops; the server endpoints (auto-advance with no manual `/tick`, stop halts, start-while-running replaces the interval, `/health` carries the status, invalid `intervalMs` rejected); and served-page markers (`btn-autotick`/`autotick-interval`; console `btnLive`). New `sim/tests/autotick.test.js` (+5) + `server.test.js` (+7). **290 tests (+12), zero failures.** OUT OF SCOPE (the NEXT slice, not built here): the tick-by-tick **recorder** / the **scripted bot** / the in-process **lockstep harness** (the bot runs in lockstep with a JSONL trace, not on this free-running heartbeat); the **tick-duration** / per-hour wall-clock mapping (deferred tuning); persistence/auth/multi-user (Phase 2).
  - [x] **Scripted reactive bot + lockstep run-recorder (landed 11-08-26):** the licence-layer sequence's step (3) — an in-process, deterministic harness that runs a single-guild scenario tick-by-tick, driven by a reactive "licence-first" bot, emitting a per-tick JSONL trace so a run can be analysed and bot profiles developed. **`tools/` only — ZERO engine change** (the `sim/` diff is empty but for the added test; snapshot schema stays **7**). Four small player-tooling files (like `tools/generate_test_claims.js`, not engine): **(1) `tools/run_scenario.js`** — the generic lockstep recorder. It seeds state via `createState` (guild + inline ventures + `windowN`; the committed mine's target seeded through `createVenture`'s own `syndicateCommitment` param — deterministic, no action needed for setup), then loops K ticks: read the snapshot → the WORLD driver returns this tick's scheduled actions → the BOT driver returns the guild's actions → `advance(state, [...world, ...bot])` (the one lockstep primitive) → append ONE JSONL line. Each line = `{ tick, guild credits + per-system stockpiles/reserves, per-good window telemetry (Q/delivered/requiredRate/ticksRemaining/sendThisTick/pctAchieved/status), production (fresh + Gate-1 fork split + each refinery's rate/minted/bottleneck), worldEvents, botDecisions (actions + reasonTag + sensed), intake verdicts }`. A **documented snapshot split** keeps every figure engine-truth (the recorder computes no game number): window/production telemetry is read from the PRE-advance snapshot (the projection the drivers acted on — which carries the met/breach resolution ON the boundary tick, before the post-advance preview rolls the window), realized holdings from the POST-advance snapshot (lag-free actuals). Fully **deterministic** (no `Math.random`/`Date`); a **NULL bot driver** yields a plain state-recorder for a static scenario; a short per-window stdout summary (like `demo.js`) rides on top. **(2) `tools/world_relief.js`** — the world driver: a pure `(snapshot, producingTick)` fn that at a chosen tick T emits `createEstablishVentureAction` for a SECOND titanium mine on another valid titanium node in the SAME system (its output pools with the first under ruling B1) — a supply RELIEF event, fired exactly once. **(3) `tools/licence_bot.js`** — the "licence-first reactive allocator": a pure `(snapshot) → { actions, reasonTag, sensed }`. It GUARANTEES the licence on pace by LEANING ON the engine's self-correcting paced send (keeps the Syndicate fork PACED — never sets `syndicate` — and ranks it FIRST in the Gate-1 order), FEEDS THE FACTORY the residual (`downstreamPct` left at its 100 default, factory ranked last), MANAGES RESERVE as a buffer (ranks the reserve fork second as an active floor, sets `reserveLevel` to the genuine surplus only — dropping to 0 under scarcity so the factory may draw the banked titanium from the one pot; the fork is fresh-only so reserve never feeds the licence, only protects the factory), DETECTS structural infeasibility (paced required-rate > fresh titanium ⇒ the mine cannot meet Q — flagged in the reasonTag, stays licence-first, no thrash), and emits its decisions as a `setProductionProfile` patch, only when its computed policy differs from what is already stored (a steady state emits no action — stability). **(4) `tools/scenarios/supply_relief.js`** — the scenario: one guild on real starter system `sys_0002`, a committed titanium mine (R_t 5, Q 16) + a plentiful carbon mine (R_c 5) + a `titanium_alloy` refinery (r 2), `windowN` 8, K 24, relief at T 12 (R_t2 5). R_t (5) is sized BELOW the licence pace Q/N (2) + the refinery demand 3r (6) = 8, so titanium is squeezed at the start; after T total fresh (10) clears the need and the refinery runs full with a small surplus the bot banks. **These numbers are SCENARIO / STRATEGY parameters** (the author's fed-in dials, like `demo.js`'s numbers) — documented as such in the file, NOT routed to `phase-1-tuning.md`, and no engine number is invented (N is fed through the state's own `windowN`). **Proven** (`sim/tests/run_scenario.test.js`, +7): two runs byte-identical (determinism); the licence is MET at every window boundary while feasible (ticks 8/16/24); during the opening squeeze the licence is served on pace AND the refinery runs below its rated output (rate 1 < 2, bottleneck titanium); after relief the refinery output rises to full (rate 2) and the bot banks a reserve; an infeasible window (Q 96/N 8 ⇒ pace 12 > fresh 5) is flagged AT-RISK; the null-driver recorder produces a valid trace; the snapshot schema is unchanged (7). A committed sample trace lands at `data/runs/supply_relief.jsonl`. **297 tests (+7), zero failures.** Engine + snapshot schema byte-identical. OUT OF SCOPE (unchanged): the decommission/mine-loss supply-crunch scenario (needs a new engine action — the NEXT slice); quota-schedule perturbation; bot profiles as parameter sets / a factory-first stance; multi-guild contention; the breach FEE (status-only); cross-run learning; persistence; wiring the bot into the live HTTP server.
  - [x] **Testbed durability prototype — file state-snapshot + action journal (landed 24-08-26):** opt-in file-backed durability for the testbed dev rig, so a local galaxy survives a restart. Follows `docs/persistence-model.md`'s **B+** shape (in-memory compute + a full-state **snapshot** + an **action journal**) but stays the DEV RIG — **NOT** the Phase-2 server: JSON `state.json` + JSONL `journal.jsonl` files ONLY, no Postgres, no multi-galaxy, no auth (all Phase 2). **The pure engine is byte-for-byte unmodified** (`persistence-model.md`: "the existing `sim/` loop, run unmodified"): the only new file is **`sim/persist.js`** (a thin durability layer, zero game logic) and the only edits are the require + wiring in **`server.js`** (plus these docs). **Opt-in via `STARFARE_PERSIST_DIR`** — set = on; unset = today's pure in-memory behaviour, byte-identical (existing tests + the default run unchanged). `persist.js` is three functions: **`saveState(state, dir)`** atomically writes `serialize.canonicalStringify(state)` (the full STATE, not `buildSnapshot`'s derived view) to `state.json` via **temp-file + rename** (invariant 4 / atomicity — a crash mid-write leaves the old file intact); **`appendJournal(tick, action, dir)`** appends one `{tick, action}` line, `tick` = `state.tick` at apply time; **`loadOrInit(dir, initFn)`** loads the snapshot then replays journal entries through the normal validate→apply path, **halting LOUDLY** if any replayed action fails validation (a determinism break, never skipped). Server wiring: boot `= loadOrInit`; **`POST /action`** validates first, then (write-ahead) **journals the accepted action BEFORE applying** it; **tick** (both `POST /tick` and the heartbeat, through the one `tickOnce()`) `saveState`s after `advance` (per-tick cadence, from the doc); **`/reset`** clears the journal + writes a fresh zero snapshot; a **SIGINT/SIGTERM shutdown hook** saves **then clears the journal** (the shutdown save is MID-INTERVAL, so the just-saved same-tick actions must not replay on top of the authoritative snapshot — mirrors `/reset`) then exits. **The ordering hazard this slice exists to get right** — the write-ahead-log **double-apply trap**: journal entries carry their apply tick, and replay applies only entries with **`tick >= loadedState.tick`**, so a snapshot-baked action still present in a non-truncated journal is idempotent-safe to replay (journal truncation is then a size optimisation, **deferred**, not a correctness requirement). **Proven** (`sim/tests/persist.test.js`, +6): round-trip determinism (`hashState(recovered) === hashState(original)`); crash-window zero-loss (actions after the last save survive via replay); the double-apply guard (a snapshot-baked, still-journalled action is NOT re-applied); and a no-op golden-hash proof that the flag-unset pure path is byte-identical to before this change. **303 tests (+6), zero failures.** By-hand `kill -9` recovery confirmed (guild + mine + tick survived; a post-snapshot action recovered via "replayed 1 journalled actions"). DEFERRED to the decision checklist, not invented: the default persist dir (kept as required operator config — no baked-in path), snapshot-cadence-as-a-tunable-dial, delta-saves, and the journal-retention/rotation policy.
  - [x] **The resource price engine — `stepPriceRecompute` filled (landed 26-08-26; design `docs/licence-and-price-system.md` Part 1, build note `docs/price-engine.md`, constants `docs/phase-1-tuning.md`):** Slice 2 of the Syndicate-economy roadmap, and the **gate for every credit-denominated mechanic**. Every non-fuel good (28: 17 raw + 11 processed) now carries a posted **Syndicate value** on state, recomputed by step 3 every tick from `level × idleness` — where **level** = Σ guild stockpile ÷ production capacity and **idleness** discounts stock being drawn downstream — EMA-smoothed, slew-capped, clamped, and **published two ticks behind**. The direction is the flipped one (`§5`'s superseded pointer): the value **rises** as a good sits in guild stockpiles and **crashes** as the hoard drains, so a hoard *is* the Syndicate's scarcity. `deuterium_fuel` is **never** priced (§8) — a permanent exclusion. **State shape:** `state.prices[good] = { posted, pending[2] }`, where the pipeline's last slot doubles as the **EMA memory** (one home per fact, invariant 5); it is serialized state and joins the determinism hash. **Consumption reaches step 3 without a second production resolve:** `tick()` now carries a per-tick **scratch context** (never state, never hashed) into which `stepProduction` accumulates each good's `fork.downstream` — the resolver's own draw. The `STEPS` order is untouched (§15.6 is a contract). **New `sim/baseline.js`** holds the `[FIRST-CUT]` **droidless baseline output** table used *only* by the pricing capacity denominator — so throttling a mine can't inflate the level; **`productionRate` is deliberately NOT refactored** (that rides with the licence slice, where commitment/fee need it). The snapshot gains a top-level `prices` block (posted only — the unpublished pipeline stays off the lens, or the front-running read dies); **additive, schema still 7**; no price history on state, so the console keeps its client-side ring buffer until the chart-binding slice. **Nothing consumes the price yet** — no sale, no fee, no dividend, no ledger effect, so invariant 2 is untouched (that is Slice 3). **Proven** (`sim/tests/prices.test.js`, +24): rises on a hoard / crashes on a drain; rarity scaling (one producer vs ten, same pile); idleness (consumed stock prices below identical static stock, and the step demonstrably used the resolver's own draw); EMA glide, slew cap and clamp band; the 2-tick lag both as an identity and as a delayed reaction to a stock shock; fuel never priced; determinism; and the **empty-galaxy no-op proof** (boot + ticks is hash-stable and every price sits exactly at base). `invariants.js` gains a **price sanity tripwire** (finite, in-band, right pipeline depth, no fuel row) asserted every tick — prices are floats fenced off the integer sweep, and a NaN would rot silently until the licence slice denominated credits in it. The three existing **golden-hash no-op proofs** keep their original hashes, now asserted against the state with the price block stripped — a *stronger* statement: this slice added prices and changed nothing else, byte for byte. **347 tests (+24), zero failures.** By-hand run on the dev rig confirmed the curve end to end (hoard climbs, a rare good climbs ~3× sharper for the same pile, a refinery eating the hoard crashes it back through base). **Left on the checklist rather than invented:** per-good base prices, per-resource/per-recipe baselines, whether the level curve stays linear, and Part 5's *normaliser confirm*.
  - [x] **The TRADE chart finished — the mapped scales, the base line, the footer fix, full width (landed 29-08-26; mapping `docs/phase-1-tuning.md` §"Price history"; revision note at the foot of `docs/client-wiring.md`):** **CLIENT-ONLY — `client/game.html` plus the mockup and two doc notes; no `sim/` edit, no snapshot change, no new action, and no new number.** The chart was built before `priceHistory` existed, so three things were provisional and one was a bug. **(1)** The scale buttons were the raw ring keys (`fine`/`medium`/`coarse`) and are now the designed **3D / 2W / 1M / 3M**, mapped per the ruled table — `coarse` serves **both** 1M (its most recent 30 samples) and 3M (all 90), which is exactly why the buttons could never be the ring keys. A button appears only once its ring holds samples, honouring the fill-forward rule (no backfill): a young galaxy shows 3D alone, 2W joins ~2 hours in, 1M/3M ~1 day in; selection is held across polls and falls back to the finest available. **(2)** The mockup's dashed **base reference line is drawn**, from the snapshot's `priceBase`, read per good and folded into the y-range so it can never be clipped — and absent `priceBase` draws **no** line rather than an invented one. **(3)** The scale-row/footer collision flagged in the price-history PR is fixed: the chart card's head/scales/foot had the default `flex-shrink:1`, so a short column squeezed the scales row to 16px around a ~27px button which overpainted the footer; all three are `flex:0 0 auto` now and only the chart area gives up space. **(4)** `.tw-wrap` goes `max-width:1440px` → **`none`** — edge to edge, heroes fixed, centre absorbing the width — with `docs/mockups/trade-panel.html` changed in the same commit so the contract matches. Verified headless at 1920×1000 against a 45,000-tick galaxy with all three rings populated. **Deferred:** the chart's aspect ratio (full width made the plot flatter; no target ratio is ruled and the top row trades against the SELL card, so it is flagged rather than adjusted), and BUY / the Open Market book / shipments, untouched.
  - [x] **The price-history store — the TRADE graph gets data (landed 29-08-26; state shape `design.md` §15.4 **Global** + the step-3 note in §15.6; depths + the tier→scale mapping `docs/phase-1-tuning.md` §"Price history"):** **ENGINE ONLY — no `client/` file is touched.** The engine posted only the CURRENT value, so the TRADE tab's chart (shipped the same day) had nothing to draw and held a "gathering history…" empty state. This adds **`state.priceHistory`** — GLOBAL top-level state beside `state.prices`, not per-guild, because the posted price is galaxy-wide: `good → { fine, medium, coarse }`, three fixed-size rings at coarsening cadences (15/288 ≈ 3 days, 120/168 ≈ 14 days, 1,440/90 ≈ 90 days ⇒ ~546 samples a good). **Why three rings:** a raw per-tick sample over three months is ~130,000 ticks × 28 goods — ~3.6M numbers re-serialized into the save and the determinism hash every tick, which is infeasible; coarsening is what makes a quarter affordable. Step 3 point-samples the **CLOSING price** once the tick's posted value is settled — never an average, which would need an accumulator in serialized state for no visible gain. **Clock-free:** the sample is `tick % cadence` and deliberately ignores `dayAnchorTick` (display telemetry, not the economy's cycle), so invariant 9 holds. Sparse: nothing is minted until the first fine bucket closes at **tick 15**, so every run shorter than that stays byte-identical — which is why `persist.test.js`'s 2-tick goldens needed no regen, while the 40-tick scaffold runs were re-pinned with the **strip-and-prove** method (all five prior hashes reproduce exactly with `priceHistory` stripped). Snapshot gains `priceHistory` (verbatim, always emitted) and **`priceBase`** (the per-good base, read through a new `prices.js` accessor, so the chart's reference line needs no number typed in the browser) — both additive, **no schema bump**, the same call the price block itself made. Plus an invariant tripwire: shape, ring caps, finite samples. **Deferred, not invented:** averaged buckets, candlesticks, min–max bands, and backfill (the past was never recorded — the rings fill forward from deploy). The client slice that maps the four scale buttons onto the three rings is recorded in `phase-1-tuning.md` and NOT built here.
  - [x] **The Syndicate BUY engine — a purchase travels and lands (landed 29-08-26; ruling `design.md` §6 "Syndicate Delivery — the BUY side" + its BUILT note; speed `docs/phase-1-tuning.md` §"World shape"):** **ENGINE ONLY — no `client/` file is touched, and no BUY UI: the confirm popup is the next slice, so the action takes its destination as a PARAMETER and is tested directly.** SELL went live the same day; BUY could not, because a purchase's goods **travel**. The engine gains **`buyFromSyndicate`** — the mirror of the sale: `cost = round(qty × postedPrice)` (#43, the SAME single price a sale earns, **no transport fee, no spread**), debited **now**, `guild.credits -= cost; syndicate.ledger += cost` (the mirror of `paySyndicateFee`, so invariant 2 is exact by construction) — and it deposits **nothing**. Instead it resolves the **nearest Syndicate waystation** (`sim/transport.js`: axial `hexDistance`, minimised over `getOutposts()`, ties broken by lowest outpost id — pure, seed-only) and records a **pending delivery** in the IN-FLIGHT layer: `{ ownerGuildId, cargo, destinationSystemId, arrivalTick }`, **no origin, no route, no status** (§6), with `arrivalTick = tick + ceil(distance × CRAFT_SPEED)` — **absolute**, which is the entire persistence story. **`CRAFT_SPEED = 300`** ticks/hex is the ruled `[FIRST-CUT]`, read from `phase-1-tuning.md` and sourced in **one** constant, never inlined; **no new number was authored**. **Step 5 filled, not added:** `stepArrivals` was a wired no-op stub in the eight-step `STEPS` array, and this slice is exactly the seam its comment named — each tick it deposits every due shipment's cargo via `addStock` into the destination system's pool, in a fixed order (arrivalTick, then destination, then owner — invariant 9), and drops it from the list. **It does NOT refresh `galacticSupply`**, deliberately: `sellToSyndicate` must (an action, applied by `intake` with no tick around it) but a tick STEP is followed by `tick()`'s own end-of-steps derive pass, so the tick owns the cache — asserted, not assumed. **A delivery that has not arrived costs nothing** (§15.6's pure schedule): the step reads `arrivalTick` and moves on. **The vanish is the ruling (§6):** if at the arrival tick the guild no longer holds the destination, contact is lost — **no deposit, no refund, no divert** — and it is **conservation-clean** because the goods were never in a stockpile (a scheduled deposit, not held inventory, so galactic supply never counted them) and the cash moved to the ledger at purchase. **ONE predicate decides "holds" at both ends** — `guildHolds(state, guildId, systemId)` (`sim/claims.js`, over the `state.claims` ownership rows): buy-validate refuses a destination it says you do not hold, and the arrival check vanishes the cargo exactly when it later answers false; two readings would mean a delivery legal to schedule and impossible to land. Refused, never clamped: fuel (§8) and any unpriced good, `qty` ≤ 0 or non-integer, a destination that is not a system the guild holds, a system no waystation can reach, and insufficient credits. **Provable no-op:** `state.shipments` has been an always-present empty array since the walking skeleton, so the IN-FLIGHT layer going live moved **no golden hash** — every pinned `GOLDEN_*` in `commitment-scaffold.test.js` and `persist.test.js` is untouched and green. Snapshot gains top-level `shipments` (+ a derived `ticksRemaining` so a later BUY/transport panel can say "arriving in N ticks" without calculating in the browser); **additive, schema still 7**. **Proven** (`sim/tests/buy.test.js`, +19): the debit and the schedule with nothing deposited and invariants green *before* any tick; rounding once on the order; the arrival landing on `arrivalTick` and **not** on `arrivalTick − 1`, into the DESTINATION pool; the vanish with its **counterfactual beside it** (same fixture, only the claim differs) so it cannot pass for another reason; a full serialize→deserialize→fly-to-arrival **mid-flight restart**; the fuel-in-transit sum reading **0** for a BUY cargo, with the tripwire shown firing when fuel is planted in it; determinism twice-run over four concurrent deliveries; and every refusal leaving the state byte-identical. **610 tests (+19), zero failures.** ⚠️ **DEFERRED, flagged not guessed** (both on the checklist below): whether the **Citadel** counts as a waystation (the count/placement ruling names it *beside* the 9 outposts, so outposts only), and — surfaced by measuring the real seed — that **300 ticks/hex is far too slow for the generated galaxy's hex scale** (6–155 hexes to the nearest waystation ⇒ 1.25–32 real-world days per delivery, against §6's "5 hours for one hex"). **NOT built here, on purpose:** the BUY UI/confirm popup (next slice), guild-routed transport with tolls/risk/interception (Phase 4), and any SELL or Open Market change.
  - [x] **The TRADE tab + Syndicate SELL — the Exchange's first executable screen (landed 29-08-26; ruling `design.md` §5 "SELL GOES LIVE"; visual + interaction contract `docs/mockups/trade-panel.html`; wiring note at the foot of `docs/client-wiring.md`):** the market tab stops being a stub. The engine gains **`sellToSyndicate`** — a guild sells stockpile goods to the Syndicate at `postedPrice`, **composed per system**: the action carries a set of `{systemId, qty}` allocations, exactly the named systems drain, and there is no guild-wide auto-drain (§5's rejected "largest-pile-first"). `credited = round(totalQty × price)`, rounded ONCE on the total (#43, the same arithmetic as `commitmentSale`); the Syndicate ledger funds it, so invariant 2 holds exactly; the goods leave the economy; there is no reserve guard; fuel is refused. **The between-tick seam:** this is the FIRST action to mutate a stockpile, and `POST /action` asserts every invariant with no tick in between, so the action refreshes `state.galacticSupply` itself (exactly as `tick` does) — without it the galactic-supply consistency invariant trips and the sale 500s. **Market impact is free:** the drained level lowers the next recompute. The client half renders the mockup's panel in the **game shell's own overlay** (`#tabPanel`) — it reads the snapshot the shell already polls and posts to `/action` directly, riding **no** console/postMessage bridge — with the tier+resource picker, the Holdings hero, the price chart bound to `priceHistory` (see the note below), and the per-system allocation table. **BUY, the guild-to-guild book and shipments stay deferred** (§5), rendered as dimmed placeholders. **NOTE — the price-history engine slice has NOT landed:** no `priceHistory` exists on state or in the snapshot, so the chart holds its honest "gathering history…" empty state and its resolution vocabulary is read from the snapshot rather than named here; nothing was faked or seeded.
  - [x] **The asset economy, slice 1 — assets exist, are gifted at founding, and a venture occupies one (landed 30-08-26; ruling `design.md` §4 "Asset occupancy — deploy, idle, and the build contract" + its BUILT note; the gift `docs/phase-1-tuning.md` §"World shape"; the panel-side corrections at `docs/venture-establishment.md` §2.2/§2.7/§3):** **ENGINE ONLY — no `client/` file is touched, and no deploy panel: the inventory screen is a later client slice, so the existing action shapes barely move and the whole thing is tested headless.** Until now a venture was seated on a site out of nothing — there was no machine behind it. §4 splits the two: a **venture** is the licenced operating entity, an **asset** is the Miner or Factory it runs. This slice makes the asset a real individual entity. **The inventory** is a nested `guild.assets` collection (a sibling of `ventures`/`vehicles`, so ownership is "the guild whose array holds it" and there is no `ownerGuildId` to drift), holding `{ id, kind: 'miner'|'factory', maintenanceCondition }`; a venture points at one through a nullable `assetId`. **"Idle" is DERIVED, never stored** — an asset is idle iff no venture of its owner references it (`sim/assets.js`, the `computeOccupancy` discipline) — so there is no `isIdle` boolean anywhere (invariant 5). **The starter gift** is `[FIRST-CUT]` **15 Miners + 10 Factories**, granted by `foundGuild` as ENGINE POLICY, not an action field (exactly like `fuelHoard: 0`), so `createFoundGuildAction`'s shape is byte-identical and no caller can ask for a different gift; ids are `asset_<guildId>_<kind>_<NN>`, deterministic and stable across runs (invariant 9), zero-padded so lexicographic order agrees with numeric. **A founding's inline ventures occupy too**, in the founding's own order out of the pool it was just granted; a founding carrying more ventures of a kind than the gift holds is **refused loudly** rather than quietly minting extra machines. **`establishVenture` now has THREE gates:** (1) the node is **vacant** (the pre-existing occupancy check); (2) the guild holds an **idle asset of the matching kind** — mining⇢miner, refining⇢factory; (3) the guild **holds the node's system**. On success the asset is **OCCUPIED, NOT CONSUMED** — it stays in the inventory and the venture's `assetId` reference is the whole of what "deployed" means — picked **deterministically as the lowest idle id** (arbitrary but pinned, or the same scenario would deploy different machines on different runs). **Gate 3 closes a real gap:** establish never checked territory before, so a venture could be seated anywhere in the galaxy; it now asks the same `guildHolds` question the BUY side asks, so the two can never disagree. This supersedes Slice 3's *"establishment is deliberately UNGATED"* note — leasing stays the future lever for building on **someone else's** land, but it will be a contract, not the absence of a check. **`maintenanceCondition` is a design-ahead PLACEHOLDER and is INERT:** a fraction with `ASSET_CONDITION_NEW = 1` (an identity — "new = full" — not a tuning number), carried and serialized so the maintenance slice needs no migration, and **nothing reads it, nothing changes it, and it never reaches production**. **The new invariant `checkAssetOccupancy` enforces referential integrity, NOT presence** — every non-null `assetId` names an asset owned by the SAME guild, no asset is referenced by two ventures, the kind matches the venture type, and every condition is finite and in scale — but it never demands a venture HAVE an asset, because the null state is legal by §4 (a venture can outlive its asset) and production does not depend on one; that is also what keeps every directly-constructed test venture valid. **Snapshot (additive, schema stays 7 per the convention):** each guild gains `assets: [{ id, kind, maintenanceCondition, deployedToVentureId }]` — the engine answering idle-vs-deployed so the client never recomputes the derivation (§5's display rule) — and each venture row carries its `assetId`. **The golden delta is exactly the assets and nothing else:** `persist.test.js`'s three committed hashes are **UNCHANGED** when the new bytes are stripped (a `withoutAssets` strip beside the existing `withoutPrices`/`withoutHistory`), with the full hash re-pinned beside them, and the run's credits (120) and production (10 titanium over 2 ticks) asserted unmoved — occupying an asset changes no game number. `commitment-scaffold.test.js`'s five goldens **did not move and needed no regen**: a guild built without founding carries no `assets` key and no `assetId` at all (omit-when-empty, the `equityPct`/`licence` discipline). **635 tests (+25), zero failures.** ⚠️ **Reconciled, not weakened:** `tools/scenarios/supply_relief.js` assembles its guild directly through `createState`, so its mid-run relief mine was refused by gate 2 ("no idle miner") — it now hands itself the same starter gift founding would have granted and attaches its three ventures to it; every property assertion in `run_scenario.test.js` passes unchanged. No other test, scenario or demo tripped either gate: every establish in the suite is already on the founded home system. ⚠️ **Deferred rather than invented:** maintenance itself (decay, repair, the guild-wide spend, the ~4x expected-life anchor — field in, mechanic out); teardown / cancel-licence / redeploy, so **deploy is one-way in this slice** and no asset returns to idle; any asset→production coupling; the other four asset sources (Syndicate commission, open market, self-built Tier-4, Ancients); and the whole client side — the deploy panel, the inventory screen and the asset picker, which now have engine truth to read whenever that slice comes.
  - [x] **Per-galaxy UTC offset — *which* midnight the cycle rolls on becomes a choice at creation (landed 28-08-26; ruling `docs/cycle-and-calendar.md` §2a, refining §2's "server midnight"):** §2 anchored the cycle to "server midnight" but never pinned the **timezone**, so it picked one by accident: the anchor read `date.getHours()` — the **container's** local time — while every display of the server clock renders a UTC ISO string. They agreed only because the box runs with `TZ` unset. The zone is now an explicit **per-galaxy** choice. **`state.utcOffsetMinutes`**, an integer in `[-720, 840]` (UTC-12:00 … UTC+14:00, default **0 = UTC**), added **omit-when-0** exactly like `dayAnchorTick`, defaulted `== null ? 0` by every reader, **frozen and persisted** with the galaxy; `/reset` leaves it unset and the restore path carries the stored value through, recomputing nothing (no clock read on load). **The anchor now computes from UTC + offset:** `minuteOfDayFromDate` reads `getUTCHours()/getUTCMinutes()` (so the container's timezone enters nowhere), a new pure `localMinuteOfDay(utcMinuteOfDay, offset)` shifts it, and the create seam anchors on the result — **still exactly one wall-clock read**, at the same operator seam, with the engine clock-free. **Exposed for display:** `utcOffsetMinutes` rides the snapshot's `calendar` block and `/health` beside `serverTime`; `serverTime` stays a **UTC ISO instant** and each display shifts it, so the player HUD (`11:23 UTC+8`) and the `/inspect` panel now tell the **same** time — the one this galaxy's midnight is measured in, not the viewer's locale (which `/inspect` used to show) or the container's (which the HUD used to show). **Inputs:** the `/inspect` Create form gains a UTC-offset field in **hours** (`+8`, `+5.5`; blank = UTC), and `tools/admin.js new-galaxy` gains `--utc-offset <hours>` — both convert to whole minutes and **refuse** a fractional-minute or out-of-range value rather than rounding it, because a galaxy is anchored **once**. It is an operator/config field, **not an economy number**: no `[FIRST-CUT]` tag, no decision-checklist entry, and the tick path never reads it. **The no-op proof:** omit-when-0 means an unconfigured galaxy writes no field and every committed golden is **byte-identical, no regen** — and the `getHours → getUTCHours` change moves nothing either, since offset 0 on a UTC host (the test env and the deployed container both) yields the same minute the local read did. **518 tests (+16), zero failures** — a new `sim/tests/utc-offset.test.js` (+13: the omit-when-0 and inert-on-the-tick-path proofs; `minuteOfDayFromDate` answering identically under `TZ=UTC`/`Asia/Tokyo`/`America/Los_Angeles`; the offset shift wrapping both ways over midnight; the validator's edges; a **UTC+8 galaxy created at a known UTC instant** landing its first boundary on local midnight with `calendar.minute` tracking local minute-of-day, and the same instant at offset 0 anchoring exactly as before; the save/load round-trip; the snapshot readout; and served-source tripwires that the HUD, `/inspect` and the CLI all carry the offset), plus `galaxy-lifecycle.test.js` (+3 live-server: create → freeze → `/health` → restart; a bad offset **refused, not clamped**, creating nothing; and an omitted offset writing no field to the volume) and the reset test extended. `tools/admin.test.js` +2 (the fractional-hours flag and the hours→minutes conversion): **18, zero failures.** ⚠️ **The limitation, stated not discovered later:** this is a **fixed numeric offset** — no IANA zones, no tzdata, and therefore **no daylight-saving auto-shift**. Right for a non-DST zone; for a DST one the galaxy's midnight is an hour off for half the year, and the remedy is the manual re-anchor §3 already describes. **Deferred rather than invented:** re-anchoring or re-offsetting an **existing** galaxy (§3 keeps that a manual future action — an offset change affects only galaxies created afterwards). Also swept while in the file: `docs/production-console-model.md` still named `FIRST_CUT_WINDOW_N = 24` as `windowN`'s default long after the ruling — corrected to `DEFAULT_WINDOW_N = 1,440` (spotted in #33).
  - [x] **Persistent per-good production/consumption history — the trend sparklines become engine state (landed 28-08-26; state shape `design.md` §15.4 + `docs/production-console-model.md` "Per-good production/consumption history"):** the follow-up the legibility pass left open on the trend columns. The console's two sparklines were drawn from a CLIENT-SIDE rolling buffer: it started **empty on every page load**, so a returning player watched "gathering history…" refill live, and no two viewers ever saw the same graph. This slice moves that history into the engine. **New stored state:** `guild.productionHistory` = `systemId → good → { prod: [int], cons: [int] }`, the last **`HISTORY_N = 60`** producing ticks, reached only through a new **`sim/history.js`** — the fourth map in the `stockpiles`/`productionProfile`/`syndicateWindows` family, minted **lazily** (only a good that actually moved gets a key) and deep-cloned in `createGuild`, exactly mirroring `syndicateWindows`. **One write site:** `applyProduction` pushes one sample per tick and shifts at the cap — `prod` = that good's OUTPUT here (Σ mine fresh **+** Σ refinery `minted`), `cons` = its DOWNSTREAM DRAW (`fork.downstream`). Those are the two numbers the old client buffer recorded, with **one deliberate correction**: it read `goods[good].fresh`, which counts MINE output only, so a refined good with a downstream consumer plotted a flat zero under a visibly minting refinery; summing the report's `mines` + `refineries` is what the console's own producer stack already shows. A series is kept **CONTIGUOUS** (a `0` on a lull, never a gap — a gap would draw a slope between non-adjacent ticks) and is **never back-filled**: the past was not recorded, so the buffer fills forward from deploy. **Snapshot:** each system's entry in the `production` block gains `history` = `{ [good]: { prod, cons } }`, beside `review` — the first piece of that block that is stored state echoed verbatim rather than recomputed, and placed at SYSTEM level because `goods` holds only the goods routed this tick (a mine with no local consumer has no `goods` entry yet still has a trend). **Additive, schema stays 7** — nothing existing changed shape, the same call `perVenture` and `site.name` made. **Client:** `TREND`/`pushTrend` deleted; both columns plot `history[good].prod`/`.cons` straight off the snapshot, so the graph is complete the instant the page paints and survives a reload or a container restart. "gathering history…" now means the genuine cold start only (< 2 recorded ticks). **Plus the two display defects:** the columns were **doubled** 66px → **132px** (viewBox `150×132` and the CSS height move together, keeping the vertical axis 1:1 — at 66 a real swing read as flat), and the placeholder text moved OUT of the `preserveAspectRatio="none"` SVG into HTML laid over the box, so its glyphs are no longer stretched horizontally (the data polyline still uses `none` — stretching a bare index axis is correct, stretching letterforms is not). **`HISTORY_N = 60` is a DISPLAY-DEPTH constant, not an economy number** (it feeds no rate, price or commitment) — given by the human, single-sourced in `sim/history.js`, and deliberately NOT a decision-checklist entry. **Determinism (invariant 9):** the buffer is deterministic stored state computed purely from the tick's own resolver report — no clock, no `Date.now` — so it enters serialization and the hash by design, and the twice-run check passes. **The golden regen, done with proof, not blind-accepted:** the three pinned goldens (`682e42e0…`, `ee6856cc…`, `56401013…`/`53c4f481…`, `e0255c73…`) are **UNCHANGED** and are now asserted against the state with `productionHistory` stripped per guild — so the tripwire says something stronger than before: *this slice added a history buffer and altered nothing else, byte for byte*. The new full-state hashes are pinned beside them (`fa63aaf4…` unlicensed, `e145b142…` committed, `170bedd2…` persist), and each proof asserts there really WAS history to strip, so it can't pass vacuously. New **`invariants.js` tripwire**: every sample whole and non-negative, neither ring past `HISTORY_N`, `prod`/`cons` the same length (they share an x-axis), the shape intact. **Verified in headless Chromium** against a real server (two titanium mines + a carbon mine + an alloy refinery in FEN-6425, 40 ticks then a throttle timeline): **before** — a cold page load shows "gathering history…" visibly stretched in a 66px strip, and even after 30 s of live polling holds only the samples the page itself watched; **after** — the full 40-tick series is on screen at 132px the moment the page paints, with the throttle steps legible in the consumption line. **502 tests (+12), zero failures**; new `sim/tests/production-history.test.js` (per-tick accrual, ring cap + which sample drops, the sparse no-key idle case, contiguity through a lull, what the two numbers are, the snapshot echo + its copy-not-alias, determinism, and the tripwire itself) plus served-page tripwires pinning the plot source, the height and the undistorted placeholder as SERVED BYTES. ⚠️ **Noted, not fixed (a separate slice, not invented here):** the trend card's headline `now` figure still reads `rg.fresh` from the NEXT-tick preview, so for a refined good it shows `0` while the stack below it lists the refinery's minted units — the same `fresh`-vs-`minted` mismatch this slice fixed in the series, one seam over. Open question **#66** (the console's one-tick preview offset) and the `archColor` archetype-dot bug are untouched.
  - [x] **Console & HUD legibility pass — the captions catch up with the day-length engine (landed 28-08-26; design `design.md` §5 status-timing note + §19 #66; contracts `docs/production-console-model.md`, `docs/console-client-and-art.md`; build notes `docs/licence-distribution.md` §3/§4 and `docs/cycle-and-calendar.md` §5):** a batch of DISPLAY defects, all of them the console showing correct engine data through captions written for the old small-window world. **Mostly client-only** (`client/console.html`, `client/game.html`); **three small tested engine/snapshot touches**, no new `[FIRST-CUT]` number, no fee, no new endpoint, **snapshot schema still 7**. **Engine/snapshot (§D):** (1) **`site.name`** — `sim/seed.js` derives a friendly place name for every node and slot at index time, `"<system> <planet's Roman ordinal in its system> · <Node|Slot> n>"` (`pl_00004_n01` in FEN-6425 → **`FEN-6425 I · Node 1`**), parsed from the id's `_nNN`/`_sNN` suffix with the ordinal taken from the planet's place in its system's `planets` array; an unparseable id falls back to the id itself rather than to a plausible invention. The snapshot copies it onto the venture's `site` block — ADDITIVE, derived from the immutable seed, in no serialized state and no determinism hash. (2) **Mid-window `met`** — `pursueFill` now reports `met` as soon as a licence is delivered its whole target, mid-window included, while **`breach` stays boundary-only**; `rollupStatus` carries the same asymmetry so the good-level pill can never disagree with the dots under it. The asymmetry is sound, not convenient: within a window `delivered` only grows and the fill gives each licence its full `qᵢ` before feeding the next, so a full licence cannot fall back, whereas a shortfall is only a fact at the window's END. **Status-only** — no credit, no delivery, no `Q` moves, and the committed golden hash `e0255c73…` and the unlicensed goldens are **byte-identical**, because the stored window carries `windowStart`/`delivered`/`sendCarry` and no verdict (now asserted directly, not just implied). (3) **The pre-game tick has no label** — `dayOf(0)` is `-1`, so a created-not-yet-ticked galaxy rendered `00-1:1439`; `calendar.js` gains **`displayLabel`** (`—` below tick 1, `format` from tick 1 on) and the snapshot's `calendar.label` reads it. The guard is at the DISPLAY seam only: `format`/`dayOf`/`minuteOf` and `calendar.day`/`minute` stay the exact, un-clamped arithmetic, because clamping the maths is the elapsed-time corruption §5 exists to refuse. **Client:** the Syndicate panel's paragraph footer becomes a **three-figure stat block** (window target · delivered · ventures met `#/##`, the last counted off the same per-venture `status` the dots render), keeping one muted line that the fee is not charged yet (3b-iii); `ticksRemaining` is shown as a **duration** (`23h 14m remaining`) — it was printed as "hours" and a tick is one minute, so the readout was **60× wrong** — and the other per-tick rates that called themselves `u/hr` now read `u/t`, matching the honest `…t left` readout (ticks are never relabelled as hours anywhere); every place a raw id used to be shown — roster row, venture chips on BOTH the production and **consumption** sides, the venture hero title and its Planet row — now reads `site.name`, with the venture id kept on the tooltip; the trend column's label `.tc-name` had lost the `.lbl` class it merged and was rendering in the page's sans body font — restated as the mono label its siblings use; and the sparkline's `height:auto` / `.tcard` 52px conflict is replaced by ONE rule at the drawing's own 66px, so the vertical axis renders 1:1 instead of at 79% (a flat series is now drawn down the middle of the box too — normalising it put every point on the baseline, so a steady economy rendered as an empty box with a rule along the floor). **HUD:** the tick ring was a free-running 60 s sweep from page load and drifted immediately; it now takes its **period** from `/health`'s `autotick.intervalMs` and resets its **phase** whenever the poll observes `snap.tick` increment — measured in headless Chromium against a 4 s heartbeat: **before** 24°→115° over 15 s with 0 resets, **after** a full 0→360° sweep with 4 resets. With no heartbeat the ring parks full rather than inventing a cadence. **Verified in headless Chromium** (before/after) against a real server — two licensed titanium mines + an alloy refinery in FEN-6425: the stat footer, the `23H 14M REMAINING` readout, `FEN-6425 I · Node 1/2` on the roster, the thermometer side and the refinery chip, the mono column label, the restored sparkline, and — on a short-window fixture — a licence delivered in full at tick 10 of 30 reading **✔ met** with 20 ticks still to run (`accruing` before the change, same fixture), beside its `1/2` in the new footer. **490 tests (+15), zero failures**; new served-page tripwires pin all four client fixes as SERVED BYTES. ⚠️ **Deferred rather than invented — new open question #66 (`design.md` §19):** the console's cycle countdown and verdict run **one tick ahead of the stored window**, because the whole `production` block is `previewProduction` (the next tick's projection, deliberately, for the anti-drift guarantee) — reproduced exactly at `N = 6` (the boundary verdict shows at `state.tick 5`; at `state.tick 6` the panel already shows the rolled window). It is not a small client fix: the stored window is not in the snapshot at all, so it needs an additive block AND a ruling on which readout reads which. ⚠️ Also noted, not fixed: `archColor` is fed a `planetId` that carries no archetype word, so every dot is the terran default — the snapshot's `site` block carries no `archetype`, and adding one is a separate slice.
  - [x] **Factory output commits — the Syndicate accrual goes producer-general (landed 28-08-26; design `design.md` §5 "LICENCE FEE MECHANICS — RULED", the factory-commitment note + the CASCADE ruling; build notes `docs/licence-fee.md` §"Factory output commits" and `docs/commitment-sale.md`):** the commitment → licence → per-tick sale → boundary fee economy worked end to end **for mines only**. One gate upstream of all of it was mine-shaped: the resolver's target loop opened `if (!v.resourceType) continue` and keyed every target on `v.resourceType`, so a refinery's output could carry **no** commitment, **no** licence, **no** Syndicate-tab row and **no** fee, and `applyForLicence` refused a factory outright — for that **mechanical** reason, never a design one. **Three touches.** (1) **One identity, four readers:** `producedGoodFor(venture)` (`sim/baseline.js`) — a mine's `resourceType`, a factory's recipe **output** good; `baselineOutputFor` delegates its `good` half to it so the two cannot drift, and the resolver's `Q`, `ownerFraction`'s equity split, the licence intake and the boundary charge's verdict lookup all read it (they MUST agree, or a factory's commitment accrues under one key and is paid or judged under another — `ownerFraction` matching on `resourceType` was a live mis-payment waiting to happen: a committed factory matched nothing, weight summed to 0, and the owner silently kept **100%** of a sale its equity terms had already sold a share of). (2) **The seam with teeth:** the Syndicate fork is FRESH-ONLY and a refined good's mine-fresh is 0 — its real fresh is Σ its factories' **`minted`**, which does not exist until AFTER the routing pass (a line's rate is `min(throttleCap, inputCap)`). So the fork's fresh cap — and the good's `pot`, since minted lands in the pool at apply *before* the delivery is subtracted — is completed in the **finalize pass** from a `mintedByGood` aggregate built in the refinery loop, in establishment order. A good is mined **XOR** refined, so one term is always 0 and every mined good is byte-identical; if that ever stops being true the resolver **halts** rather than build one cap from two sources. (3) **Intake opens:** `applyForLicence` prices the fee off `baselineOutputFor` (batches/tick × the recipe's output qty) at the **output good's** posted price and sets `syndicateCommitment = commitmentUnitsFor(...)`; the scaffold drops its `resourceType` rejection, keeping the fail-loud for a venture that produces nothing identifiable. **`applyProduction` needed no change** to deliver, sell or charge — only the verdict *lookup* key moved. **No new number:** `baselineOutputFor` already held the factory baseline. Tier-2/3 is treated **exactly like Tier-1, no special cases**, so Tier 3+ lights up the day a 2→3 recipe exists (none is added here). **CASCADE BREACH IS THE RULING (28-08-26), not a bug to prevent:** there is no double-commit prevention, no input reservation and no starvation forgiveness — commit your titanium and the alloy factory that needed it is starved, mints less, under-delivers and **breaches for the full fee**. Nothing codes it; it is what an honest fresh cap does, and the test pins it with the fed counterfactual beside it so it cannot pass for another reason. **Provable no-op:** an uncommitted factory triggers none of the new paths (no window state, no routing, no credits), and every pinned golden — `GOLDEN_UNLICENSED`, `GOLDEN_COMMITTED_WITH_*`, `persist.test.js`'s three — is **untouched and green**. Client: **no change needed** (the console's `producersOf` already walks refineries; the game panel is already output-good-general) — only the served-page tripwire gained an assertion that the ONE mirrored `BASELINE_RATE` still covers both tiers, so per-recipe baselines cannot silently mis-preview a Tier-2 commitment. **Proven** (`sim/tests/factory-commitment.test.js`, +14, and two rewritten refusal pins): **558 tests (+16 net), zero failures.** ⚠️ **DEFERRED, flagged not guessed:** the **`percent`-of-fresh send mode on a REFINED good** — the control resolves in the routing pass, where a refined good's fresh is still 0, so a factory under `percent` intends 0 every tick and will breach; paced (the default) and absolute are exact. Threading it would mean resolving one good's window in two different places depending on how it is produced, so it is left open, pinned by a test that says so, and listed on the decision checklist below.
  - [x] **Player licence panel wired — the licence economy becomes reachable from the game (landed 27-08-26; design `design.md` §5 + `docs/venture-establishment.md` §2.4–2.6; build note at the foot of `docs/client-wiring.md`):** the licence economy was fully built (3a sale, 3b-i fee, the per-venture distribution, the 1,440-tick anchored cycle) and exercised by the operator console and the tests, but `client/game.html`'s licence panel had never been wired to it — `doDeploy` sent `establishVenture` alone, and the panel said so on screen. A player who set a commitment and deployed got a bare **unlicensed** venture, which is why the console's Syndicate panel correctly read *"no commitment yet"* for everything they built. **CLIENT-ONLY:** one file (`client/game.html`) plus two served-page tests in `sim/tests/server.test.js`; **no engine, action, resolver or snapshot change, schema still 7, and no new `[FIRST-CUT]` number.** **A licensed deploy is TWO actions**, in order, because `applyForLicence` licenses a venture that must already exist: `establishVenture` carrying `equityPct` (§5 makes equity a term of the VENTURE, set at establishment — Slice 3a — not a term of the licence), then `applyForLicence { committedOutputPct, windowDays }` for the same `ventureId`. An **unlicensed** deploy is unchanged: one action, no `equityPct` (the field is optional, so its absence stays byte-identical), no licence. The equity term is sent as `sliderPercent / 100` — the same quantity as `S.o01 × EQUITY_CEILING` but landing exactly, so state records the `0.25` the player chose and not `0.25000000000000006`. **The two-step is NOT atomic and the client says so:** if the establish lands and the licence is refused, the venture exists unlicensed, and the receipt reports the refusal's own `reason`, that nothing is committed or sold, and — when one was offered — that the equity IS recorded on the venture but has no commitment sale to take a share of. Never a success carrying terms the engine did not store. **The fee is an ESTIMATE before signing and the LOCKED figure after:** the engine prices `basicFee` off the posted price at the signing tick, so no pre-sign computation can be the number charged — the panel shows only §5's four-corner **relief** (% of basic), tagged `estimate`, and the fabricated `120 ¢/cyc` readout is deleted; the receipt then prints the engine's own `discountedFee` / `basicFee` at `lockedPrice`, read back off the snapshot, and states that the fee is **locked and NOT YET CHARGED** (3b-iii is not built). **The receipt reads engine truth**: `showSuccess` looks the new venture up through a new `window.__myVenture` (own ventures only, §7) and prints the stored `syndicateCommitment`, `equityPct` and `licence.windowDays` rather than echoing the sliders; if the re-read does not return the venture it says so instead of falling back to the panel's numbers. **The cycle length is no longer mirrored** — the client's `TICKS_PER_CYCLE` constant is deleted and `N` comes live off the snapshot's `calendar.windowN`, so the commitment preview `round(pct × baseline × N)` is exactly the engine's `commitmentUnitsFor`; with no snapshot read it shows a dash rather than an invented number. **Every mock caption that is now false is retired** — the Licence section's `Mock · no engine` tag (→ the green `Real · sent to the engine`), the *"None of it exists in the engine yet"* blanket note, the `Licence Summary` **Mock** tag, `showSuccess`'s *"the licence terms above are not stored"*, and four adviser-reel pages (*"the licence economy is not in the engine yet"*, *"every venture is created with a commitment of zero"*, *"no equity is recorded anywhere yet"*, *"No terms are stored yet"*) — each replaced by what actually happens, with the parts still missing named (the fee **charge**, renegotiation). What is genuinely still design-ahead keeps its own tag rather than hiding under a retired blanket: the **asset** picker (§4, `assetId` reserved — deliberately not started here), the **breach fee**, and **reputation** (#61). **Four mirrored constants, now tripwired:** the browser has no constants endpoint, so the panel mirrors `EQUITY_CEILING`, the fee `CORNERS` (keyed key-for-key with the engine's object), `EQUITY_SHAPE_K` and the mine baseline — a new served-page test asserts each against `sim/licence.js` / `sim/baseline.js`, and asserts the sliders' bounds against `WINDOW_DAYS_MIN`/`MAX` and the ceiling, so an engine number that moves goes red instead of the panel quietly showing the old one. **Verified in headless Chromium** against a real booted server, driving the real drill-down (system detail → planet manifest → the vacant node row): a **licensed** deploy at 20% / 25% equity / 21 days POSTed the two payloads and produced `syndicateCommitment 1440` (= `commitmentUnitsFor(0.20, 5, 1440)`), `equityPct 0.25`, `committedFromTick 1`, `licence { …, lockedPrice 10, basicFee 7200, discountedFee 6184 }`; an **unlicensed** deploy sent one action with no `equityPct` and produced `syndicateCommitment 0`, `licence null`; the good's window then carried `Q 1440` with `perVenture` naming the licensed mine only, and the console's Syndicate panel — which read *"no commitment yet"* before this slice — showed `commitment 1,440 u` with a live licence-progress row; and a forced licence refusal produced an established, genuinely unlicensed venture with the receipt saying REFUSED / UNLICENSED and why. **475 tests (+2), zero failures.** ⚠️ **Deferred rather than invented:** an atomic **`establishAndLicence`** engine action (it would close the partial-failure gap in one intake — noted, not built, and no engine change was made here), and an engine-side **fee-preview endpoint** (it would make the pre-sign figure exact rather than a labelled shape). **Out of scope and untouched:** the fee charge (3b-iii), the asset economy, and the operator console's own display fixes (the "hours" mislabel, the preview offset, the icon, the tick-0 label) — a separate console slice.
  - [x] **Console rewire — the Syndicate roster goes per-venture, and gains the pursue ranking (landed 27-08-26; design `design.md` §5 "Breach is judged PER VENTURE"; contract `docs/production-console-model.md`; revision note at the foot of `docs/client-wiring.md`):** the display half of the licence-distribution slice. **CLIENT-ONLY** — one file (`client/console.html`) plus four served-page assertions in `sim/tests/server.test.js`; **no engine, resolver or snapshot change, schema still 7**, and no new `[FIRST-CUT]` number. §5's display rule holds throughout: every target, delivered figure and verdict on these rows arrives in the snapshot; the browser orders rows and re-sequences ids, and computes **no game number**. **What was wrong:** `synRoster` passed the single good-level `R.windowStatus` into every `licRow`, so two mines with different fates showed the **same dot**, and each row showed a stored commitment with no delivery against it — honest while the engine tracked delivery per good, dishonest the moment met/breach became per licence. **Per-row verdict:** `synResolve` reads the window's `perVenture` block and attaches each venture's own `{ commitment, delivered, status }` by ventureId; the row renders its own dot and its `delivered / commitment`, where `commitment` is the engine's `qᵢ` — the target for THIS window, so a mid-window joiner shows the **pro-rated** figure it is actually judged against. `windowStatus` survives for the thermometer's overview only. A licensed row with **no** `perVenture` entry keeps the neutral mark rather than borrowing the good's status (nothing was judged ≠ having failed). **The ranking:** committed rows are drawn in the good's stored `pursue` order, reconciled for display by `reconcilePursueIds`, which mirrors the engine's `reconcilePursue` rule for rule (drop a dead id, honour a duplicate once, append the omitted in establishment order); uncommitted producers stay below, unranked and unrankable. Two movers per row (`▲`/`▼`, **disabled at the ends rather than hidden** so the row never re-flows) POST the **whole new order** through the existing `sendProfile` path, exactly as the Gate-1 fork rank POSTs its whole permutation — `{"goods":{"titanium":{"pursue":["mine-beta","mine-alpha"]}}}` — and the order moved is the **reconciled** one on screen, so a first move on a never-ranked good stores establishment order with that one swap applied. A **clear ranking** button POSTs `pursue: null` (the engine's tri-state clear) and is offered only when a ranking is stored. **The poll-clobber guard:** a ~1 s re-render between mousedown and click detaches the button and the click lands on nothing — `trackDrag`'s failure in click-driven form. A `pollHold` **counter** (not a flag: the two holds overlap) holds the poll from mousedown until the move's own response has re-rendered (`sendProfile` now returns its promise); the mouseup release fires on **any** mouseup, so a press dragged off the control cannot freeze LIVE. **A layout regression caught and fixed in the same slice:** the rank + movers cost ~30px of a 152px row and truncated `mine-alpha` to `mine-b…` — so the `delivered / commitment` line moved UNDER the venture id and the rank moved inside the movers' 18px column; rows are tall (height ∝ commitment), so the free axis is the vertical one. Ids fit at 1440/1600/1920px. **Copy corrected:** the notes claiming delivery is per-good and that "the fee, breach and pursue controls are not shown at all, because there is nothing behind them" are gone; the panel now says each row is judged on its own, that the pile fills top-of-list first with each licence filled in full before the next is fed, and that met means **all** of what was promised. The **fee** is still not shown and the note says so (3b-iii) rather than printing a figure that never moves; the top-up block stays tagged mock and is now the only mock left in the panel. **Verified in headless Chromium** against a real server (two licensed titanium mines, targets 6 and 4, `Q` 10, the send pinned to 2/tick so the window ends 8 of 10): default order → `mine-alpha 6/6 ✔ met`, `mine-beta 2/4 ! breach`; one `▲` on mine-beta → `mine-beta 4/4 ✔`, `mine-alpha 4/6 !` — **the same flip `licence-distribution.test.js` asserts, now visible** — surviving a whole further window to the next boundary; `clear ranking` restores the first column; mid-window both rows read `accruing`; an unlicensed producer reads `no commitment` with a neutral mark and no movers; and with LIVE polling on, **3 of 3** reorder clicks at 1.1 s intervals landed. **437 tests, zero failures** — total unchanged (the four new assertions joined the existing served-page test). ⚠️ **Deferred, not invented:** drag-to-reorder (the movers are this panel's established idiom and one move is one POST; a drag would need `trackDrag`'s rect-capture treatment generalised from a track to a list), and showing which rank a licence would *need* to be met — a projection the engine does not emit and the browser is not allowed to compute.
  - [x] **Cycle length — `N` promoted 24 → 1,440, the number-flip slice (landed 27-08-26; ruling `docs/cycle-and-calendar.md` §1 + §7 Slice 1; `design.md` §5 "windowed accrual"; constants `docs/phase-1-tuning.md`):** the FIRST of the cycle/calendar ruling's two slices — **the number, and nothing else**. `FIRST_CUT_WINDOW_N` in `sim/windows.js` goes **24 → 1,440**, so an unconfigured (live) galaxy runs a **24-hour commitment cycle** instead of a 24-minute one. **No new `[FIRST-CUT]` number:** 1,440 is *derived* — a cycle is one full day (ruled 27-08-26) and a tick is one minute (ruled 24-08-26), so 24 h × 60 min/h = 1,440 (§18 #5 satisfied). The `[FIRST-CUT]` / UNRULED / "24 is a pure guess" comment block above the constant is retired and replaced with that derivation, the still-true "fallback only — scenarios/tests override via `state.windowN`" note, and a forward pointer to Slice 2. **The `FIRST_CUT_` prefix on the NAME is deliberately kept** — renaming touches six files and would bury a one-line number change under a rename diff; flagged in the comment as a follow-up tidy, not silently widened. **What it changes:** every commitment figure was a derivative of `5 × 24` and becomes one of `5 × 1,440`, so a **100 % titanium licence owes 7,200 units a cycle where it owed 120** — the playtest-surfaced figure the ruling exists to fix. The basic fee scales on the same axis (`FEE_RATE × baseline × N × price`), so the tuned **0.10 ratio is unmoved** and only the absolute grows, exactly as `phase-1-tuning.md`'s `FEE_RATE` note requires. **Nothing else moved:** met/breach, the sale and the fee all re-derive from `N` and were not restructured. **The tripwire** (new `sim/tests/cycle-length.test.js`, +7): a derived number is the kind that gets silently reverted by someone who reads `24` in an old comment, so it is pinned on all three surfaces it can be observed from, **every one with no `state.windowN` set** — the constant and its `24 × 60` derivation; the licence terms it sizes (the **real** `applyForLicence` intake path stamps **7,200**, and the fee's ratio stays at `FEE_RATE`); and the cadence the **live tick engine** runs (still window 1 at tick 24 — the retired cadence is gone; still window 1 at tick 1,440 — the boundary tick is the cycle's LAST, not the next cycle's first; rolls at 1,441). Every pre-existing suite injects its own short `windowN`, so this file is the **only** place the shipped default is under test. **444 tests (+7), zero failures** — exactly the tripwire, nothing else moved, which is itself the proof that nothing reads the fallback that shouldn't. **The committed and unlicensed golden hashes are byte-identical**, as §7 predicted. ⚠️ **One correction to §7's stated reasoning** (the ruling and its outcome are both right; the justification is imprecise): §7 and the `phase-1-tuning.md` entry say the flip is safe because "every test sets its own `state.windowN` — none pins the fallback". The *committed* golden does; the **UNLICENSED golden does not** — `commitment-scaffold.test.js` calls `sysState([...])` with no `windowN` and genuinely reads the fallback. It is byte-identical for a different reason: every mine in it has commitment 0, so nothing on that path ever consults the window. Surfaced rather than papered over, and left for the human to reword. **OUT of scope, deferred not invented:** the midnight anchor / `dayAnchorTick` / the `(tick − dayAnchorTick) % N` boundary and the calendar module (`dayOf`/`minuteOf`/`tickAt`/`format`) are **Slice 2**; `windowDays` / renegotiation stays **#64**, untouched.
  - [x] **The midnight anchor + the calendar layer (landed 27-08-26; ruling `docs/cycle-and-calendar.md` §2/§3/§4/§5/§6 + §7 Slice 2; `design.md` §5 "Global boundary"):** the SECOND and last slice of the cycle/calendar ruling — cycle boundaries now fall on **server midnight**, and a pure calendar derives days and times from the plain tick. **ENGINE + server; no client change** (the console rendering is a later slice). **`dayAnchorTick`** is a per-galaxy integer offset on state, added **omit-when-absent** exactly like `windowN`, defaulted `== null ? 0` by every reader: the boundary becomes `(tick − dayAnchorTick) % N == 0` and `winStartFor(tick, N, anchor)` becomes `tick − (((tick − 1 − anchor) % N + N) % N)` — the **safe-modulo** form, because an anchored galaxy's anchor is NEGATIVE and JS `%` keeps the dividend's sign. At the default 0 both reduce **exactly** to the pre-anchor expressions, which is the whole safety case. Threaded through `production.js` (the opts read, `winStartFor`, the boundary, and `previewProduction` so the preview cannot drift from the apply), `tick.js` (the sale weights against the same window the report was built on) and `invariants.js`. **The anchor's SIGN is a real decision, not a formatting detail:** `anchorForCreation` returns the **negative** representative (`−minuteOfDay`), not its positive congruent twin. Both give identical boundaries (the boundary reduces mod N) but they disagree under `dayOf`, which **divides**: the negative anchor puts the creation tick on **day 0**, the twin puts it on **day −1** and rolls to 0 nine hours later. Pinned by a test that asserts the twin's wrongness beside the choice. **`sim/calendar.js`** — pure, stateless, clock-free: `minuteOf` / `dayOf` / `tickAt` / `format` ("DDDD:TTTT", **display only**, never parsed back) on the `(tick − 1 − anchor)` phase §5 calls load-bearing — the **same** phase `winStartFor` uses, so the calendar's day-roll and the engine's met/breach boundary are the SAME EVENT by construction and cannot drift; asserted directly, and `winStartFor` is asserted to agree with `tickAt(dayOf(t), 0)` across lengths and anchors. Plus `anchorForCreation(minuteOfDay, N)` (pure) and `minuteOfDayFromDate(date)` (converts a Date the CALLER supplies). **The ONE wall-clock read** is in `server.js`'s `/admin/galaxy/new` handler, at the operator seam beside `STARFARE_TICK_MS` — never in `advance`/`tick`/`resolve`. `/reset` and the restore path deliberately do NOT anchor (restore loads the persisted value; §3 rules **no catch-up** — cycles stay exactly N ticks and real-midnight alignment is allowed to drift). The `[galaxy] CREATED` line now logs the anchor and the implied first-midnight tick. **Snapshot** gains a top-level `calendar` `{ day, minute, label, windowN, dayAnchorTick }` beside `tick` — **additive, DERIVED, schema still 7**, entering no determinism hash. **Rename:** `FIRST_CUT_WINDOW_N` → **`DEFAULT_WINDOW_N`** across its 6 sites (it is the ruled default and stopped being a first cut when the number was ruled); pure rename, done here because these files were already open rather than as a standalone rename-only diff. ⚠️ **One invariant had to be LOOSENED, and this is the thing to review first:** `windowStart ≥ 1` is now `windowStart ≥ 1 + dayAnchorTick`, and `windowStart` is exempted from the non-negativity sweep (`delivered` keeps it — that one really is a unit count). A galaxy created mid-day has a **stub first cycle** whose NOMINAL window opened *before the galaxy existed*, so its `windowStart` is legitimately ≤ 0. This is **not** a defect to clamp away: `windowFraction` measures presence against that nominal start, which is exactly how §4's partial first cycle gets pro-rated by the machinery that already handles a mid-window join — clamping to 1 would hand a stub-cycle licence a **full day's target inside a part-day window**, the un-meetable obligation the pro-rate exists to prevent. At anchor 0 the new bound is byte-identical to the old one, and both directions are tested (the legitimate stub passes; one tick earlier still trips). **A bug the tests caught in this slice's own code:** the operator log first computed the first-midnight tick as `−anchor` (minutes *elapsed* today) where it is `N + anchor` (minutes *remaining*) — 870 where the truth was 570; fixed and pinned across the range. **473 tests (+29), zero failures** — `sim/tests/calendar.test.js` (+12: §5's worked examples at `N = 1440` **and** a small `N` to show the formulas are length-relative, `tickAt` round-trips, day-roll ⇔ boundary, the anchor's sign, midnight needing no anchor), `sim/tests/day-anchor.test.js` (+15: the no-op proofs, the anchored first boundary at the next midnight with N-spacing after, the stub pro-rate, the loosened tripwire both ways, save/load round-trip, a restart re-deriving nothing, the snapshot clock, and a **clock-free scan** of `sim/` that strips comments first — with a teeth test proving the scan can go red, since this file's own prose mentions `Date.now()`), and `galaxy-lifecycle.test.js` (+2 live-server: creation sets a frozen anchor that survives a restart, and `/reset` does not re-anchor). **The committed and unlicensed golden hashes are byte-identical** — the load-bearing proof (invariant 9), since an unset anchor writes no field at all. ⚠️ **Deferred rather than invented:** rendering the calendar in the console (a later client slice — only the snapshot seam is emitted here); `windowDays` **renegotiation behaviour**, still **#64** — §6's `renegotiationDeadline` is a FORMULA in the doc and no deadline detection or term-reopening was built; manual **re-anchoring** after a long outage (§3 rules it a future operator action, not automatic); and folding the negative-`windowStart` consequence into `cycle-and-calendar.md` §4 itself — surfaced here rather than edited into the ruling.
  - [x] **The operator CLI — `tools/admin.js`, so ops is a command not a paste (landed 27-08-26; runbook `docs/deployment-runbook.md` §12):** **TOOLS ONLY — no `sim/` change, no new endpoint, no new game number, no npm dependency.** Operating and verifying the live container meant pasting a browser-console blob or a hand-rolled `curl`/`node` snippet: fragile (it lands in the wrong shell) and unversioned (it lived in a chat message). `tools/admin.js` is the committed replacement — a **dependency-free** Node CLI (plain Node + global `fetch`, so `docker exec starfare node tools/admin.js …` works in the stock `node:22-alpine` image) that is a **thin HTTP client** over the routes `sim/server.js` already serves. Seven commands, each wrapping existing endpoints verbatim: **`health`** (`/health`), **`snapshot [--json] [--pick a.b.c]`** (`/snapshot`), **`starters`** (`/starters`), **`new-galaxy [--seed N]`** (`/admin/galaxy/new`, reading `calendar.dayAnchorTick` back), **`tick [n]`** (`/tick`), **`seat-demo [--window N]`** (the generalised, committed form of the `seat_licence_demo.sh` paste — two titanium mines on one starter, both licensed at 100 %; `--window N` takes the `/reset` + `setWindowN` tick-0 path, the ONLY place a short window is reachable, since `setWindowN` is a setup-only knob), and **`verify-cycle [--seed N]`** — **the standard post-redeploy check**: build a galaxy, license one mine at 100 %, read it back and assert `syndicateCommitment === 7200`, `calendar.windowN === 1440`, `calendar.dayAnchorTick` present. Exit `0` on success, `1` on failure (a refused action included), so it is scriptable/CI-able. **The tool AUTHORS no number:** 7,200 and 1,440 are quoted engine truth (`docs/cycle-and-calendar.md` §1, `sim/tests/cycle-length.test.js`) written down as EXPECTATIONS so a container running older code fails loudly; the setup values it posts (2,000 credits, rate 5, `windowDays` 7) are quoted from `client/game.html`, `docs/phase-1-tuning.md` and `sim/licence.js`'s `WINDOW_DAYS_MIN` respectively. **The tripwire (`design.md` §18, "mechanical enforcement beats an AI reviewer"):** the HTTP glue is thin, so the logic that can be WRONG was extracted pure and unit-tested in `tools/admin.test.js` (16 tests, run like `generate_seed.test.js`: `node --test tools/admin.test.js`) — `parseArgs` (table-driven, incl. unknown flag / missing value / non-integer), `pick`, `pickResourceNode`/`findResourceNodes` (fixture layouts with and without titanium), and above all **`judgeVerify`**, whose `pass` IS `verify-cycle`'s exit code: the passing set plus each failing variant (the retired 120, the retired `windowN` 24, a null anchor) and the anchor-0 case that must still PASS. The HTTP orchestration stays a deliberately untested-by-unit shell, exercised by running the command.
  - [x] **Licence distribution — per-venture met/breach via the pursue-order fill (landed 27-08-26; design `design.md` §5 "Breach is judged PER VENTURE, not per good" + "the pursue fill draws on the whole window's delivered pile — no fencing by arrival time"; contract `docs/production-console-model.md`; build note `docs/licence-distribution.md`):** the engine tracked ONE aggregate window per `(guild, system, good)` and judged met/breach on it — but a licence is a per-VENTURE contract and the fee is per venture, so an aggregate breach said *someone* fell short without saying **who**, which is exactly what 3b-iii has to charge. This slice builds §5's ruled answer. **No new `[FIRST-CUT]` number, and nothing is charged.** **The `pursue` control (STORED):** `productionProfile[systemId].goods[good].pursue`, an ordered array of ventureIds, threaded exactly like `order` — `getGoodPolicy` (sparse, like `syndicate`: present only when set, because ABSENT means establishment order), `applyField`/`setEntry` (tri-state, `null` clears), `cloneProfile`, `setProductionProfile` validation (an array of non-empty strings, or `null`), plus `storedPursue`/`storedPursueGoods` accessors so the nested shape stays in one file. Deliberately **not** checked against current ventures, mirroring `throttles`: the profile is standing intent, reconciled at READ time. **`Q = Σ qᵢ` (the F-C rounding fix):** the per-venture target `qᵢ = round(committedContribution)` is rounded FIRST and the rounded targets summed — it was `round(Σ contribution)`, and those are different integers on a fractional window. That matters now because the fill hands each venture its own `qᵢ` while the Syndicate fork self-terminates at `Q`: a `Q` below `Σ qᵢ` leaves the last licence unable to reach a target the pile was never *allowed* to hold (a **phantom breach**), a `Q` above it admits a unit no verdict accounts for. `qᵢ` is the ONE per-venture target, read by both the sum and the fill — the same single-source discipline `committedContribution` exists for; the paced `requiredRate` re-derives for free. **Byte-identical where it must be:** a full-window venture has `fraction = 1`, so `Σ round = round(Σ)` exactly and `GOLDEN_COMMITTED_WITH_PRICES` is unmoved (`e0255c73…`), unlicensed goldens untouched. **The fill (DERIVED, no new stored state):** reconcile the stored ranking at read time — DROP an id no longer committing the good, APPEND every committing venture the list omits in **establishment order** (the same stable tie-break Gate 3's FCFS default uses, not a new one) — then walk it top-down, each venture taking `min(qᵢ, remaining)`. **Binary:** `met` iff it got its ENTIRE target, one unit short is `breach`, no partial credit; `accruing` mid-window (a projection, since the ranking is editable until the boundary). **The N-4 ruling is asserted, not merely allowed:** the fill is over the window's WHOLE pile with no arrival-time fencing, so a mid-window joiner ranked first can be met on units delivered before its licence existed and the mine that actually sent them breaches — a test pins that behaviour with the unranked counterfactual beside it, so it cannot be quietly "fixed". **The good-level `status` is now a ROLLUP** of the per-venture verdicts (`met` only if all met) — the aggregate derived from the individuals, never the reverse; with `Q = Σ qᵢ` and a greedy fill it is the same verdict `delivered >= Q` gave, which is why the change is byte-identical, but it is now *composed* the way the design says it is. **A fourth review trigger, `stale_pursue`** — a ranking naming a venture no longer producing that good in the system (the same class as a stale throttle), checked against PRODUCERS rather than licence-holders so ranking an unlicensed producer ahead of time stays legitimate; the issue sorter's key widened to `(kind, good, ventureId)`, unchanged for the three older kinds. **Snapshot** gains `…goods[good].window.perVenture = { ventureId: { commitment, delivered, status } }`; **additive, schema still 7**, and keyed exactly as `client/console.html`'s `synResolve`/`licRow` want, so the console rewire is pure wiring in a follow-up. **The sale is untouched** — it is the other clock (per tick, contribution-weighted) and making it follow a ranking that does not exist until the boundary would price money-for-goods by a fact not yet known. **Proven** (`sim/tests/licence-distribution.test.js`, +19, plus 2 `pursue` accessor guards in `profile.test.js`): the priority fill, the same run with the ranking reversed (same pile, same commitments, **flipped verdicts**), one unit short reading `breach`, `Σ perVenture.delivered = window.delivered`, the rounding case where `Σ round = 4` and `round(Σ) = 3` with full delivery meeting **everyone**, the N-4 fill plus its counterfactual, the rollup in both directions, read-time reconciliation (a dead id ignored AND flagged; an omitted venture appended last), the control's round-trip and `null` clear, six malformed rankings refused byte-identically, the snapshot shape, the charges-nothing proof (two whole windows, the guild's only credit movement is 3a's sale, per tick), and determinism. **437 tests (+21), zero failures.** ⚠️ **Out of scope, unchanged:** the fee charge (3b-iii, including the still-open ruling on whether a partial first window's fee is pro-rated, free or full), the console rewire, and the window-end reserve-backfill. ⚠️ **Deferred rather than invented — new open question #65:** `invariants.js` has long noted `delivered ≤ Q` as "left to the distribution slice"; the recompute is now trivial but the *rule* is not — a window can be legitimately over-delivered if a commitment shrinks mid-window, and a tripwire is a halt, so whether over-delivery is impossible / a gift / carried forward is a ruling, recorded at §19 #65 and left unasserted (unreachable today, since nothing can shrink a commitment mid-window).
  - [x] **Licence-layer hardening — the sale's mis-payment fixed, the tripwire gaps closed (landed 27-08-26; design `design.md` §5 "The Venture Licence Fee" + "LICENCE FEE MECHANICS — RULED"; build note `docs/licence-hardening.md`):** not a new mechanic and **no new `[FIRST-CUT]` number** — one live bug, the duplication that caused it, and the tripwires that would have caught it. **The bug:** `ownerFraction` (`sim/licence.js`) weighted the commitment sale's proceeds split by each venture's **raw** `syndicateCommitment`, while the good's target `Q` (`sim/production.js`) was built from `commitment × windowFraction`. The two agreed only while the fraction was pinned at 1; **Slice 3b-ii unpinned it**, so a guild running two mines on one good in one system — different window fractions AND different equity — was paid the **wrong split**, and paid it **silently**: invariant 2 still balanced to the credit (guild gain and ledger debit are one number by construction), only the line between the owner's keep and the ledger's `o` share moved. A mis-attribution, not a conservation break — which is why 406 tests never saw it. **The fix is structural, not a second multiplication:** the root cause is two computations of one quantity, so the duplication was removed — `committedContribution(venture, windowStart, N)` now lives in `sim/licence.js` and BOTH consumers call it (the `Q` sum, behaviour-identical; and `ownerFraction`, which is the fix), with `applyProduction` threading the same `windowStart`/`windowN` the resolver used that tick. The non-positive skip moved INSIDE it, so the two also agree on **sign**. **Byte-identical where it must be:** `GOLDEN_COMMITTED_WITH_PRICES` is single-venture/full-window, so its hash is unmoved (`e0255c73…`), and the unlicensed golden is untouched. **Documented so it is not "fixed" later:** the sale is a PER-TICK proportional attribution (money for goods) and the coming pursue-order fill is a PER-WINDOW boundary attribution (which contracts you honoured) — two clocks, by design; the proportional weight is the sale's final answer, recorded in §5 and in the code. **Four tripwires added:** `venture.syndicateCommitment` joins the integer/non-negativity sweep (a fractional value hid inside `Q`'s own `Math.round`; a **negative** one SUBTRACTED from its good's aggregate and silently shrank a **sibling** mine's target); a licensed, committed venture must carry `committedFromTick` (absent, `windowFraction` returns 1 and the mine silently owes a whole window again); the stamp must be `licence.signedTick + 1` (the existing guard only checks the fraction is in `(0,1]`, which ANY integer ≥ 1 satisfies, so an off-by-one or a far-future stamp sailed through); and `windowStart ≥ 1` (0 is not a small window-start, it is a window that never opened). **`delivered ≤ Q` deliberately NOT added** — `Q` is not stored (invariant 5), so it needs a per-venture recompute inside the sweep; left to the distribution slice and noted in `invariants.js`. **`createVenture` now carries `committedFromTick`** — it was the one licence field not destructured, so a scenario handing one in lost it and the venture reverted to a full window; omitted-when-absent, so the no-op hash holds. **Comment/doc sweep in the same commit:** the fee charge renumbered 3b-ii → **3b-iii** everywhere it had been missed (`licence.js`, `actions.js`, `invariants.js`, `state.js`, `licence-fee.test.js`, `docs/licence-fee.md`), the retired `fraction == 1` guard no longer credited as live (`actions.js` ×2, `design.md` §15.4, `docs/licence-fee.md`), "these dev setters retire" reworded to *retired as the commitment source, kept for tests/scenarios/the bot*, and the dead pre-price-engine Titanium market in `docs/phase-1-tuning.md` (base $3, sensitivity 0.6 — a value the price engine explicitly **rejected**) struck and pointed at the Resource-prices table + `sim/prices.js`. **CHARGES NOTHING** and adds no mechanic. **Proven** (`sim/tests/multi-venture-commitment.test.js`, +9, plus the reworked `ownerFraction` unit assertion and a `createVenture` round-trip): the headline fixture — two ventures, one good, one system, differing fractions and equity, run through a window boundary — **fails on the pre-fix engine (paid 50 where the contribution-weighted split is 52) and passes after**, the counterfactual asserted beside it so it cannot pass for another reason; plus `Q` = Σ contribution, the boundary verdict and roll, invariant-2 cleanliness per tick, and every tripwire above tripped on the exact field. **416 tests (+10), zero failures.** ⚠️ **Still open, unchanged and not invented here:** whether a partial first window's **FEE** is pro-rated, free, or full (3b-iii), and per-venture breach attribution (the distribution slice).
  - [x] **Licence Slice 3b-ii — the mid-window join pro-rate (landed 27-08-26; design `design.md` §5 "Window boundaries and edge cases (Slice B)", the Option-A join ruling whose build was DEFERRED; build note `docs/mid-window-pro-rate.md`):** not a new decision — §5's deferred build, and the prerequisite 3b-i flagged. Slice B-i built the pro-rate's arithmetic and pinned it unreachable (`windowFraction` returned 1 for everything) behind a tripwire asserting exactly that; 3b-i then introduced the thing that reaches it — a licence granted at an arbitrary tick — and with the pin still in, a mine licensed part-way through a window owed a **whole window's `Q`** with only part of a window to deliver it: an unavoidable breach on a contract just signed. **`applyForLicence` now stamps `committedFromTick`**, and the whole slice turns on it being **`signedTick + 1`, not `signedTick`**: intake runs BEFORE the tick, so a venture licensed while `state.tick` is T first produces on the tick yielding T+1, and `windowFraction` counts producing ticks from the stamp to the boundary inclusive — stamping `signedTick` would credit it with a tick it could never produce in and make it owe a unit it could not deliver. **`Q` is rounded** where it is finalised (goods are integers, §15.2), so the paced `requiredRate` and the met/breach judgement read the SAME integer target and pacing can never aim at a number the boundary refuses — `Math.round`, matching the send carry's convention; **not a tunable, and this slice introduces no `[FIRST-CUT]` number at all**. **Byte-identical where it must be:** with a full-window fraction, `round` of an integer is the identity, so every pre-existing path is unchanged — pinned by the **untouched** committed golden hashes (`commitment-scaffold`, `persist`), not merely argued. Only `applyForLicence` stamps the tick; the `setSyndicateCommitment` dev scaffold stays deliberately full-window and its test asserting so stays green. **§5's `fraction == 1` deferral tripwire has done its job and is retired**, replaced by a correctness tripwire in `invariants.js` (a present `committedFromTick` is an integer ≥ 1; the fraction it yields is in `(0, 1]`) — honestly a guard on the MATH rather than on data, since for an integer stamp the range holds by construction, so it goes red if `windowFraction`'s formula or the stamp's timing ever changes in a way that would hand a venture a target it cannot meet. Snapshot gains `ventures[].committedFromTick`; **additive, schema still 7**. **Charges nothing** — credits move only through 3a's sale, asserted per tick. **Proven** (`sim/tests/mid-window-prorate.test.js`, +15, plus the reworked `window-accrual` `(a)` slot): the headline — a mine licensed mid-window **MEETS** its pro-rated `Q` where it used to breach — with the counterfactual beside it (strip the stamp and the identical run breaches, so the headline cannot pass for another reason); a sliver-sized last-tick licence still owes and meets; `Q` integral on a fractional window with pacing reading the same target; later windows revert to the full `Q`; a licence signed exactly as a window opens is never pro-rated; a broken join tick trips; the fraction range swept across `N` × join-tick; determinism; and the unlicensed no-op proof. **406 tests (+16), zero failures.** By-hand run reproduced the original failure fixed: at `N = 4` the mid-window mine owes 10 not 20, and both it and a full-window mine resolve **met** at the boundary. ⚠️ **Open for 3b-iii (the fee charge, renumbered by this slice):** whether a partial first window's **FEE** is pro-rated, free, or full — the pro-rate settles what such a licence owes in *goods*, not what it pays in *credits*, and a ruling is needed before anything is charged.
  - [x] **The served client shell goes `Cache-Control: no-cache` (landed 28-08-26; runbook `docs/deployment-runbook.md` §9 + §10):** **plumbing — one HTTP response header, no client, engine or game-logic change, and no new number of any kind.** **The problem:** the server sent its HTML with no cache directive at all, so a browser applied HEURISTIC caching and kept serving the old shell after a redeploy. Because the pages carry their JS and CSS **inline**, a stale `game.html`/`console.html` is a stale **app** — which is why every deploy ended in a hard-refresh or an incognito window. Confirmed as the browser and not the CDN: a fresh browser cache loaded the new code, and the container's own bytes were already current. **The fix:** every response this server sends now carries `Cache-Control: no-cache` — all six `writeHead` sites, via one named `NO_CACHE` constant that carries the reasoning in one place. `no-cache` does **not** mean "don't store"; it means "revalidate before using", and since these responses carry no validator (no ETag, no `Last-Modified`) that revalidation is a plain refetch of the current bytes. Cloudflare honours it too, so the tunnel path is covered, not only the browser. **Scope of the header, and why it is uniform:** the three HTML shells (`/`, `/console`, `/inspect`) · the static-asset branch — those files are **not content-hashed**, so a long `max-age` would be exactly the staleness trap being removed, and at this scale revalidating a handful of images costs nothing · every JSON API, which was a one-line addition because they all funnel through `sendJson`, plus `/galaxy`, the one endpoint that writes its own header (and therefore the one call site a future refactor would miss). "Nothing this server serves can go stale on a deploy" is a simpler rule than a per-file judgement call. **Tripwire:** `sim/tests/server.test.js` asserts the header on all three shells, on a real asset, and on `/health`, `/snapshot`, `/goods`, `/starters` and `/galaxy` — so it cannot silently go missing in a refactor. **No golden could move** (no state, no snapshot, no tick is touched — a response header only) and none did; the committed and unlicensed hashes are untouched. **541 tests (+1), zero failures.** ⚠️ **Deferred rather than invented:** conditional revalidation (ETag / `Last-Modified` → 304), which would make the revalidation cheap rather than a refetch — unnecessary at this size · content-hashed / versioned asset filenames, the "proper" long-cache scheme, which `no-cache` is the correct interim for until such a scheme exists · **`HEAD` requests**, which this rig answers with a 404 (every handler tests `method === 'GET'`) — noticed while proving the header on the wire, pre-existing, and left alone; the runbook now says to dump headers off a GET rather than use `curl -I`.
  - [x] **The console's system inventory panel + the Tier-3 catalog seam (landed 28-08-26; design `design.md` §15.2 "Manufacturing *tiers*" — the Tier-3 placeholder note; contract `docs/production-console-model.md` §"The right hero's resting state"):** **almost entirely a client slice**, with one small, single-sourced engine seam. **The problem:** the console's right (industrial) hero showed a dead "select a venture" placeholder whenever nothing was selected — a third of the screen saying nothing. **The panel:** that resting branch now renders this system's inventory — every good the engine names, grouped `TIER 1` / `TIER 2` / `TIER 3`, each a labelled bar, sorted descending by quantity (ties alphabetical by id). Quantities are a plain per-system read (`onHand` → `guilds[g].stockpilesBySystem[sysId][good]`, ruling B1's pool, **not** the guild-wide total), so a fully-committed good that ships out each tick honestly reads near 0 — holdings, not flow (the sparklines already show flow). **Goods at 0 are listed too**, which is what makes the panel a vocabulary rather than a highlight reel. The one piece of arithmetic is the bar fill — `qty ÷ the largest qty in that same tier`, per-tier normalised so one big raw good can't flatten the refined tier, divide-by-zero guarded — **display math on displayed numbers, exactly like the existing thermometer**; no game number is computed in the browser, and the number is overlaid on the track so an almost-empty bar reads as clearly as a full one. **Sort-and-hold:** numbers and fills update live every poll, but the row ORDER is held so rows don't dance as quantities cross tick-to-tick; it re-sorts only when the panel opens or the system changes (the cached order is keyed by `sysId`). **The chip toggle:** venture chips become single-select **with toggle-off** — clicking the already-selected chip clears `selVenture` and the hero falls back to the inventory; `renderStage`'s open-on-the-lead-venture had to learn to respect that clear, or the next poll would have undone it. **The engine seam — `TIER3_GOODS`:** the console could only group goods into tiers it was TOLD about, and the engine exposed only raw/processed, so a Tier-3 group was unnameable. Three placeholder ids (`small`/`medium`/`heavy_reactor_engine`) are now declared in `sim/resources.js` and served additively as `/goods`'s `tier3` — **in the engine precisely so the browser never invents game vocabulary**, which a client-side list would have been (a second definition, free to drift). They are **display-only and fenced off the economy**: not in `STOCKPILE_GOODS`/`RAW_RESOURCES`/`PROCESSED_GOODS`, no recipe, no baseline, no price, never in `stockpilesBySystem` — a tripwire in `tests/resources.test.js` asserts every one of those, so they cannot leak into the tick. **No new `[FIRST-CUT]` number:** the placeholders carry no tuning, and the 2→3 manufacture recipes stay deferred to the human's Tier-3 design. **The goldens are byte-identical** — `TIER3_GOODS` is a new frozen constant nothing stateful reads, so no pinned hash moved (all of `commitment-scaffold`, `licence-fee*`, `mid-window-prorate`, `persist` still green unchanged). **Deuterium/fuel/B1 untouched:** raw `deuterium` is an ordinary Tier-1 row like any other ore, and refined fuel is guild-wide, not a stockpile good, so it is absent from this panel by construction, not by a special case. **540 tests (+3), zero failures.** ⚠️ **Deferred rather than invented:** the Tier-3 recipes, baselines and prices · any split of the 11 processed goods into Tier 2 vs Tier 3 (all stay Tier 2 — one refine-step from raw) · the EMBEDDED console's hero (in `?embed=1` the host client owns the right zone and receives only a `postVenture` message; a toggle-off posts nothing, so the host keeps showing the last venture — wiring a "cleared" message is a game-client slice, not this one — **CLOSED the same day** by *The embedded console's inventory reaches the player* above: the inventory now rides that bridge as a second `kind`) · the right zone's static `Venture` heading, which now sits above an inventory when nothing is selected — renaming it is a design call, not one to make silently.
  - [x] **In-game inventory panel — three cosmetic fixes (landed 28-08-26; contract `docs/production-console-model.md` §"The game's row shape is its own"):** **pixels only** — `client/game.html`'s `paintInventoryHero()` and its `.ih-inv-*` CSS, nothing else. Three things the operator flagged on the panel that landed hours earlier. **(1) The name moves INSIDE the bar.** The row was a name column beside a bar with only the quantity overlaid; the row is now one integrated thermometer — the fill behind, the name riding on its left, the quantity on its right. They are **flex siblings over the fill**, not two absolute boxes, so the number takes exactly the width it needs and the name truncates against it: no reserved gutter to guess at, and no overlap when a quantity gains a digit. **(2) Roughly doubled.** Row text 10px → 19px, bar 14px → 28px, the `Tier N` header 9px → 17px, group and row margins nudged to match. **(3) The raw system id is gone from the header** — it was duplicate information (the shell names the system already) and the eyebrow "System inventory" is header enough; the title element is hidden so it costs no space. `system` **stays on the bridge message** — the game just stops displaying it, so a future friendly system name has something to read. **One fix the sizes forced:** the detail screen's `.app` is `min-height:calc(100vh - 104px)`, so it GROWS to its tallest child rather than clipping — at 28px rows the list pushed the whole shell taller and `.ih-inv`'s own `overflow-y:auto` never engaged, stranding Tier 3 below the window with nothing to scroll. Pinning the hero to that same `calc(100vh - 104px)` (the third use of a value already in the file, not a new number) makes `.ih-body`'s `height:100%` definite again and hands the scroll back. Verified: hero 846px, list 733px in a 1,137px scroll — all three tiers reachable. **The STANDALONE console's panel (`.inv-*` in `console.html`) is untouched** — it keeps its tighter column layout; matching it up is a separate call. **No data, bridge, engine or schema change**, no golden moved, no new `[FIRST-CUT]` number. **542 tests, zero failures** — the existing bridge tripwire extended (name-inside-bar append order, the flex sizing that prevents overlap, the doubled px, the bounded height, and that the title is no longer fed `d.system` while the console still sends it). Verified in headless Chromium through the game client, before/after. ⚠️ **Deferred rather than invented:** a friendly system NAME for the header (nothing serves one to the client today) · widening the 326px right zone so long names like `Medium Reactor Engine` stop ellipsizing at this type size · the same name-in-bar treatment for the standalone console.
  - [x] **The embedded console's inventory reaches the player — it rides the venture bridge into the game's right zone (landed 28-08-26; contract `docs/production-console-model.md` §"…and embedded, it rides the venture bridge", `docs/client-wiring.md` §Slice 4 revision):** **client-only, two files.** **The problem:** the inventory panel that landed earlier today renders in the standalone `/console`, but a player never opens that page. The game embeds the console with `?embed=1`, and in embed mode the console builds no side zones — the game shell draws them — so the panel, which lives in the right zone, simply vanished for the player. **The fix:** it travels the **same `postMessage` bridge the venture nameplate has ridden since Slice 4** — nothing new invented, a second message on the rail that was already there. Two messages, one per hero mode: `{kind:'venture', name, role, facility}` when a chip is selected, `{kind:'inventory', system, rows:[{tier,good,label,qty,frac}…]}` when none is. **The rows are the panel's own data, not a second opinion:** the sort/normalise logic was **extracted, not forked** — `inventoryRows()` is the one function that groups by tier, reads `onHand` (stockpilesBySystem, ruling B1) and works out the per-tier `frac`, and it now has exactly two consumers, `inventoryPanel()` (standalone, unchanged on screen) and `postInventory()` (embed). The send fires **on every poll while the console is resting**, so the quantities in the game are as live as the standalone ones, in the **same held order** — sort-and-hold is upstream of both, so the embedded rows cannot dance where the standalone ones don't. Alongside it, `heroSelection()` extracts the one question both frames ask (is a venture selected AND really here?), so "which mode" is decided once. **The game side renders and derives nothing** (§5): it groups the sent rows by `tier` and draws `frac` as a bar in its own typography (`ih-eyebrow`/mono labels), and in that mode fades the venture art and hides "Open venture management" — neither means anything over an inventory — flipping straight back to the nameplate when a chip is picked again. The eyebrow follows the mode (`Venture` / `System inventory`); the STANDALONE console's static `Venture` heading is deliberately **left alone** — that rename is still the human's call on the checklist. **This closes the "a toggle-off posts nothing" gap** the inventory panel deferred, where the host kept showing the last venture forever. **No engine, endpoint or schema change**, no new `[FIRST-CUT]` number, goldens byte-identical. **542 tests (+1), zero failures** — a served-page tripwire pinning both ends of the wire (the console's `kind:'inventory'` sender and single-sourced rows; the game's `kind=="inventory"` handler, its frac-only arithmetic, and that it never reaches for `stockpilesBySystem` itself). Verified in headless Chromium through the GAME client: venture selected → nameplate; toggle off → the tiered inventory (Tier 1/2/3, 31 rows, Tier-3 placeholders at 0, art and manage button gone); 8 ticks → Titanium 48→112 and Copper 6→14 live with the row order unmoved; re-select → back to the nameplate. ⚠️ **Deferred rather than invented:** the standalone `Venture` heading rename · a place NAME for the embedded nameplate (it still shows the raw ventureId, as it has since Slice 4 — the bridge sends `name:id`) · any inventory affordance in the game beyond reading it.
  - [x] **Player copy catches up with the fee charge (landed 28-08-26; the client half of Slice 3b-iii, `docs/licence-fee.md` §"The charge"):** the charge made two player-facing lines false, so the same branch fixes them — **CLIENT-ONLY**, two files plus served-page tests in `sim/tests/server.test.js`; **no engine, action, resolver or snapshot change, schema still 7**, no new `[FIRST-CUT]` number. `client/console.html`'s Syndicate footer said *"The fee is not charged yet (licence Slice 3b-iii), so none is shown"* and `client/game.html`'s licence receipt said the rate was *"LOCKED and NOT YET CHARGED — the fee debit is not built"*; both now say what §5 actually does — charged at each **cycle boundary**, the discounted fee if the licence met its commitment that cycle, the **full basic fee** if it breached. Four supporting comments in `console.html` that asserted the charge was unbuilt are corrected too: what the ROW shows is a display decision, not a gap. **The footer also now reports the charge**, read straight off the snapshot's per-guild `licenceFee` record (`synFeeFoot`): the tick it fired and the lump, **labelled guild-wide** — because `charged` sums every licence in every system on every good (tick.js Layer 2) while the panel is one good in one system, so an unlabelled figure beside a titanium roster would read as titanium's. No fee figure went on the ROW, deliberately: the rows show THIS window's live accrual and the charge is LAST boundary's, and one figure on two clocks is the confusion #66 already documents. **The still-true caveats were left alone** rather than swept: an unlicensed venture owes no fee, the pre-sign figure is an estimate against the market at signing, renegotiation (#64) and the asset economy (§4) are unbuilt, and the fee-preview endpoint / atomic `establishAndLicence` remain noted follow-ups — each is now pinned PRESENT by a served-page assertion, so a future blanket "remove every no-fee mention" cannot delete them. **Verified in headless Chromium** against a real booted server (a licensed titanium mine, `windowN` 4): at tick 4 the engine charged `{ charged: 15, mine-a: met, owed 15 of a 20 basic }` and the panel rendered *"…Last charged at tick 4: 15 ¢ across all your licences, guild-wide."*; stopped at tick 2, the same panel read *"Nothing charged yet — no cycle has closed on a licence of yours."* No page errors from the new code. **Served-page tripwires:** the new wording is pinned as bytes and the five stale strings pinned ABSENT so they cannot creep back in a merge; the existing licence-panel test's `NOT YET CHARGED` assertion **flipped** rather than being dropped. **537 tests (+2), zero failures**; every golden byte-identical (no `sim/` engine file was touched).
  - [x] **Licence Slice 3b-iii — the licence fee is CHARGED at the window boundary (landed 28-08-26; design `design.md` §5 "LICENCE FEE MECHANICS — RULED" + the invariant-3 carve-out in §15.5; build note `docs/licence-fee.md` §"The charge"):** the last bullet of the fee sequence, and the one that finally moves credits. §5 had already ruled every rule and every constant, so this slice **authors no new mechanic and no new `[FIRST-CUT]` number** — it is the socket, not a new circuit: `basicFee`/`discountedFee` were locked at signing (3b-i), the per-venture met/breach verdict is the pursue-order fill's (licence distribution), the `windowFraction` is 3b-ii's, and the boundary is the day anchor's. **The charge:** at each ANCHORED window boundary each licensed venture owes `round((met ? discountedFee : basicFee) × windowFraction)`, and a guild's oweds across **all** its systems and goods sum into ONE lump debited from `guild.credits` and credited to `state.syndicate.ledger`. **Two layers, per §5.** *Layer 1* — verdict and owed amount are per `(system, good)`, per VENTURE (the Syndicate pile a venture is judged against belongs to a system and cannot be shared): `feeOwed(licence, status, fraction)` in `sim/licence.js`, one small pure function, unit-testable off a server. *Layer 2* — the CHARGE is guild-level and system/good-agnostic, because a guild has one credit pool and every window closes on the same global boundary. **The total is DERIVED from the individuals** — rounded per venture, then summed; there is no `(system, good)` fee *pot* to split, and a pooled total is never rounded. **Mechanism (flagged, not assumed):** `applyProduction` (`sim/tick.js`) computes its system's oweds from the `perVenture` verdicts it already holds and **returns** the accrual; `stepProduction` sums a guild's systems and applies the single debit. The tempting post-production pass was rejected — `status` is derived telemetry reaching no stored byte, so a second pass would rebuild the pursue fill from stored `delivered` against a window on the cusp of rolling. **One departure, load-bearing:** the loop is over the guild's stored **licences**, looking each verdict up, NOT over `perVenture` rows — because §5's **0% commitment floor** means a venture that committed nothing has no `Q`, no window block and no row at all, yet is licensed, `met` (0 ≥ 0) and PAYS (100% of basic at no equity). Looping over rows would hand every 0% licence a silent free ride; an absent row reads `met`, the same verdict the fill would give it. **That reading is GUARDED:** the resolver routes every good whose aggregate `Q` is positive and emits a target row for every contributing venture, so a COMMITTED licence always has a real verdict — an absent row for one that owed units **halts**, naming the tick and the owed quantity, rather than charging the discount on a contract that was never judged (the quietest possible undercharge, invisible in every balance). The predicate is `committedContribution`, the same function the resolver builds its targets from, so the guard cannot disagree with what it guards; the tripwire is asserted red with the guard removed. **Edge cases, each tested:** a commitment with no stored `licence` (the `setSyndicateCommitment` dev scaffold) is **skipped** — no locked fee, nothing to charge, and this is what keeps the scaffold goldens byte-identical · **breach voids the discount and is binary** — one unit short of twenty pays the full basic fee · **`accruing` never charges** (`feeOwed` **throws** on a non-verdict status rather than silently returning 0) · a **mid-window** licence pays `round(fee × fraction)` at its first boundary and the whole fee after, against the SAME `curWindowStart`/`N` the verdict used · a **two-system** guild is debited one lump equal to the sum of its ventures' oweds · the debit **may drive credits negative** (intended) · the per-tick **sale (3a) is untouched** and runs on the boundary tick too — two independent internal transfers, neither folded into the other. **The record:** `guild.lastLicenceFee = { tick, charged, ventures: { ventureId: { status, owed, basicFee, discountedFee } } }`, written LAZILY — only at a boundary where the guild really holds a licensed venture, replaced never accumulated — because the verdict is **momentary**: the instant the boundary tick ends the window rolls, the pile resets and the met/breach that priced the fee is gone from everywhere else (invariant 5, the same reason `recordSale` exists). Surfaced additively as the snapshot's per-guild **`licenceFee`** (`{ tick, thisTick, charged, ventures }`), **schema stays 7**. **The invariant change — a live doc contradiction reconciled, same commit:** §5 had ruled guild credits exempt from non-negativity and neither invariant 3's text nor the code had caught up. `sim/invariants.js` now checks `guild.credits` with `{ nonNegative: false }` (exactly as `syndicate.ledger` already was) and `design.md` §15.5 carves it out beside the ledger **with the reason**: the fee is charged in full whether or not the guild can pay, because refusing it or flooring it at zero would let a guild escape the cost of a contract it broke by being poor. The **integer** check is untouched — the carve-out freed the sign, not the type, and a counterfactual test pins that a fractional balance still trips. Invariant 2 stays exact through the debt (guild −X, ledger +X: one integer, both legs). **A shared boundary formula:** the anchored `(tick − dayAnchorTick) % N == 0` moved out of `resolveProduction`'s inline copy into `isWindowBoundary` (`sim/windows.js`), so the resolver that resolves the verdicts and the tick that charges off them cannot drift onto different ticks. Same arithmetic, same bytes. **Proof — the "charges nothing" assertions FLIP, and every golden holds.** Three slices each shipped a *"the only credit movement is the sale"* proof; all were **updated, not deleted** (`licence-fee.test.js` now asserts sale-minus-fee on the boundary and sale-alone everywhere else, with the fee > 0 there and 0 elsewhere; `mid-window-prorate.test.js` the same walk plus a new pro-rated-first-window charge test; `multi-venture-commitment.test.js`'s F-A adds the fee back so the sale's own attribution stays pinned). `licence-distribution.test.js` and `commitment-scaffold.test.js` still assert *no charge* and are renamed to say what they now prove — the **licence-less skip**. **The committed goldens are byte-identical, and that is a result:** neither pinned run holds a `venture.licence` (the committed one is dev-scaffold shape, the persist one unlicensed), so nothing in them is charged — `682e42e0…`, `ee6856cc…`, `fa63aaf4…`, `e0255c73…`, `e145b142…`, `564010…`/`53c4f4…`/`170bed…` all **unchanged**. So a **strip-and-prove** was written for a run that IS licensed, which a re-pinned number could not state: the same fixture over three windows, once with the licence stored (`1b62d5ef…`, credits 614, ledger −614) and once with the `licence` object deleted after signing — the pre-3b-iii behaviour for that run (`6e8ce686…`, credits 659, ledger −659); `expectedLump = discountedFee 15 × 3 boundaries = 45`, `614 = 659 − 45`, and the charged state with `lastLicenceFee` + `licence` stripped and the 45 put back hashes **`6e8ce686…` — byte-identical**. If the slice moved one other byte (a good, a pool, a window accumulator, a carry, a history sample, the sale record) that line goes red. **Determinism (invariant 9)** green: guilds in array order, systems in the sorted order `stepProduction` already uses, ventures in establishment order, no clock read, and the lump is a sum so order cannot reach the number anyway. **535 tests (+17), zero failures.** ⚠️ **Deferred rather than invented — no engine question was answered silently.** Renegotiation and the `windowDays` deadline (#64) — the charge reads the LOCKED fees and re-prices nothing · reputation, breach marks, forced closure, investor dividends (§5 / #61 / Slice 5) — a breach charges the full fee **and nothing else** · a fee-preview endpoint · an atomic `establishAndLicence` · **the console/HUD fee display**: the record and snapshot field exist so a CLIENT slice can show the charge — *(**done in the very next commit on this branch**, so no stale copy ships: `console.html`'s Syndicate footer and `game.html`'s licence receipt now state the boundary charge, and the footer reports the guild's last one off `licenceFee`. See the entry above.)*
  - [x] **Licence Slice 3b-i — the licence entity + the fee math, nothing charged (landed 27-08-26; design `design.md` §5 "LICENCE FEE MECHANICS — RULED" + its four-corner grid, build note `docs/licence-fee.md`):** a mining venture is now licensed by a real **`applyForLicence`** action — the player path the throwaway `setSyndicateCommitment` scaffold stood in for (the scaffold stays, for tests and the bot). The player offers two terms, a share of **droidless baseline** output and a renegotiation window in days; the Syndicate does not negotiate, so everything else is computed at signing and stored in an embedded **`venture.licence`** = `{ committedOutputPct, windowDays, signedTick, lockedPrice, basicFee, discountedFee }`. **The fee:** `basicFee = feeRate × baseline-per-tick × N × the posted price at signing`, discounted by §5's four-corner grid `100/75/75/0` with the equity axis shaped `o' = oNorm^k` — so toe-dipping buys proportionally little and the payoff **accelerates** toward the 49% ceiling, while commitment stays linear and the corners stay exact (`0^k = 0`, `1^k = 1`). **The basis is the BASELINE, never `productionRate`** — a throttled mine owes exactly what a flat-out one of the same type owes, which a test pins. **Both fees are computed once and stored**: a test moves the market 9× up and 4× down afterwards and they do not budge — that is what makes *when* you sign a real decision (§5). Granting a licence **sets `syndicateCommitment` = `round(pct × baseline × N)`**, so it is what switches **3a's sale on**. **Refused, never clamped:** out-of-range terms, a factory (the §5 accrual sums `Q` over mines only, so a factory licence could never be delivered or judged — deferred until the accrual is extended), fuel (§8), a venture you don't own, and a **second application** (changing agreed terms is *renegotiation*, not built). `setWindowN` is now **also** refused once any licence exists — a locked fee and commitment are measured over one window, and moving `N` would silently make them describe a window that no longer exists. `venture.licence` is **omitted when absent**, so an unlicensed galaxy stays byte-identical. Snapshot gains `ventures[].licence` (or null); **additive, schema still 7**. `invariants.js` asserts the stored terms every tick (integer fees, discount ≤ basic, the ranges intake refuses outside of, a real locked price) — they are locked numbers nothing will ever re-derive, so a bad one would simply sit there until 3b-ii debited it. **CHARGES NOTHING**, and it is proven per-tick rather than asserted: across a 30-tick licensed run the guild's credit change equals 3a's sale **exactly, every tick**, window boundary included. ⚠️ **Flagged as a prerequisite for 3b-ii:** `windowFraction` stays pinned at 1 (no `committedFromTick`), so a venture licensed **mid-window** owes the whole window's `Q` and can breach its first window unavoidably — harmless while nothing is charged, unfair the moment the full basic fee is. 3b-ii needs either the mid-window **pro-rate** (§5's join ruling) or a **first-window grace**; both are rulings, so neither was invented here. **Proven** (`sim/tests/licence-fee.test.js`, +24): the four corners, the shaped interior, the accelerating relief, the grant/lock/commitment path, every refusal leaving state byte-identical, locked-at-issuance in both market directions, the sale switching on with invariant 2 exact, the no-charge proof, the unlicensed no-op/determinism proof, the snapshot surface, and five corrupted-licence tripwire cases. **390 tests (+24), zero failures.** By-hand run confirmed two mines on different terms — 105 of a 120 basic fee at 50%/no-equity, **0 of 120** at 100%/49% (§5's deep-entanglement corner) — earning through a window boundary with not one credit debited. **`[FIRST-CUT]` and recorded in `docs/phase-1-tuning.md`:** `FEE_RATE` 0.10, the corners, `k` = 2, the 0% commitment floor, the 7–42-day window bounds (the 49% ceiling is design, not a dial). §15.4's `Licence`-entity sketch is superseded by the embedded shape and now says so.
  - [x] **Licence Slice 3a — commitment-as-sale: committed goods earn credits (landed 26-08-26; design `docs/licence-and-price-system.md` Part 2 + `design.md` §5 "Output commitment", build note `docs/commitment-sale.md`):** the credit leg of the Syndicate fork delivery. Committed units already left the guild's pool every tick and were taken for **nothing**; now they are **bought**. The owner is paid `round((1 − o) × units × posted price)` into `guild.credits` and the Syndicate ledger is debited **the same integer** — one rounding, both legs, so **invariant 2 holds to the credit** (#43: `round(qty × price)`, identical both sides). **Two calls the contract left open are now pinned and reconciled into §5 itself:** "market rate" is the **POSTED** (two-ticks-lagged) price — the rate a sale executes at was fixed two ticks before the sale, which is what the publish lag was for — and the sale is **per tick**, not per cycle, because the delivery it pays for is already per tick (the *fee* stays on the 24h cycle, Slice 3b). **The equity lever is live:** `Venture.equityPct` (`o`, a fraction ≤ §5's structural **0.49** ceiling, set at establishment, **refused not clamped** above it — a silently-clamped offer would be the engine rewriting agreed terms). With no investors in existence the `o` share is paid to nobody: it simply never leaves the ledger (§5's "or, with none, to the Syndicate ledger"), so offering equity costs the owner from the first tick exactly as designed. Where two ventures committing the same good offer different equity, the split is **commitment-weighted** — the same arithmetic that built the good's aggregate `Q`. New **`sim/licence.js`** is the licence layer's home (the ceiling, the split, the rounding) and the socket Slice 3b's fee lands in; `Guild.lastSyndicateSale` records what the Syndicate bought, stamped with its producing tick, written lazily so an unlicensed guild carries no key. Snapshot gains `guilds[].syndicateSale` (with a `thisTick` flag so the console needn't compute it) and `ventures[].equityPct`; **additive, schema still 7**. **Charges nothing** — no fee, no breach penalty, no reputation, no investor payment (3b/4/5); a breaching guild is still **paid for what it did deliver**, which the breach test now asserts. **Proven** (`sim/tests/commitment-sale.test.js`, +19): the sale pays and conservation holds exactly · `o = 0` keeps all, `o = 0.4` keeps 60% with the rest staying in the ledger · no bystander guild is paid · the weighted split · an unlicensed venture moves nothing and the commitment-free boot is hash-identical · the sale uses the **posted** price (pinned) and the price it paid was fixed two ticks earlier · a missing price halts loudly rather than handing goods over free · determinism · integer credits through 30 ticks of fractional equity · the ceiling refused-not-clamped · out-of-range equity and a tampered sale record both trip new `invariants.js` tripwires. **366 tests (+19), zero failures.** The unlicensed golden-hash no-op proof is **untouched** and is now where the whole no-op meaning lives; the *committed* run's golden was deliberately retired and re-pinned, because this slice's entire purpose is that a committed delivery now pays. By-hand run confirmed two identical guilds differing only in `o` earning at exactly 100% and 60%, ledger negative by their sum. **Deferred rather than invented:** a runtime equity **setter** (§5 makes altering terms a *renegotiation*, window- and majority-gated — a free setter would pre-empt it), and the `licenceId`/`shareholders` fields, which are documented in `createVenture` rather than stubbed as empty objects that would sit in every save and in the determinism hash for a field nothing reads.
  - **Plan for the licence layer - the next section (SCOPE agreed 11-08-26 in design, so a later session doesn't relitigate):** the owner frames it as **"apply for a licence, then deploy the asset to fulfil it."** *Minimal viable scope* - what makes the console's commitment bar come alive and gives a bot a real "deliver vs. keep-and-pay" choice: (1) a **Licence entity** that makes a per-good commitment `Q` over window `N` a real, granted thing (today `Q` is a fed-in `syndicateCommitment` placeholder), and (2) a **breach fee** - at window end (`tick % N == 0`), `delivered < Q` **debits the guild's credits** (the credits ledger + conservation-of-credits invariant 2 already exist). *Explicitly DEFERRED - do NOT build here:* investing / equity / dividends (the guild part-owning the Syndicate); resource **pricing / trading / the fuel market** (a separate axis - step 3 / Stage 3); and **PostgreSQL persistence stays Phase 2** - the in-memory state-holder in `sim/server.js` is the designed swap-seam, so the bot loop runs **live-but-ephemeral** on it now and the DB is a later clean port once the licence entities stop moving. *Sequence for the section:* (1) the minimal licence layer (design **here** first, then a Code prompt); (2) **auto-tick** - the parked heartbeat (play/pause + interval in `sim/server.js`, still the one engine); (3) the **scripted bot** - testbed tooling (like `tools/generate_test_claims.js`) that reads `/snapshot` and POSTs `setProductionProfile`/`throttles` each tick to steer a guild toward meeting its licence while balancing reserve/downstream/stockpile (the long-wanted second/bot guild, tier-1/2); then (4, the Phase-1 -> **Phase 2** transition) persistence + Docker packaging. *Open for the next thread's design (settle on paper before any prompt - working practice #7):* the **Licence entity shape** + where `Q`/`N` live; the **apply-for-a-licence** action (who grants it, eligibility, any acquisition cost); the **breach-fee model** (flat vs shortfall-proportional) + its `[FIRST-CUT]` number -> `docs/phase-1-tuning.md`; and the **failure modes** - a licence over-committing a throttled line; a licensed venture destroyed mid-window; the fee driving credits negative and how invariant 2 handles it (the Syndicate ledger may go negative; guild credits?); and a bot structurally unable to meet `Q`. **DESIGNED 11-08-26 — the injection-scaffold sub-slice is settled (so a later session doesn't relitigate):** the FIRST thing built toward the licence layer is a deliberately throwaway *commitment-injection dev layer*, NOT the `Licence` entity — chosen to light the console's commitment bar and give a scripted bot real targets with minimal build. Two dev-scaffold actions on the live in-memory server: `setSyndicateCommitment {guildId, ventureId, commitment}` (per-venture, integer ≥ 0, `0` clears; REJECTED on a non-`resourceType` venture; does NOT set `committedFromTick`) and `setWindowN {windowN}` (engine-wide `state.windowN`, integer ≥ 1, REJECTED once `tick > 0`); two testbed controls (the player console is unchanged — its bar lights from the snapshot). NO engine/resolver/snapshot-schema change, NO fee, NO credits (breach stays status-only). The real `Licence` entity + breach fee + apply-workflow stay DEFERRED and become `syndicateCommitment`'s source later, retiring these setters. Tick-length (1 s/tick) belongs to the auto-tick slice, not this one. Detail: design.md §15.4 scaffold note (11-08-26).

**Follow-on decisions surfaced in the Stage 2 build (01-08-26)** — each needs a ruling before the code that consumes it (working practice #5); until then it is an explicit, flagged placeholder in the code, never a silent number:
- [x] **Starter mining rate** — **ruled**: Titanium's base extraction rate is **5/tick** (`[FIRST-CUT]`; recorded in phase-1-tuning.md, 01-08-26). Extraction rate is a per-resource property, not a mine tier; droid buffs remain out of scope. `sim/demo.js`'s quiet ticks now visibly accumulate Titanium while conservation holds.
- [x] **Waystation count + placement — resolved 03-08-26**: adopted the seed's **9 outposts** as the Syndicate waystations; the zero-state claims them by reference. No invented count — it is whatever the generator places, deterministically.
- **Home-node assignment on founding** — *partially resolved 03-08-26*: `foundGuild` now takes an explicit `homeSystemId`, validates it is a starter-eligible system that is unclaimed, seats the guild on that system's Terran homeworld, and adds a guild-owned home claim; the genesis claim does **not** grant the +20 (setup, not a play action). Still open: **bot** home auto-assignment (which of the 368 starters each bot gets), a later scenario concern.
- [x] **#22 Deuterium's identity — ruled 02-08-26**: `deuterium` (raw, mined) and `deuterium_fuel` (refined; the fuel held in reserve/hoards) are two distinct goods; the refined good is not minable and is in no archetype pool. The id string `deuterium_fuel` is a `[FIRST-CUT]` open to veto. Encoded in `sim/resources.js`; full ruling in design.md §20.
- [x] **Venture output model — decided 02-08-26**: the typeless per-venture `outputStockpile` is retired; produced goods are typed and flow into the owner guild's `stockpiles` (one home, no double-counting; producer attribution via `lifetimeProduced`). Reversible if a produced-but-unsold-at-venture state is later needed (design.md §15.4).
- **`percent`-of-fresh Syndicate send on a REFINED good** — surfaced by the factory-commitment slice (28-08-26) and deliberately NOT guessed. The `percent` control is a share of THIS tick's fresh, resolved in the resolver's routing pass; a refined good's real fresh is its factories' `minted`, which cannot exist until after that pass (a line's rate depends on the input allocations the pass produces). So a committed **factory** under `percent` intends **0** every tick and will breach, while paced (the default) and absolute are exact. Two honest fixes exist and both are rulings, not build details: **(a)** resolve a refined good's window in the finalize pass instead — one good's window then resolves in two different places depending on how it is produced, and the routing pass's `availableForConsumers` would be computed against a stale intent the moment a Tier-3 recipe gives a Tier-2 good consumers; or **(b)** rule that `percent` mode is simply not offered on a refined good, and refuse it at intake. Pinned meanwhile by "DEFERRED, PINNED" in `sim/tests/factory-commitment.test.js` so it cannot rot silently.
- **Syndicate craft speed vs. the seed's hex scale** — surfaced by the BUY engine slice (29-08-26) and deliberately NOT re-guessed. The ruled `[FIRST-CUT]` **300 ticks/hex** was chosen against design.md §6's intuition of journeys a few hexes long; the generated galaxy is far wider — on seed 7331 the nearest-waystation distance is **6 hexes at best, ~60 median, 155 at worst**, so a delivery takes **1,800–46,500 ticks (1.25–32 real-world days)**. Shipped as ruled and pinned by a test that states the number. The ruling needed is either a much smaller ticks-per-hex or a distance that is not raw hexes (a log, or per-ring bands) — both are numbers, so neither was chosen in code. Full note in `docs/phase-1-tuning.md` §"World shape".
- **Does the Citadel count as a Syndicate waystation?** — surfaced by the BUY engine slice (29-08-26). `nearestWaystation` searches the seed's **9 outposts** only, because the count/placement ruling (03-08-26) reads "the seed's **Citadel + its 9 Syndicate outposts** (the waystations)" — the Citadel named *beside* them, not among them. Including it would shorten deliveries to systems near the galactic origin and is a one-line change; it is a design ruling about what the Citadel *is*, not a code choice, so it is asked here rather than assumed.
- **Fuel price inputs** — surfaced by the representation slice, deferred to the pricing step: the committed `fuel-allocation-model.md` floats fuel price on the **reserve pool alone**, while the working rule from the 02-08-26 design chat wants **reserve + guild hoards**. `galacticSupply.fuel` already reports both figures; which the price reads is a Stage-3 ruling.
- **Input-priority flow for competing production lines** — NO LONGER PARKED (05-08-26): now designed as **Gate 3** of the production flow (design.md §5, "The Production Flow") and building as the resource-processing pipeline slices. FCFS-in-establishment-order is retained as the documented **default**. Original note kept below for the history. (flagged 04-08-26). When several of a guild's refineries draw the SAME input from its shared stockpile, they currently run in **venture-establishment order** within the production step, so the earliest-established line wins a scarce input and downstream lines can starve. This is honest emergent behaviour, not a bug, but a **priority mechanic** — letting a guild rank which lines get first call on a contested input (or split it by a policy) — is a real finer-gameplay lever for later. Deferred until it's wanted; when built, it needs a deterministic tie-break and its own tripwire. (Noted so the observation isn't lost; no ruling required yet.)
- [x] **Settlement-slot counts — ruled 02-08-26** `[FIRST-CUT]`: per-planet non-mining build spaces by archetype — Terran 15; rocky/desert 7–10; ice/crystalline 4–7; oceanic 3–4; gasGiant/molten/irradiated 0. In the generator (`SETTLEMENT_SLOTS`) and `docs/phase-1-tuning.md`; open to a veto.
- [x] **Venture seating model — decided 02-08-26**: a venture's `siteId` (resource node or settlement slot) is the authoritative location; occupancy is derived (`sim/occupancy.js`), never stored on the site (design.md §15). `resourceType` stays a denormalised copy of the node's, guarded by the site-occupancy invariant. The tick loop stays seed-free.

  - [x] **Client-wiring section — COMPLETE 25-08-26. The game client becomes the game (designed 24-08-26, `docs/client-wiring.md`):** turns the deployed dev rig into the actual game, PLAYED THROUGH THE PLAYER CLIENT — the whole core loop (found → explore → establish → produce → persist) done by a player in the browser, with a purpose-built **watch-only** operator panel at `/inspect` replacing the DELETED `client/testbed.html`. Governing principles (full form in the doc): the game client is the **only way to ACT** (nothing builds through a dev panel); the watcher only watches (`/inspect` has zero controls); **two doors only** (`/` player client, `/inspect` watcher); the **clock auto-starts on boot** (`STARFARE_TICK_MS` — operator config, not tuning, per §15.7); **demos DELETED** (the client loads the real committed `data/seed.json` via a new **`GET /galaxy`**, the same seed `sim/seed.js` reads); **player-safe vs god's-eye is a real split** (the client reads the *player projection*; `/inspect` is the operator/engine read plane the Storyteller + Syndicate pricing will later consume; real per-viewer server-side filtering DEFERRED to Phase 2/3); **wire, don't rebuild** (`foundGuild`/`establishVenture`/`setProductionProfile` already exist + tested, `console.html` already HTTP-wired behind its mock — engine/tick/snapshot-schema untouched, client computes no game number §5); **verify by PLAYING, not watching numbers**. Five deployable slices, each its own Code prompt + PR: **(0)** `GET /galaxy` + the `/inspect` watcher + clock-on-boot; **(1)** found-your-guild from the client + map/HUD live + demos deleted + **testbed deleted** + front-door flip; **(2)** system/planet drill-down (seed geometry + live occupancy); **(3)** the establishment popup → `establishVenture` (licence terms **MOCK** over a real venture); **(4)** console un-mock (iframe → served `/console`). OUT OF SCOPE (later, reached by playing this): real licence economics, multi-user/auth, bots, market, transport/tolls, narrative, server-side per-viewer projection. Detail + the player-projection field contract: `docs/client-wiring.md`.

    - [x] **Console fix — the three distribution sliders drag properly (25-08-26):** the Production (`downstreamPct`), Stockpile (`reserveLevel`) and Syndicate (`syndicate` send) sliders could not be dragged to a partial value — every drag pinned to the maximum (100% / all). **Root cause, and it was not the value math:** each `mousemove` POSTed, `sendAction` re-renders on the POST response, the re-render rebuilt the DOM and **detached** the very track element the drag was holding, and a detached node's `getBoundingClientRect()` is all zeros — so `(clientX - left) / width` was `Infinity`, clamped to `1`, i.e. the max. The ~1 s LIVE poll re-rendered too and compounded it, but the per-POST re-render alone broke it, so it was broken with View: PAUSED as well. **Three-part fix, applied to all three tracks through one shared `trackDrag` helper:** (1) the track's rect is captured ONCE at `mousedown` and the drag fraction is computed from that captured rect (a zero-width capture does nothing — never divide by zero); (2) `mousemove` paints the fill/handle/label **locally only** and POSTs nothing — the single `setProductionProfile` fires on `mouseup`, so a drag that used to send 13 POSTs now sends **one** (a plain click still commits on the `mouseup` that ends it); (3) a module-level `isDragging` flag makes `livePoll` skip its `render()` for the duration, so a live re-render can never snap the handle mid-drag. Each track's value math is **unchanged** (`downstreamPct = round(f*100)`, `reserveLevel = round(f*out)`, syndicate `= round(f*fresh)`), as are the native `change`-driven controls and the `.sy-req` / REQUIRED snap clicks. Client-only: one file (`client/console.html`), no `sim/` change, engine/server/snapshot untouched, schema still 7, **323 tests unchanged**. Verified by hand in a real browser against a producing titanium mine, LIVE and PAUSED and in embed mode — before: `90, 100, 100, 100 …` (13 POSTs, stored 100); after: a single `downstreamPct: 30` for a 90%→30% drag, stored 30. Detail: the revision note at the foot of `docs/client-wiring.md`.
    - [x] **Slice 4 — un-mock the console (landed 25-08-26) — THE CLIENT-WIRING SECTION IS COMPLETE:** `client/game.html`'s `mountConsole` now points the console iframe at the **served `/console`**, carrying the focus as `?guild=<id>&system=<id>` (same origin, so the console reads `/snapshot` and POSTs `setProductionProfile` against this galaxy), and re-points whenever the focused system changes. The **embedded mock blob is deleted** — the base64 string, the `atob` decode, `URL.createObjectURL`, `window.__consoleBlobUrl` and the Slice-1 MOCK tag — dropping **~193 KB** from `client/game.html`; the phantom "Weaver Guild" refinery is gone and the console shows the same real ventures as the manifest and `/inspect`. `client/console.html` reads the query string on boot and **pins** that system (a pinned system with no ventures renders the honest idle state rather than silently switching to home; with no params it self-selects exactly as before), and re-emits the one cross-frame message the client listens for — `{source:'starfare-console', kind:'venture', name, facility}` — on render for the focused system's first producer and on a click of any venture row, so the industrial hero art follows the venture in hand (`game.html`'s listener and `__VENTURE_ART` untouched). **One judgement call, flagged:** embedded, the console hides its two OPERATOR drives (TICK, RESET) — neither is a player move and a persistent world is not a player's to step or erase; standalone `/console` keeps them. No `sim/` change: engine, tick and snapshot untouched, schema still 7. **With this the whole core loop — found → explore → establish → produce/tune — runs through the client on the live persistent galaxy, with `/inspect` as the operator watcher.**
    - [x] **Slice 3 — establish a venture, through the game (landed 24-08-26):** the designed establishment popup (`docs/mockups/venture-establishment.html`) is ported into `client/game.html` — same palette, tokens and fonts the detail screen already declares, no second design system — and opens from a **VACANT** site in the Slice-2 manifest (a resource node ⇒ mining, Tier 1, fixed resource; a settlement slot ⇒ refining, tier + recipe). **ONE thing is real:** Deploy POSTs `establishVenture` through the existing `/action` with exactly the fields `sim/actions.js` validates — `{type, guildId, ventureId: <guildId>_<siteId>, siteId, ventureType, resourceType|recipeId, productionRate}` — and nothing else; on `accepted:true` the client re-reads `/snapshot` and the manifest flips to YOUR MINE / YOUR REFINERY, producing next tick, and on `accepted:false` the refusal reason is shown instead of success. The **recipe picker is real** (the live `GET /recipes` catalog, id + inputs → output; Tier 3/4 are present but disabled, "not yet built" §4). **Everything economic is MOCK and tagged as such on screen** — the asset picker/inventory, licensed/unlicensed, output commitment, equity, the licence fee and its four-corner hinge graph (ported intact, `K=2`, corners 100/75/75/0), the breach fee, reputation and the renegotiation window: design-ahead of the engine (`venture-establishment.md` §2.4–2.6, design.md §5), carrying their `[FIRST-CUT]` tags, and **sent nowhere**. `syndicateCommitment` stays 0 — **superseded 27-08-26** by the *Player licence panel wired* entry above: licensed/unlicensed, output commitment, equity, the renegotiation window and the fee are now real (the fee locked, not yet charged), and only the asset picker, the breach fee and reputation remain mock; no `assetId` is sent (reserved, §4); the success screen's "Open venture management" stays a stub (undesigned, §2.7). Establishment is deliberately **UNGATED**: a player may establish inside another guild's claim — the popup *shows* whose territory it is and does not block, because leasing is the future economic lever (`venture-establishment.md` §4), not a prohibition. The client's uniform `productionRate` **5** is a `[FIRST-CUT]` recorded in `docs/phase-1-tuning.md`. **No server file changed** — there is no new endpoint; engine, tick and snapshot untouched, schema still 7.
    - [x] **Slice 2 — the drill-down goes live on real occupancy (landed 24-08-26):** the system → planet manifest now shows the truth — seed geometry (already in memory from `GET /galaxy`; **no new `/system/:id` round-trip**) joined to the snapshot's `occupancy` by **site id**, which `adaptSeedSystem()` now preserves on every resource node and settlement slot. Each row reads **VACANT**, **YOUR MINE / YOUR REFINERY** (venture id + the resource, or the recipe's output good, from a one-off `GET /recipes` read of the static catalog), or **OCCUPIED · <guild>** for a rival's site — that it is taken and by whom, and deliberately nothing else (§7). The poll's `LIVE` now carries `occupancy` verbatim plus a player-safe venture view (own: `id`/`siteId`/`type`/`resourceType`/`recipeId`; others: owner identity only — no rate, no resource, no recipe, no stockpiles). Slice 1's "live data next" placeholder and the node overlay's "occupancy goes live next" copy are replaced by the real readout; an open manifest re-renders only when occupancy actually moves. A vacant site is reported honestly and is **not yet actionable** — establishing is Slice 3. **Folded-in server ruling:** `POST /reset` now **re-arms the boot clock** (`bootTickMs` hoisted to module scope; reset still stops-then-zeroes, then restarts auto-tick if `STARFARE_TICK_MS` was set at boot; unset ⇒ off, manual dev flow unchanged), so a deployed galaxy is never left frozen — this revises Slice 0 / design.md §15.7's "reset stops the heartbeat" for a booted deployment. Engine, tick and snapshot untouched; schema still 7.
    - [x] **Slice 1 — found your guild + the live galaxy, through the game (landed 24-08-26):** `docs/mockups/starfare-client-full.html` moved to **`client/game.html`** and is now served at **`GET /`** — the front door is the game. **`client/testbed.html` is DELETED** (its watch role was replaced by `/inspect` in Slice 0; its action controls become real player surfaces, slice by slice), and a path-traversal-safe `GET /assets/<path>` route serves the client's art. The **demo galaxy is deleted**, not bypassed: `buildDemoGalaxy()`, `__demoSeed`, the two seed/claims file-pickers and every baked identity literal (guild "Vanguard", 4,200 credits, tick 1,204) are gone — the map loads the real committed seed from `GET /galaxy` (its first full-scale render: 1,500 systems, 5,290 planets, 9 waystations, the Citadel) and the HUD reads the player's own `name`/`credits`, `tick`, and `claims[]` from a ~1 s `/snapshot` poll. The connect screen is a **real first-run flow**: no guild ⇒ pick a starter (`GET /starters`) + name it ⇒ POST `foundGuild` with **2,000** starting credits (`[FIRST-CUT]`, recorded in `docs/phase-1-tuning.md`, debited from the Syndicate ledger by the engine); a guild already present ⇒ adopt it (single-guild dev rig; real per-player identity stays Phase 3). Territory colours from the live claims — the player's own systems in the player accent, rivals by a stable per-id hue. The client computes **no game number** and reads only the player-safe projection (§7). Engine, tick and snapshot untouched; schema still 7. Out of scope and NOT faked: the detail screen's live occupancy (Slice 2) — its "VACANT" labels were neutralised to a *live data next* placeholder rather than guessed — establishing ventures (Slice 3), and the console, which stays the mock blob behind a visible MOCK tag (Slice 4).
    - [x] **Slice 0 — plumbing + the watcher (landed 24-08-26):** `GET /galaxy` serves the committed `data/seed.json` verbatim (read once at boot, the SAME bytes `sim/seed.js` parses, so map and detail can never disagree on geometry); `GET /inspect` serves the new **watch-only** operator panel `client/inspect.html` (a ~1 s poll of `/snapshot`, the FULL god's-eye render — supply, ledger, every guild's credits/stockpiles/`stockpilesBySystem`/`productionProfile`, the whole `production` block, ventures, occupancy, claims — **zero action controls**, tripwired by asserting the served bytes name no mutating route); and the auto-tick heartbeat now **starts on boot** from `STARFARE_TICK_MS` (operator config: unset ⇒ off, byte-for-byte today's behaviour; an unusable value fails the boot loudly; the EXISTING `startAutotick`/`tickOnce` are reused unchanged). Engine and snapshot untouched — schema still 7. `client/state_inspector.html` stays as the offline file-load debug tool; `client/testbed.html` is still the only seeding path and `/` is still the testbed — both change in Slice 1.

  - **Galaxy lifecycle + admin panel (designed 25-08-26, `docs/galaxy-lifecycle.md`):** a server hosts ONE galaxy with a runtime **create → play → delete** lifecycle from an **admin panel** (the `/inspect` watcher grows operator controls; the player portal `/` stays free of destructive controls). The **seed joins the persisted save** (active galaxy = `{seed.json,state.json,journal.jsonl}` in the volume): empty volume = **NO GALAXY** → operator **Create Galaxy** (optional seed number, else randomised, always shown as `SEED ####`) runs `generateGalaxySeed` (waystations/Citadel included), reloads the seed index, builds a fresh zero-state re-planting the Syndicate claims on the new landmarks, persists, and ticks from **0** regardless of players; **Delete Galaxy** clears it. Atomic (never new-seed-with-old-state). Tick cadence `STARFARE_TICK_MS` (production 60000 = 1/min). No app auth yet (Cloudflare Access; guild = HUD identity). Sliced: **A** engine/server, **B** client. Detail: `docs/galaxy-lifecycle.md`.
    - [x] **Slice A — engine/server (landed 25-08-26):** the seed source is now settable at runtime (`sim/seed.js` `setSeed(seedObject)` invalidates the cached index; with none set it still falls back to the committed `data/seed.json`, so every test and the no-volume dev run are unchanged). The seed joins the save: `sim/persist.js` gains `saveSeed`/`loadSeed`/`deleteGalaxy`, so the active galaxy is `{seed.json, state.json, journal.jsonl}` in `STARFARE_PERSIST_DIR`. `sim/server.js` boots into **NO-GALAXY** (`liveState === null`, clock ARMED not started) when the volume holds no seed, or **ACTIVE** when it does; every endpoint answers `{state:'no-galaxy'}` cleanly instead of throwing (`/galaxy` and the seed reads 404, `/snapshot` 200 with the marker, the mutating routes 409). **`POST /admin/galaxy/new {seed?}`** runs the atomic sequence — stop clock → resolve the number (given, or randomised) → `generateGalaxySeed` → `saveSeed` → `setSeed` → fresh `createZeroState()` on the NEW seed → `saveState` + `clearJournal` → start the clock, ticking from 0 regardless of guilds — and **`POST /admin/galaxy/delete`** stops the clock, removes the trio and returns to NO-GALAXY. A **consistency guard** on boot refuses to load a seed paired with another galaxy's state (an interrupted Create): it rebuilds a fresh zero-state on the active seed, loudly, and persists the repair. `/health` gains `galaxy`, `seed`, `uptimeSeconds` and `serverTime`. The pure engine (`tick.js`/`run.js`/`production.js`/`actions.js`/`snapshot.js`/`state.js`) is untouched and the schema stays 7. **323 tests, zero failures**, including a back-compat proof that the no-volume path is hash-identical. Slice B (the admin panel UI) is next.
    - [x] **Slice B — admin panel, onboarding & HUD (client) (landed 25-08-26):** Slice A's endpoints became UI, with no `sim/` change at all. **`/inspect` becomes the admin panel** (`galaxy-lifecycle.md`, which revises client-wiring.md §2's "the watcher only watches"): it now reads `GET /health` alongside `/snapshot` on the same ~1 s cadence and shows the lifecycle state — `ACTIVE — SEED ####` or `NO GALAXY` — plus tick, guilds, the server's wall clock and its uptime as `DD:HH:MM:SS`; a seed field (blank randomises) with **Create Galaxy**, and **Delete Galaxy**, both behind an explicit destructive confirm that names what is destroyed (Create is offered in *both* states because creating over an active galaxy replaces it; Delete is disabled when there is nothing to delete), and both re-read the server afterwards rather than trusting their own optimism. The watch loop now detects the `{state:'no-galaxy'}` body **before** `validate()` — it is a normal HTTP-200 answer, not a bad snapshot — and **hides** the god's-eye panels rather than leaving a deleted galaxy's figures on screen. The Slice-0 tripwire still holds: the served bytes name no `/action`, `/tick`, `/reset` or `/autotick`, so the panel may create or delete a galaxy but still cannot advance one, act inside one, or reset one. **The player client (`/`) gained no admin control** (asserted by hand: `/admin/galaxy` appears nowhere in its DOM). It now reads the **lifecycle first**: NO-GALAXY is a calm waiting state — "the operator hasn't opened a galaxy yet" — that re-asks `/health` and connects by itself the moment a galaxy exists, and deliberately fetches neither `/galaxy` nor `/starters` while waiting (both 404 by design there); the live poll treats the no-galaxy snapshot body the same way, dropping the map and returning to the waiting screen if a galaxy is deleted underneath a player. **Onboarding** now offers only the **unsettled** starters — the seed's starter list minus every system id in the snapshot's `claims[]` — each labelled by its ring, and says so when none is left. **HUD labels** read `SEED ####`, `Player Guild: <name>` (or an **Establish Guild** affordance where the name would sit, which returns to the connect screen), `Server Time HH:MM` and `Server Uptime DD:HH:MM:SS` — all four from `/health`, none computed here; the tick counter still comes from the snapshot. Verified by hand end to end on a real server with a boot clock: NO GALAXY → create seed 4242 → the waiting player connects on its own → 388 starters offered, ringed → found → HUD live → delete under the player → waiting again → create on a randomised seed → fresh onboarding; the starter filter proved on live data (388 offered → 387 with one home claimed). **323 tests, zero failures** — `sim/` untouched, schema still 7.

### Stage 3 — The sandbox

- [ ] Full roster: five spreadsheet guilds (bot) + player guild; full graph
- [ ] **Squeeze regression check**: headless scripted replay of the sheet's 12-turn action grid reproduces the signature — price 5 → 7.03 → 16.75 → 38.48 peak → recovery; Dracis shortfalls 6/7/2; Refiner finishes richer than the Stockpiler (design.md §8)
- [ ] Territory claim/contest working (influence-commitment contests; negotiated transfer)
- [ ] Tolls and tariffs settable, negotiable with bots; disposition moves on costly actions, decays, stays capped, never touches prices
- [ ] Ventures with automation parameters (production is renewable-powered — no fuel/energy draw; fuel is burned by spacecraft only, §3 / #54)
- [ ] UI: map, guild dashboard, market panel with price history, territory/negotiation panels, event log, fast-forward
- [ ] Boundary test: territory suite passes against **two** world adapters (the real graph + a toy topology)
- [ ] Play sessions logged: can the player cause a squeeze? resist one? does #31's answer change when territory levers exist?

### Definition of done (from the build prompt, unchanged)

- [ ] Tests exist and run; invariants 1, 2, 3, 9 assert every tick and halt loudly
- [ ] Same starting state, run twice, byte-identical at tick N
- [ ] Tick loop pure, no DOM; all eight steps named functions in fixed order
- [ ] The squeeze reproduces the three signature results as an internal check
- [ ] A real session is playable: claim/contest, set/negotiate tolls and tariffs, watch disposition move, cause or fight a squeeze
- [ ] Territory/toll logic sits behind the world boundary; swapping in hex geography later shouldn't touch it
- [ ] **design.md updated in the same delivery**: what the phase taught, #31's answer as observed in play, #2's formula as now implemented, every tuning number decided, and the widened-scope note kept accurate

**Exit criteria:** the definition of done, plus an honest written answer to "does the shape of this game feel right to actually play."

---

## Phase 2 — Persist and tick on a server

**Goal:** close the tab, come back an hour later, something happened.
**Entry:** Phase 1 done (the loop being pure and DOM-free is what makes this phase a port, not a rewrite).
**Persistence/runtime ruling:** in-memory state + per-tick snapshot + action journal (B+); see `docs/persistence-model.md`.

- [ ] Node server hosting the existing `sim/` loop unmodified
- [ ] Real heartbeat (working assumption: one minute — confirm #5), with the tick as a *checking* clock: due-tick items resolve on their own deadlines
- [ ] Crash-safe ticking: restart knows exactly which tick it was on; never double-applies or skips
- [ ] PostgreSQL schema for the §15.4 entities; transactions as the mechanical answer to invariant 4
- [ ] Migration: in-memory Phase 1 state → persisted live state; seed remains a JSON file, linked by reference only
- [ ] HTTP client: refresh-and-see, no WebSockets yet
- [ ] Invariant checks running server-side every tick, with violation halting + alerting
- [x] Decide `generatedAt`'s fate in the seed — **resolved early 20-07-26: dropped** from all generators + committed fixtures; file is now byte-identical across runs (design.md §16). *Residual:* `test_claims.json` still embeds an environment-dependent `seedFile` path — separate open determinism question, not yet ruled.

**Exit criteria:** the milestone sentence at the top, plus a week of unattended ticking with zero invariant violations.

---

## Track G — Galaxy generation recovery *(parallel track)*

**Goal:** re-implement the lost 15-07 generator work against its surviving spec (design.md §2, §13, §16), and make the galaxy layer visible for the first time.
**Entry:** none — schedulable any time. **Required before Phase 4.**

- [x] Searched the local machine (17-07-26): recovered `client/system_planet_ui_mockup.html` and a partial 15-07-era generator. Its archetype/resource-node/starter/validation logic (`validation: PASSED` on seed 7331) has now been **consolidated into `tools/generate_seed.js`** (01-08-26), with Terran corrected to §2 and applied as a decorating pass that preserves the base skeleton; the partial file was removed. The old **5,089-vs-5,290 planet-count discrepancy is resolved** (one generator, 5,290; the 15-07 build's ~393-starter figure was its own generator's and is superseded — this one yields 337). **Still missing:** ring classification, the rare-tier ×4/×1/×0.25 gradient, and the repair pass — §2's 41%/3.6% figures remain the verification targets for the next slice. `galaxy-map-hex.html` and `SyndicateMarketplace.jsx` remain unrecovered.
- [x] **Archetypes + resource nodes landed (01-08-26):** steps 5, 6, 8, 10 consolidated into `generate_seed.js` as a skeleton-preserving decorating pass; `data/seed.json` regenerated (5,290 planets, 23,420 nodes, 337 starter systems, `validation: PASSED`); Terran corrected to §2. Remaining below: rings + gradient + repair + the 41%/3.6% verification + viewer/UI rendering.
- [x] Ring classification (inner/middle/outer by Citadel distance) — pure geometry, no RNG; `system.ring` set at build time so the base skeleton stays byte-identical (02-08-26)
- [x] Nine archetypes, weighted draw, rare-tier ring multipliers (×4 / ×1 / ×0.25) — measures ~38.7% inner / ~14.4% middle / ~4.3% outer rare-tier on seed 7331 (02-08-26)
- [x] Resource-node generation: archetype pools + ranges; Terran guaranteed minimums (incl. Gold/Silver/Tungsten) + 3 random extras; Oceanic ≥2 Deuterium; 10-node cap asserted at startup, Terran the sole named exception
- [x] **Settlement slots landed (02-08-26):** per-planet non-mining build spaces (`settlementSlots`, ids `_sNN`), count by archetype (Terran 15; rocky/desert 7–10; ice/crystalline 4–7; oceanic 3–4; gasGiant/molten/irradiated 0) `[FIRST-CUT]`; added as a decorate pass AFTER repair so every prior field stays byte-identical — `data/seed.json` regenerated (32,813 slots), determinism re-verified, generator tripwire covers the counts/ids. Drives where refinery/factory/construction ventures can later be seated (consumed live via `sim/seed.js`).
- [x] Starter-eligible tagging: system has ≥1 Terran planet
- [x] Rare-tier repair pass (≥2 rare-tier within ⅓ radius of every starter system) — fires zero times on 7331; 359 repairs at a stress floor of 50, still valid (02-08-26)
- [x] Validation pass: every resourceType reachable, starter pool non-empty and spread across all 16 sectors, rare-tier guarantee post-repair; results in `validation.passed`/`validation.issues` (02-08-26)
- [x] Verified on seed 7331 (02-08-26): **368** starter systems (the 393 was a different, unrecovered build); rare-tier spread ~38.7% inner / ~4.3% outer (9.0×), close to the ~41%/~3.6% targets and exactly what ×4/×1/×0.25 predict; repair pass fires zero times; stress floor of 50 applies 359 repairs and still validates
- [x] Determinism re-verified: same seed → byte-identical galaxy; `data/test_claims.json` still byte-identical (skeleton unchanged) (02-08-26)
- [x] Viewer renders `ring` (#34) — System View header shows INNER/MIDDLE/OUTER and the map-side info panel carries a Ring row; verified in-browser (02-08-26)
- [x] Index UI (System View) wired to real seed data (#34, #35) — the mockup's system/planet view is ported into `seed_viewer.html` as a `#detail-screen` overlay, opened by OPEN DETAILS on a clicked system and fed that system's archetypes + resource nodes; exit returns to the map (01-08-26)
- [x] System View verified rendering in-browser (02-08-26) — fixed two defects in the port: a dropped `</div>` that left the overlay unclosed (swallowing the page scripts), and a CSS comment containing `*/` that voided the `#detail-screen` rule so the overlay never positioned over the map
- [x] Decided (04-08-26): **retire** the standalone `system_planet_ui_mockup.html` — its System View lives in `seed_viewer.html` (wired to real seed data), so the standalone file (on hardcoded demo data) was removed to prevent drift.
- [ ] Decide **which rare tier a repaired planet becomes** — the repair pass (design.md §2, §16 step 9) currently makes a base-weighted draw among molten/irradiated/crystalline. Unobservable on seed 7331 (repair never fires), so this is a flag, not a live number; a ruling of "accept the base-weighted draw" is a valid answer.
- [ ] Decide node **richness/yield** (slice A left it ungenerated — nodes carry only `id` + `resourceType`; a node's richness is an undecided number kept out of the canonical seed until designed)
- [ ] Decide `Planet.stats`' fate (#33): build or delete

**Exit criteria:** a committed seed regenerable from a published integer, with the galaxy layer visible in at least one client.

---

## Phase 3 — Multiplayer foundations

**Goal:** multiple humans share one galaxy against the same market, and the atomicity invariant carries real weight.
**Entry:** Phase 2 done.

- [ ] Accounts, login, sessions
- [ ] Concurrent action intake through the same validate-as-it-arrives discipline, now under real concurrency (Postgres transactions doing the work)
- [ ] The Syndicate Exchange's **player order book** (design.md §5): asks/bids, conventional sorting, divergence from the Syndicate value line
- [ ] Partial-transparency rules enforced server-side (stockpiles hidden, venture types visible)
- [ ] Adversarial self-testing: simulated ~200-actor squeeze against a single shared row (design.md §15.7) — the measurement deciding whether the escape hatches are needed, **and invariant 4's (atomicity of shared-state mutation) mechanical tripwire**: the money-printer/dupe class of bug it names, uncatchable until real concurrency exists
- [ ] First population test at ~5 humans (#12)

**Exit criteria:** a weekend playtest with ≥3 humans, zero conservation violations, and at least one genuinely felt betrayal.

---

## Phase 4 — Territory, routes, tolls, travel time (the real map)

**Goal:** the map graduates from a picture into a system players contest.
**Entry:** Phase 3 done; Track G done.

- [ ] World adapter swap: graph → hex galaxy; the Phase 1 boundary test is the proof this doesn't touch economic logic
- [ ] Claims live on the Territory Map (one row per system/outpost/gate; bare-hex ownership derived, never stored)
- [ ] Outposts (anchor ≤10 of an own system) and Toll Gates (≤10 of an own system *or* outpost) as placeable live-state claims; Tier‑4 construction kits as their build path
- [ ] Toll Paths: strictly gate-to-gate; single risk profile known at entry; one roll at entry, no per-tick attention
- [ ] Shipments with real travel time; the **change calculator** built at route-segment granularity (open space per-segment checks vs toll-path single roll) — resolving #7
- [ ] Pirate-bot ambient hazard (basic form) so open space costs something; attributable vs non-attributable line enforced
- [ ] Syndicate public transport as a real round-trip Shipment (§6 terms); `feePercent` and hauler speed decided (#27 residue)
- [ ] Vehicle gate honored: Syndicate never ferries deployable assets
- [ ] Legality hooks stubbed (defiance is *recorded* even though courts don't exist until Phase 5)

**Exit criteria:** a toll empire is buildable, avoidable, and worth arguing about — traffic genuinely chooses between paying and flying dark.

---

## Phase 5 — The political layer

**Goal:** council sessions people scheme about.
**Entry:** Phase 4 done (disputes need something real to dispute).

- [ ] Council votes: influence-weighted, real time windows, zero-sum/resource-constrained framing
- [ ] Legality disputes over recorded defiance (mining without licence, blockade-running); everyone-votes model (#3) unless play shows otherwise
- [ ] Fines: legislated schedules, compounding repeats, paid to victims (conservation-safe), pile-on guard
- [ ] Forced-compliance rulings; precedent with decay
- [ ] Vote-weight integrity (invariant 6) tested under influence spent-vs-voted races
- [ ] Vote-window fairness mitigation chosen (#14)

**Exit criteria:** a real dispute resolved by a real vote whose outcome someone lobbied for.

---

## Phase 6 — Bots and the Storyteller

**Goal:** sparse servers feel alive; leaders feel hunted; nobody gets taxed by rule.
**Entry:** Phase 5 done (bots need politics to have standing interests in).

- [ ] Bot tiers 1–3 complete (Phase 1's tier 1–2 work graduates; tier 3 filler factions added)
- [ ] Disposition system finished and feeding vote behavior (#11 numbers finalized from Phase 1 experience)
- [ ] Pirate bots complete: event-driven escalation, fencing/black market
- [ ] Storyteller: signals (concentration, Gini, time-since-loss, stagnation), surface-area targeting, disposition-fed weights, ~70/30 telegraphed/sudden, named presets (#10)
- [ ] Events redistribute opportunity, not just delete value — verified against play logs
- [ ] *(Stretch / v2)*: the narrative layer — Rebellion containment tax, cover missions, intel/evidence, the Ancients (#16–21)

**Exit criteria:** a server left alone for a week produces a story worth retelling.

---

## Phase 7 — Self-hosting, config, onboarding, polish

**Goal:** someone who isn't the developer runs a galaxy, and someone who's never read the design doc learns the game by playing it.
**Entry:** Phase 6 done.

- [ ] Server config surface: fine severities, vote windows, tick rate, starting resources, bot density, storyteller presets
- [ ] Onboarding live: reserved starter systems, homeworld placement, vehicle gate, Titanium starter quest against `lifetimeProduced`, archetype-neutral kit; the second-vehicle path decided (#8)
- [ ] Legibility pass: dashboard/notifications for the many-clocks tension
- [ ] Deployment shape documented for self-hosters; seed shareability by number
- [ ] *(Later)* master-server listing layer

**Exit criteria:** one self-hosted galaxy running that the developer didn't set up.

---

## Standing rules (all phases)

1. **The repo is the only memory.** No artifact leaves a session unsaved. The doc updates in the same commit as the code it describes.
2. **Sequential vertical slices**, never parallel modules — the seams are the hard part and no session owns a seam.
3. **Tests are the tripwires**, AI review is a supplement. When unsure what to test: one of the nine invariants.
4. **Never invent a number silently.** Unsourced constants go to the decision checklist and get a ruling.
5. **Tag phase gates** in git (`phase1-stage2`, `phase1-done`, …) so any past playable state is one checkout away.
