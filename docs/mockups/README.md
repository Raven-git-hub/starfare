# Mockups — visual references, not wired code

**console_mock6_1.html** — the Archive-styled visual target for the player-facing
System Production Console. VISUAL reference ONLY: its hand-coded data model predates
Slice A (a stockpile %/Q fork + a separate reserve floor; a Production %/Q fork) and it
computes the Syndicate pace client-side. The real console (client/console.html) matches
this LOOK but wires every value to the live schema-7 snapshot and computes NO game number
— see the build prompt and design.md §5 / §15.4. Match the look; do not copy the model.
