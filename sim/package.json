{
  "name": "starfare-sim",
  "version": "0.0.1",
  "private": true,
  "description": "Phase 1 economy engine — pure tick loop + invariant harness, plus a thin dev-harness testbed server. No DOM in the engine.",
  "scripts": {
    "test": "node --test"
  }
}
